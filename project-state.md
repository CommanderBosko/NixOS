# NixOS Project State

_Last updated: 2026-09-20 (session 113)_

## Current Project State

_Older blocks are in [project-state-archive.md](project-state-archive.md)._

**2026-09-20 flake bump landed (`f978b9c`, not activated); pi-hole's list-sync config repaired and gravity rebuilt; xwayland-satellite pin re-checked twice, still holding (2026-09-17 evening through 2026-09-20, session 113).**
- **`/flake-update-verify` (Training Mode OFF, commit gate approved) bumped 6 inputs + 1 transitive** — `disko`, `dms` (+ `dank-qml-common` via dms), `financeguru`, `home-manager`, `nixpkgs`, `sops-nix`. `nix flake check` and a per-host `toplevel.drvPath` deep-eval both passed on all 4 hosts; committed lock-only and pushed as `f978b9c`. **Not applied to any host** — it stacks on the 2026-09-07 bump (PR #21) that's also still unapplied, so one `/fleet-rollout` covers both. vpn-server's eval is unchanged because it sits on the pinned `nixpkgs-stable`. No pin was active in the lock going in (the run's step-1 memory check found none).
- **pi-hole `pihole-updatelists` config repaired — no repo change, all on the pi-hole host.** The user's "`pihole-updatelists` alias" is really `/usr/local/sbin/pihole-updatelists` (jacklul's tool, driven by a systemd timer; config `/etc/pihole-updatelists.conf`) — there is no shell alias. Its `BLOCKLISTS_URL` was missing **HaGeZi TIF**, which had been added to the pi-hole itself via REST API on 2026-08-25 but never to the conf, so the conf and the live list set had drifted apart; added it right after the URLhaus line. The two lists deliberately disabled on 2026-08-25 (FadeMind add.Risk, Mandiant APT1) **stayed disabled** because their comment no longer matches the tool's "Managed by pihole-updatelists" marker, so the timer can't silently re-enable them. The conf also still listed `v.firebog.net/hosts/RPiList-Malware.txt`, which wasn't on the pi-hole at all (checked live: maintained, updated 2026-09-14, ~596K domains, part of firebog's original ticked baseline — it had fallen out of the live DB while staying in the conf); the user asked for it, so it was re-added and gravity rebuilt (3.29M → **3.89M** gravity domains, no errors). **Pi-hole now runs 29 lists** (was 28).
- **xwayland-satellite pin re-checked twice (2026-09-17 evening, 2026-09-19) via `pinned-package-status-check` — verdict both times: keep the 0.8.1 pin.** Issue #156 still open (23 comments, last activity 2026-09-02, no maintainer reply/fix PR/ETA), nixpkgs-unstable's newest is still 0.8.2, upstream's newest release is still v0.8.2 (2026-07-22). Registry unchanged, nothing to save. (Process note: one check's first page fetch of the issue reported 0 comments; that was the fetch missing them — the GitHub API confirmed 23.)
- **Informational only:** asked what OpenPrinting is — it's the umbrella project behind CUPS/cups-filters/cups-browsed/ipp-usb, i.e. the printing stack this repo already runs (`services.printing` + `cups-browsed`). Nothing to change.

**Jellyfin plugin ecosystem researched and partly rolled out (Intro Skipper + YouTube-metadata live, OpenSubtitles pending user signup); a real security gap in the existing `add-secret` skill found and fixed along the way (2026-09-17, session 112).**
- **`/research` on the Jellyfin plugin ecosystem** (10/10 sources) found no tool cleanly does both declarative Jellyfin settings *and* declarative plugin installs — `nixos-jellyfin` (active) covers settings-only, `declarative-jellyfin`'s own author is abandoning it for `Jellarr` (pre-release, non-Nix-native, the only one that does plugin-as-code). Decision: **parked** — server already works, revisit only if the server ever needs to move. Saved to memory `reference_jellyfin_plugins_research` (Volatility: fast).
- **YouTube-metadata plugin (`ankenyr/jellyfin-youtube-metadata-plugin`) installed and verified live** on gaming's Jellyfin — reads Pinchflat's existing yt-dlp `info.json` sidecars (filename convention already matched, no Pinchflat change needed); configured with the **local reader only** on the YouTube library (remote fetcher left off, so it never invokes yt-dlp itself — same egress-safety reasoning as the earlier Pinchflat split-tunnel fix). Confirmed populating real titles/descriptions/thumbnails on a live item.
- **Intro Skipper installed after a dead-manifest-URL detour**: the documented `https://intro-skipper.org/manifest.json` 308-redirects to a GitHub HTML page (broken on their end, not a config error) — fixed by using the version-pinned raw-GitHub manifest URL instead. Plugin installed, "Detect and Analyze" task run.
- **OpenSubtitles plugin: intentionally not installed yet** — it's the right pick for auto-fetched subtitles (confirmed via research), but needs a real opensubtitles.com account (email+password) the agent can't create; user will provide credentials later. Free tier is 20 downloads/day (5/day with no account), rate-limited — fine for ongoing content, a big backlog scan could exhaust it.
- **Merge Versions explained, not installed**: matches by provider ID (not filename), zero storage/perf cost since it's a database-level presentation change only. Checked live: 0 duplicate-quality copies in the Movies library today, so it has nothing to do yet — safe to install-and-forget or defer.
- **All of the above driven live via the Jellyfin REST API using a dedicated `claude-automation` API key**, once the user asked whether to hand over permanent access. Rather than leaving it as a bare file/env var (this repo is **public** — one accidental `git add -A` puts a plaintext credential in git history forever), added it as a **new sops-nix secret** (`jellyfin-api-key` in `secrets/hosts/gaming.yaml`) matching the existing `pinchflat-env`/WireGuard-key pattern. Not wired into any NixOS module (`sops.secrets."..."`) since nothing but ad hoc Claude Code use needs it — decrypted on demand, never touches disk in plaintext. **No rebuild needed** — this isn't NixOS-declarative territory at all, just an encrypted-at-rest credential.
- **Real security gap found and fixed in the existing `.claude/skills/add-secret` skill** while adding that secret: `sops-secret.sh`'s `set`/`create` mode had the **agent** run `sops set <file> <key> <value>` with the plaintext as a literal command argument, and `verify-secret.sh` printed the **full decrypted file** to the transcript — both directly violate `modules/sops.nix`'s own stated rule ("secret values are added by the user directly, never by an agent"). This wasn't hypothetical: it's the exact rule this session's own jellyfin-api-key flow was following by hand (user pastes value via `!`-prefixed command → hands agent a file path → agent verifies via masked compare only). Fixed rather than replaced (kept the skill's existing edit/rotate/new-host-onboarding breadth): `set`/`create` now take a file path instead of an inline value, the skill explicitly bars the agent from invoking the write script itself, and verification only ever prints masked match/mismatch or OK/FAILED. Bumped to v0.3.0 with a Gotchas entry. Committed together with the jellyfin-api-key secret as `fcae72c`, pushed.
- **Jellyfin library-type "flat Movies grid" experiment tried and reverted, root cause confirmed structural, not configurable.** User wanted the YouTube library to show a flat thumbnail grid instead of the channel→dated-season→episode drill-down. No API endpoint changes an existing library's type in-place (confirmed via the live OpenAPI spec); did a delete+recreate as `movies` type instead (user ran the irreversible-delete step themselves via `!`, since Claude Code's own auto-mode classifier correctly blocked the agent from running it directly). Result: Jellyfin's folder resolver still parsed Pinchflat's nested `channel/dated-folder/video` layout as `Series`/`Season`/`Episode` regardless of the library's declared type — the dated-subfolder naming itself triggers TV-show detection, independent of the collection-type setting. **Reverted back to `tvshows`**, confirmed 26 episodes showing correct titles/thumbnails/descriptions again. A real flat-grid fix would require changing Pinchflat's own output folder structure, not anything on the Jellyfin/NixOS side — not pursued this session.
- **No NixOS config/host changes this session** except the one encrypted-secret addition above (no rebuild implication); all plugin/library work happened entirely inside Jellyfin's own dashboard/REST API, outside this repo's declarative scope.

**Skill-ecosystem lightening pass in 4 parts: a creative-example audit, a `references/`-routing refactor that caught a real Nix-wiring gap, closing that gap for future skills, and a twice-run `/improve-system` sweep that built a new `skill-builder` sub-agent (2026-09-16, session 111).**
- **`skill-upgrade` audited all 70 skills for embedded creative examples** (a fully-composed sentence offered as a copy-target, vs. a fixed schema/template — which is fine). Only one genuine hit: `session-closer`'s Step 2 "Be specific" example was a fully-written WireGuard/sops-nix sentence, risking the model pattern-matching toward that domain/phrasing instead of describing its own session. Rewrote it as an interface (what's-needed / constraint / done-looks-like framing) instead of a worked example. Committed `7b8fd41`.
- **`references/`-routing refactor**: both CLAUDE.md files and 14 SKILL.md files carried heavy Gotchas/procedure blocks that only mattered for one rare sub-case, yet loaded on every invocation regardless. Split 16 new `references/*.md` files out verbatim (nothing paraphrased), leaving one-line pointers — e.g. `audit-config/references/known-workarounds.md`, `new-host/references/sops-registration.md`, `flake-update-verify/references/gotchas.md`. Two of 5 parallel review forks got context-contaminated by the orchestrator's own status narration mid-run (one began believing it was the orchestrator and tried to kill sibling agents) — caught, both stopped, their 28 files read directly instead; findings unaffected, filed as product feedback. **Real gap found along the way**: `research`, `agent-suggestion`, and `improve-system` were symlinked file-by-file (SKILL.md only) in `bosko-claude.nix`, so their new `references/` dirs would never have reached `~/.claude` even after a rebuild — switched all three to recursive directory symlinks. Committed `1c994b6` (34 files).
- **Closed the "retrofit-only" gap**: a brand-new skill drafted via `new-skill` (directly, or via `skill-suggestion`/`agent-suggestion`/`improve-system`) had no equivalent step to route heavy/narrow context to `references/` at creation time, and `skill-audit`'s 6-lens rubric couldn't catch one that shipped bloated anyway. Added `new-skill` Step 2c (mirrors Step 2b's deterministic-to-scripts logic) and a 7th `skill-audit` rubric lens. Committed `f284a62`.
- **`/improve-system` run twice this session** — a full pass, then a scoped incremental follow-up. First pass: `skill-upgrade` hardened `session-closer`/`skill-audit` with 2 real misfires; `agent-suggestion` built and wired a new **`skill-builder`** custom sub-agent (parallel skill drafting) and rewired `skill-suggestion`'s Step 5 to dispatch to it — smoke-tested via a general-purpose agent standing in, since a brand-new repo-managed agent isn't dispatchable by name until after rebuild+reboot; `skill-audit` swept all 71 skills (5 parallel groups, full retry after all 5 hit a session usage-limit mid-run) and landed 11 approved fixes — script extraction (`diagnose-hung-game`'s Sentry/proton-chain/UE-log steps added as flags on its existing script), 2 new `AskUserQuestion` gates (`flake-update-verify`'s dirty-tree stop, `pinned-package-status-check`'s no-arg package list), drift-prone hardcoded values now read live (`new-host`'s pi-hole IP via `hosts.json`, `new-peer`'s VPN server endpoint via `modules/vpn.nix`), 4 missing `## Arguments` sections, 2 stale gotchas re-synced; `fewer-permission-prompts` added 3 allowlist entries. Committed `1f9ddc6` (23 files). Second pass was scoped to what changed rather than a blind re-sweep: one real misfire caught live — a stale-content `Edit` failure hit while applying skill-audit's own approved fix — added a gotcha to `improve-system` itself, plus a gotcha to `agent-suggestion` documenting that a brand-new agent can't be smoke-tested by name pre-rebuild (the same workaround `skill-builder` itself needed above).
- **No NixOS config/host changes this session** — pure Claude-ecosystem process work; `nixos-dry-run` re-verified clean after every edit batch (source-derivation-only churn, switch-safe, no reboot strictly required for the content itself — see Current Goals for why a reboot is still wanted, for the new agent).

**Full-repo audit of absolutist ("never"/"always"/fixed-threshold) rules across both CLAUDE.md files and all 71 skills; 17 files rewritten into risk/ambiguity/reuse-scaled standards, 3 verified internal contradictions resolved (2026-09-15, session 110).**
- **5 parallel forks scanned all ~70 skill files for absolutist rules standing in for judgment calls**, while both CLAUDE.md files were audited inline (already in context) and the one hook (`check-session-debt.sh`) was confirmed to carry no embedded policy — it's a passive read-only notifier. One fork (batch 4) went out of scope: instead of extracting from its assigned 15 skills, it re-read files from other batches and produced its own unrequested synthesis/contradiction table. Re-launched correctly for just its assigned batch; the incident itself directly motivated the CLAUDE.md fix below. Feedback queued locally, not sent.
- **17 files rewrote absolutist phrasing into judgment-scaled standards**: skill-creation step-count threshold, interview-before-any-work, the 90-day research-staleness cutoff, `agent-suggestion`'s single-occurrence-never-qualifies bar, `new-skill`'s 2+-bucket split test, `create-secret-scan`'s "very large/old repo" threshold, `refresh-manager-profile`'s always-incremental rule, `new-host`/`new-module`'s "do not deviate" convention rule, `package-nix-tool`'s already-packaged stop (no branch for "missing what we need"), `fleet-rollout`'s fixed desktops-first host order, `git-push`'s exact-phrase confirm-skip gate, and `repo-creator`'s SSH-only remote. Real boundaries (secrets, force-push, sudo gates, destructive-op confirms) were audited and deliberately left untouched.
- **Resolved 3 verified internal contradictions**: `interview`'s Rules section literally said "never skip the key-decisions verification" while its own Gotchas already documented a well-scoped-fix exception — folded the exception into the Rule itself, trimmed the now-redundant Gotcha. `skill-upgrade`'s Gotchas-entry writes now auto-apply and report, matching how `improve-system` already classified that identical edit as low-risk. `git-push`/`session-closer` each gained a note that invoking a skill whose own job description already commits to pushing (e.g. "close the session," "end of day") counts as consent, so `git-push`'s confirm gate only fires for a standalone, ambiguous push request.
- **Added a scope-discipline clause to the project CLAUDE.md's Parallelize-with-Sub-Agents rule** — a spawned sub-agent must stay strictly inside its assigned piece; wandering into a sibling's scope or the synthesis step is a bug to correct, not extra helpfulness. Synced the same paragraph (generalized, no NixOS-specific examples) into `claude-rules`' canonical `assets/rules.md` block so future projects inherit it too.
- Committed as `48e856c` (17 files, +57/-33 lines), config re-verified clean via `nixos-dry-run` after each of the two edit batches. **Repo-managed global skills (`agent-suggestion`, `create-secret-scan`, `git-push`, `interview`, `new-skill`, `refresh-manager-profile`, `repo-creator`, `session-closer`, `skill-suggestion`, `skill-upgrade`, `claude-rules/assets/rules.md`) plus the global `~/.claude/CLAUDE.md` need `nh os boot` + reboot** to reach `~/.claude` — see Current Goals. The project-local `.claude/skills/{fleet-rollout,new-host,new-module,package-nix-tool}` and this repo's own root `CLAUDE.md` are already live (read directly from the checkout, no symlink involved).
- **xwayland-satellite 0.8.1 pin: one open verdict closed, no repo change.** Re-confirmed live on gaming (generation 414, since 2026-09-09) and that upstream issue #156 is still open — a new 2026-09-02 upstream comment shows the identical popup bug reproducing on both 0.8.2 and current `main`, undercutting the pin's original "version-specific regression" rationale. User confirmed the Steam dropdown bug hasn't recurred since the 09-09 switch, closing the open question from session 105/109: the pin stays as-is until #156 closes and a newer release is confirmed good.

**Weekly `improve-system` PR #23 reviewed+merged, its skipped `/send-results` step root-caused, and a combined `/skill-suggestion`+`/skill-upgrade` pass ran across 94 transcripts (2026-09-14, session 109).**
- **PR #23 (weekly `improve-system` sweep — 4 skill fixes: `flake-update-verify`, `fleet-rollout`, `secret-scan`'s `GH_TOKEN` pattern, `shared-module-check`) reviewed via `review-improve-system-pr` and merged** (`b0f5a75`) — guardrail held (all `SKILL.md`/`scripts` under `.claude/skills/`, no `.nix` touched), content independently verified against each described fix, not just the guardrail. Project-local — live immediately.
- **Root-caused why `/send-results` never fired for that run — not a wiring gap.** The routine's tracking issue jumped from posting the consolidated report straight to "closing this issue," skipping Step 5 (write `~/.claude/improve-system/report-<ts>.md`, hand off to `send-results`) entirely; confirmed via the report directory having nothing from 2026-09-13. Added a Gotcha to `dotfiles/bosko/claude/skills/improve-system/SKILL.md` so a future run verifies the report file actually exists before declaring done. Committed `e1ac4b4`, rebased onto PR #23's own squash-merge commit, pushed `69ca7f5`. **Repo-managed global skill — needs `nh os boot` + reboot** to reach `~/.claude/skills/improve-system`.
- **Combined `/skill-suggestion` + `/skill-upgrade` run: 4 parallel `transcript-scanner` agents mined all 94 transcripts (4 batches) for new-skill candidates**, alongside an inline skill-upgrade misfire scan. Built **`pinned-package-status-check`** (new, project-local) — generalizes the "has xwayland-satellite been fixed yet" check that had been run by hand 3-4 times (read the pinned-packages registry → check nixpkgs' current version → check the tracked upstream issue → report a lift/keep verdict). A second candidate, `pihole-manage-list`, was **not built** — it turned out to duplicate the existing `.claude/skills/pihole-api/` skill; the scanning batch that raised it had been fed an incomplete skill roster, and the duplication was caught by checking the real skill directory before drafting rather than after. Also added 2 Gotchas: `save-memory` (Step 2's "read `MEMORY.md`" was only an `ls`, never an actual `Read`, so Step 4's `Edit` failed "File has not been read yet"), `review-improve-system-pr` (`gh pr diff` rejects multiple pathspecs after `--`). Committed `f5f4a0f`. One process note: the new skill file was written straight through rather than pausing for `new-skill`'s own pre-write draft review — it's project-local and trivial to amend.
- **xwayland-satellite pin status re-checked by hand one more time before the new skill existed to automate it** — still 0.8.2 upstream, issue #156 still open, no reason to unpin. This is the exact check `pinned-package-status-check` now covers going forward.

## Current Goals

- **All 3 desktop hosts: rebuild to pick up the 2026-09-20 flake bump** (session 113, `f978b9c`) — `disko`/`dms`/`financeguru`/`home-manager`/`nixpkgs`/`sops-nix`; verified clean (flake-check + 4-host deep-eval), not activated. `boot` recommended over `switch` since DMS/home-manager moved (session-level services); `/fleet-rollout` is the structured path, and it also carries the still-unapplied 2026-09-07 bump below. No xwayland-satellite change rides along (pin holds, #156 still open).
- **OpenSubtitles: install once the user has opensubtitles.com credentials** (session 112) — the only piece of the Jellyfin plugin research still outstanding; Intro Skipper and the YouTube-metadata plugin are already live. No blocker on this repo's side.
- **All 3 desktop hosts: rebuild+reboot to bring session 111's skill-ecosystem work live in `~/.claude`** (`7b8fd41`, `1c994b6`, `f284a62`, `1f9ddc6`) — covers the new `skill-builder` custom agent (`dotfiles/bosko/claude/agents/skill-builder.md`; needs the reboot to be dispatchable by name, not just a rebuild), the `references/`-routing refactor across 10 global skills (`agent-suggestion`, `improve-system`, `new-skill`, `refresh-manager-profile`, `repo-creator`, `research`, `resume-session`, `send-results`, `session-analysis`, `session-closer`, `skill-audit`, `skill-suggestion`, `skill-upgrade`) plus both CLAUDE.md files, and the 11 skill-audit fixes. The project-local `.claude/skills/{audit-config,diagnose-hung-game,flake-update-verify,new-host,new-module,new-peer,pinned-package-status-check,remote-rebuild,ship-nix-change,update,vpn-status,wayland-screenshot}` fixes and this repo's own root `CLAUDE.md` are already live — no rebuild needed for those. Can ride along with every other pending switch below.
- **Any host: rebuild (no reboot needed) to bring session 110's absolutist-rules rewrite live in `~/.claude`** (session 110, `48e856c`) — pure prose changes to `dotfiles/bosko/claude/CLAUDE.md` and `dotfiles/bosko/claude/skills/{agent-suggestion,claude-rules/assets/rules.md,create-secret-scan,git-push,interview,new-skill,refresh-manager-profile,repo-creator,session-closer,skill-suggestion,skill-upgrade}`; no `.nix` file touched, verified via `nixos-dry-run`. Can ride along with every other pending switch below. The project-local `.claude/skills/{fleet-rollout,new-host,new-module,package-nix-tool}` and this repo's own root `CLAUDE.md` are already live — no rebuild needed for those.
- **All 3 desktop hosts: rebuild+reboot to bring `improve-system`'s new report-file-verification gotcha live in `~/.claude`** (session 109, `69ca7f5`) — `dotfiles/bosko/claude/skills/improve-system/SKILL.md`, repo-managed/symlinked-global. Can ride along with every other pending switch below. PR #23's own 4 skill fixes and the new `pinned-package-status-check` skill are project-local (`.claude/skills/`) — already live, no rebuild needed.
- **natalie-laptop: rebuild (boot, reboot recommended) to drop the VirtualBox host module** (session 107, `4237d7d`) — removes `hosts/natalie-laptop/virtualisation.nix` and its `vboxusers` group membership; it was never rebuilt onto in the first place (still owed its original from-source VirtualBox build), so this just clears a stale pending item rather than reversing anything live. Verified clean via `shared-module-check` 4-host deep-eval.
- **All 3 desktop hosts: rebuild (switch, no reboot needed) to pick up `system-config-printer`** (session 106, `f535057`; relocated into `modules/printing.nix` session 108, `dc37a1f`, no functional change) — verified clean via `shared-module-check` 4-host deep-eval. After rebuild, confirm the app launches and shows the print queue (Canon TS9500 via gutenprint). Can ride along with every other pending switch below.
- **All 3 desktop hosts: rebuild to pick up the 2026-09-07 flake bump (nixpkgs/home-manager/dms)** (session 104, `f780dba`, PR #21) — verified clean (flake-check + 4-host deep-eval); `/fleet-rollout` is the way to apply. Carries no xwayland-satellite fix (still 0.8.2, issue #156 still open — nothing to retest re: the Steam dropdown bug).
- **All 3 desktop hosts: rebuild+reboot to pick up PR #20's repo-managed skill fixes** (session 104, `da25cc2`) — `repo-creator`/`search-pkg`'s bare-relative-path fixes and `session-analysis`'s new `assets/analysis-template.md` are all under `dotfiles/bosko/claude/skills/`, symlinked-global. Can ride along with every other pending switch below.
- ~~gaming: rebuild (switch, no reboot needed) to apply the kitty full-screen fix~~ (session 103, `ba155a2`) — **REVERTED (session 105, `c9e624d`)**: user changed their mind before ever switching onto it: kitty stays pinned to `asus-1` but no longer opens maximized. No rebuild needed for this item any more.
- ~~gaming: rebuild (switch, no reboot needed) to apply BOTH the Mod+G Deezer stagger fix and the xwayland-satellite 0.8.1 pin, then test the Steam dropdown menus~~ (session 102 `fcb2cc5` + session 105 `4201283`) — **RESOLVED (stale item corrected session 113)**: gaming's generation 414 (built 2026-09-09) carries both commits, and the user confirmed the Steam dropdown bug fixed on 2026-09-15 (see the session-110 block above and memory `project_steam_dropdown_menu_bug`); Deezer's cold-boot Mod+G stagger wasn't separately confirmed. Original text: after switching, confirm Deezer opens alongside Steam/Vesktop/Lutris/qBittorrent on a cold-booted Mod+G press, AND relaunch Steam to check whether the File/Store/Library/Community dropdown menus now behave with `xwayland-satellite` pinned back to 0.8.1. If the dropdown bug persists, the pin didn't help — revert it (`git revert 4201283`) and fall back to `steam -system-composer` / keyboard-only nav / Big Picture Mode. Both fixes are shared dotfiles, so laptop/natalie-laptop pick them up on their own next rebuild too.
- **All 3 desktop hosts: rebuild+reboot to pick up PR #18's repo-managed skill fixes** (session 101, `dd2f534`) — `dotfiles/bosko/claude/skills/{agent-suggestion,dream,improve-memory,refresh-manager-profile,session-analysis,skill-upgrade}` are symlinked-global and need it; the project-local `ship-nix-change`/`shared-module-check`/`nix-repl`/`.claude/settings.json` changes in the same PR are already live. Can ride along with every other pending switch below.
- **gaming: DONE (session 100)** — the 20:44 2026-09-04 reboot (confirmed via `who -b`) picked up the entire backlog below down through session 92's items; live-verified directly (skill symlinks, `gemini-cli` absence, `dotnet --version`) rather than assumed. **The `godot4`/`dotnet --version` verification criterion in session 94's item below is corrected: the resolving binary is `godot4-mono`/`godot-mono`, not `godot4`.** laptop/natalie-laptop still need their own rebuild+reboot to pick up everything through session 92 (each bullet below still applies to those two hosts).
- **gaming: reboot pending to activate the hplip drop + flatpak `network-online.target` ordering fix** (session 100, `ae3b40b`+`cfb8caa`) — both already `nh os boot`-staged (confirmed via direct store inspection), just needs the actual reboot. laptop/natalie-laptop pick up the flatpak fix (shared `modules/desktop-apps.nix`) on their next rebuild too; hplip's removal is fleet-wide (shared `modules/printing.nix`) as well.
- **All 3 desktop hosts: rebuild to pick up `improve-system`'s new Discord-report Step 5** (session 100, `92e6075`) — pure `SKILL.md` text change, no reboot needed; verified via `nixos-dry-run`. Not yet in gaming's currently-staged generation (committed after that generation was built) — needs its own `nh os boot` first.
- **All 3 desktop hosts: rebuild to pick up the 2026-09-04 flake bump** (session 99, `2d11999`) — verified clean (shallow + 4-host deep-eval); `boot` recommended over `switch` since DMS/home-manager moved (session-level services). Can ride along with every other pending switch below.
- **All 3 desktop hosts: rebuild to pick up the `/dream` suite (4 new global skills), the `send-results` Artifact-link fix, and the `discord-webhook-url` secret** (session 99, `256a7cc`/`0748119`) — repo-managed/symlinked skill files + a sops secret, no reboot needed (pure `home.file`/secret addition). After rebuild, `/dream` and `/send-results` resolve to their live-tested versions in `~/.claude/skills/`. Can ride along with every other pending switch below.
- **All 3 niri hosts: rebuild to pick up the `rebuild-boot`/`rebuild-switch` alias split and the Meta+G launch-set fix** (session 98, `dce0913`+`a5fa6b1`) — both verified clean (dry-run/flake still evaluates); the niri fix needs no reboot (`switch` recommended), the alias rename just needs a fresh shell after activation. After rebuild, confirm Meta+G launches Steam/Vesktop/Lutris/Deezer/qBittorrent on gaming and that `rebuild-boot`/`rebuild-switch` both resolve. Can ride along with every other pending switch below.
- **All 3 desktop hosts: rebuild to pick up `save-memory`'s PR #16 project-dir fix** (session 97, `4475e6d`) — pure `SKILL.md` text change (path resolution via `find-transcript-dir.sh`), no reboot needed. Can ride along with every other pending switch below.
- **gaming/laptop/natalie-laptop: rebuild to pick up `development.nix` (godot-mono 4.7.2 + dotnet-sdk 8.0.424) now in `desktopModules`** (session 94, `c8290f5`+`512561e`) — verified clean via dry-run (gaming) + `shared-module-check` 4-host deep-eval. After rebuild, confirm `godot4-mono`/`godot-mono` (not `godot4` — corrected above) and `dotnet --version` both resolve on PATH. Can ride along with every other pending switch below.
- **gaming/laptop/natalie-laptop: rebuild to pick up the 2026-08-25 flake bump (`c84892f`) and the Tailscale MCP connector (`e8c183a`)** (session 92) — both verified clean (flake-check + 4-host deep-eval); the connector needs its own post-rebuild live-verify per memory `project_tailscale_mcp_connector` (check `~/.claude.json`'s `.mcpServers.tailscale`, `/run/secrets/tailscale-mcp-env`, and a real tool call — not yet confirmed on gaming despite its reboot, since reading `/run/secrets/` needs sudo). Stacks on top of every other pending switch below — one reboot per host covers all of them.
- ~~Investigate the phantom `save-memory` skill~~ (session 92) — **RESOLVED (session 95)**, see Current Project State above.
- **All 3 desktop hosts: rebuild to pick up `save-memory`'s promotion to a global skill** (session 95, `7b8c3e3`) — verified clean via `nixos-dry-run` (pure symlink move, no reboot needed). Can ride along with every other pending switch below.
- **`resume-session`/`skill-audit`: rebuild+reboot to bring PR #12's fixes live in `~/.claude`** (session 92, `754f63f`) — `rollback.sh`'s fix is project-local and already live; these two are global repo-managed skills. Can ride along with the other pending switches.
- **All 4 hosts: `nh os switch`/`boot` to actually drop `gemini-cli` from the store** (session 91, `406b373`) — verified clean via `shared-module-check`'s 4-host deep-eval; no reboot needed (pure package-list change), can ride along with the other pending switches below.
- **laptop and natalie-laptop: pull + rebuild to pick up the declarative Auto Mode reconcile** (session 90, `f01382b`) — writes `permissions.defaultMode="auto"` plus the full `autoMode.soft_deny`/`environment` policy into each host's own `~/.claude/settings.json` automatically on rebuild; no more manual per-host `/auto-mode-setup`. Verified clean via `nixos-dry-run` + 4-host deep-eval; user says they'll handle these rebuilds themselves.
- **Rebuild + reboot to bring `3230dfd` (research skill memory/volatility feature + the new CLAUDE.md stale-research-memory rule) live in `~/.claude`** (session 88) — both files are already repo-managed/symlinked, just needs the rebuild. Can ride along with the other pending switches below.
- **Apply the 2026-08-21 flake bump (`041a0d1`: nixpkgs/home-manager/dms) to any host** (session 88) — verified clean via flake-check + 4-host deep-eval, committed+pushed lock-only per `/flake-update-verify`'s scope; `/fleet-rollout` is the way to apply. Stacks on top of the still-unapplied 2026-08-14 bump below.
- **All 3 desktop hosts: `nh os switch` to apply `ssh.nix`'s LAN→Tailscale IP swap for `gaming`/`laptop`/`natalie-laptop` aliases** (session 87, `2d3ab40`) — verified clean via `shared-module-check` (4-host deep-eval). Until each host switches, its own local `~/.ssh/config` still resolves the *other* two hosts' aliases to the old LAN IPs. Can ride along with the other pending switches below.
- ~~laptop: `nh os switch` to apply the bluetooth ISO-socket fix (`fc756c3`) and the shared-folder Tailscale-IP fix (`693d573`)~~ (session 86) — **RESOLVED (session 89)**: laptop switched and confirmed live (verified the CIFS mount-timeout portion directly; bluetooth/Tailscale-IP inferred live from the same switch, same reasoning pattern as prior batch-resolutions in this log).
- **natalie-laptop: `nh os switch`/`nh os boot` to pick up the shared-folder module's real fix — `86ad36e`+`24a1d2d`** (session 89) — natalie-laptop shares `modules/shared-folder-client.nix` with laptop and hasn't been rebuilt since either commit; it still has the pre-`693d573` LAN-IP hostname *and* the never-actually-applied 90s CIFS timeout until it gets its own switch. Verified clean via `shared-module-check` 4-host deep-eval, `switch` recommended (no reboot needed).
- ~~laptop and natalie-laptop: `nh os boot`/`switch` + `sudo tailscale up` to bring the 3-host Tailscale mesh live~~ (session 81, `34b9e72`) — **RESOLVED (session 82)**: user rebuilt both hosts and ran `sudo tailscale up` on each; all 5 devices (gaming, laptop, natalie-laptop, pi-hole, famdash) confirmed live via `tailscale status` and the admin website. See Current Project State. The exit-node/egress-masking gap (Tailscale mesh alone doesn't reproduce wg0's full-tunnel behavior) is still open — see Known Issues re: Pinchflat.
- **laptop and natalie-laptop: `nh os switch` to apply the pi-hole-over-Tailscale DNS change** (session 82, `30c73ee`) — only gaming has switched onto this so far; dry-run + 4-host deep-eval already verified clean, safe to apply. Sudo-gated, left to the user. Can ride along with the other pending switches below.
- **Verify the Tailscale-admin-console global nameserver actually applies pi-hole DNS on laptop and natalie-laptop** (session 84) — confirmed only on gaming so far (both were offline during this session); once online, the same check works: `host doubleclick.net` should return `0.0.0.0` via the system resolver.
- **Once vpn-server is back: restore wg0** (session 81, `5b1ad16`) — uncomment the `modules/vpn.nix` import in `flake.nix`'s `desktopModules`, the `wg0.address` blocks in all 3 desktop hosts' `networking.nix` (+ natalie-laptop's `autostart=false`), and gaming's Pinchflat split-tunnel `postUp`/`postDown`/`checkReversePath` in `hosts/gaming/pinchflat.nix`. Grep `vpn.nix`/`wg0` across those files to find every spot. No automated watcher for "vpn-server is back" — user is self-monitoring (see Recent Decisions), so this only happens once they say the word.
- **Check whether Pinchflat downloads are actually still working with wg0 down** (session 81) — flagged, not verified either way; the egress-IP masking its bot-detection bypass relied on is currently gone (Tailscale mesh doesn't provide it). Worth a live check next time Pinchflat is touched, rather than assuming either way.
- **Rebuild + reboot to bring `4feaa11` (agent-suggestion's errored-invocation-cutoff Gotchas hardening) live in `~/.claude`** (session 80) — committed *after* session 80's own rebuild checkpoint. **Update (session 81): very likely resolved as a side effect** of a later `nh os switch` on gaming during this arc's Tailscale-gaming work (ran after `4feaa11` and `443fb9a` were both already committed) — inferred, not individually re-verified; see Current Project State. Everything committed *before* session 80's own rebuild checkpoint (`d0b7ead`/`agent-suggestion` itself, `b11c809`'s PR #10 fixes to `new-skill`/`create-loop`/`create-secret-scan`, and session 78's `82b5d1b` batch) rode along on that earlier rebuild — `d0b7ead` and `transcript-scanner` (`24cfc27`, confirmed live by real use in session 80) were explicitly reverified; the rest weren't individually re-checked file-by-file but are inferred live from the same rebuild event, same as prior sessions' batch-resolutions.
- **`repo-creator`'s commit-template asset extraction (session 81, `443fb9a`) — same inferred-live status as `4feaa11` above**, not individually re-verified.
- ~~laptop and natalie-laptop: `nh os switch`/`nh os boot` to pick up `services.upower.enable`~~ (session 77, `ae6e428`) — **no longer tracked (session 78)**: per memory `project_dms_battery_upower_fix`, the user is applying this manually across all 3 hosts themselves; treat as complete/self-managed unless a fresh battery-display symptom shows up.
- **`financeguru` bump (`f17aeab`, session 76) not yet applied to any host** — verified clean via dry-run (+92.8 KiB, `switch` recommended), can ride along with the upower fix above on the same switch.
- **Apply the 2026-08-14 flake bump (`4ea763c`) via `/fleet-rollout` when ready** (session 73) — nixpkgs/home-manager/dms/sops-nix all moved and are verified clean (flake-check + 4-host deep-eval), but committing was explicitly scoped to lock-only; no host has switched onto it yet. Can ride along with the idle-lock bump below on the same reboot.
- **Run `nh os switch` on any niri host to apply the 5→10 min idle-lock timeout bump** (session 72, `e375565`) — safe per dry-run (no kernel/DM changes), restarts the `swayidle` user service with the new 600s timeout.
- **Run `nh os boot` + reboot to bring the 2026-08-10 catch-up skill fixes live** (`resume-session` skill, slash-command transcript-detection fix, improve-system PR #8 content fix) — session 72, see Known Issues.
- ~~Run `nh os switch` on gaming to bring the VPN health-watchdog timer AND the 2026-08-09 flake bump live~~ (session 69 `1a4d440`, session 70 `e6b8367`) — **RESOLVED (session 71)**, see Known Issues. Still worth confirming the watchdog timer's `notify-send` toast actually pops under DMS (e.g. by temporarily pointing it at a bad host), per the open question from session 69 — that specific check hasn't happened yet.
- ~~Run `nh os boot` + reboot to bring 8 global-skill fixes from the 2026-08-09 skill-audit sweep live in `~/.claude`~~ (session 70) — **RESOLVED (session 71)**, see Known Issues.
- ~~laptop + natalie-laptop: still need their own `nh os boot`/`switch` + reboot~~ to pick up the pi-hole/famdash DNS fix (`ac1c4ad`) and the niri `playerctld` media-key fix (`8feeb46`) — **RESOLVED (session 71)**, see Known Issues.
- **Grab a real Thunar screenshot on gaming to visually confirm Colloid-Teal-Dark once the user is off the desktop** (session 71) — config-level verification (dconf, symlinks, HM service status) is solid on all 3 hosts, but the last screenshot attempt caught an active gaming session (StarCraft II + YouTube) instead of Thunar; not worth interrupting live use to force it.
- **Decide whether to actually build the NixOS-as-router project** (session 69) — research concluded and written to `~/projects/home-lab/docs/router-options.md` (a sibling repo, not this one), but no hardware bought and nothing scaffolded here yet. If it moves forward: pick N100 vs N150 mini-PC (functionally near-identical, NIC chipset matters more than CPU — see that session's Q&A), then use the `new-host` skill to scaffold `hosts/router/`.
- ~~Set up Pinchflat's Media Profile and the Jellyfin "YouTube" library~~ — **RESOLVED (2026-08-03, session 68b, confirmed via memory `project_pinchflat_jellyfin`)**: the "YouTube" library exists in Jellyfin and a manual rescan surfaced the previously-downloaded videos on the client; no further action needed.
- **Check back on the Yes Theory source once they post again** (session 67) — `download_cutoff_date` (2026-07-27) means nothing downloads until then; this is the first real chance to verify NFO/chapter/SponsorBlock output actually reaches Jellyfin correctly.
- **Visually verify the niri cursor-theming fix** (2026-07-25, commit `a5a6582`) — dry-run passed but never confirmed live with a screenshot; gaming has since had a full reboot+switch (session 67) so the config is active, this just needs an actual look (`wayland-screenshot`) — check a text-field I-beam, drag-resize, and grab cursor render as `breeze_cursors`. laptop/natalie-laptop still need their own rebuild first.
- **Watch the 2026-08-02 scheduled `improve-system` run to confirm the session 57 hardening holds unattended** — session 57 found and fixed a *second* real bug in the routine (a persisted-session stale-baseline causing it to re-report an already-fixed 6-item backlog for 3 weeks straight; see Current Project State) and rearchitected it: `persist_session: false`, the reuse-shortcut removed entirely, and a new self-verifiable auto-apply tier added. Manually triggered and confirmed working end-to-end same-session (PR [#4](https://github.com/CommanderBosko/NixOS/pull/4), merged), but the *next real unattended Sunday fire* is the first genuine test of the new design outside a manual/watched run — check the tracking issue it opens for a clean checkpoint sequence and, if it finds anything, whether the new auto-apply tier actually lands a PR rather than just reporting. The original GitHub-App-install root cause (session 52, commit info below) remains fixed and is not itself in question. Deferred question still open: **decide whether reviewing/merging its PRs needs its own skill** (deferred since session 46) — this session did the review+merge manually (see Current Project State) rather than building one; revisit if it keeps happening by hand.
- **gaming: DONE (session 67)** — full reboot (15:11) + switch (15:31) confirmed via direct live verification (`who -b`, generation timestamps, `systemctl`), picking up everything below plus the whole 2026-07-30 skill-audit backlog. **laptop + natalie-laptop: still pending, rebuild + reboot** — user said they'll handle these three themselves (session 50). Picks up the whole accumulated pile of pending niri/HM changes (sessions 34-48: `config.kdl` HM symlink, `qt6ct.conf`, keybinds/window-rules, kdeglobals fix, flake bumps, per-host `niri-overlay.kdl`, the session-48 app-stack swap) plus session 50's `natalie-laptop` Plasma removal (now niri-only) and the 32 skills' `model: haiku` tagging (no rebuild needed for that part — project-local + already-live global skills, listed here only because it rides along). **Gaming was previously confirmed live through session 48** (generations 333→337) — re-verify after this rebuild picks up session 50's `natalie-laptop`-only flake.nix/niri.nix changes too (gaming's own config is unaffected by those, but shares the file). On laptop, also confirm DMS's `qtThemingEnabled` toggle is on (see Known Issues). **Update (session 58, 2026-07-27): root cause of the persisting printer issue found** — natalie-laptop's generation 63 was built from a checkout 54 seconds too old to include `6877f4b` in substance (confirmed via `nix derivation show`, not just `git log`); a fresh `nixos-dry-run` against the current checkout now builds clean with the fix applied. The user's broader "ongoing issues" report (clock desync, VPN killing internet, this printer bug) was investigated directly on natalie-laptop via SSH this session — clock desync is a confirmed hardware fault (dying CMOS/RTC battery, no fix possible in config), VPN was root-caused to `wg-quick-wg0` autostart racing WiFi at boot (fixed, commit `21df99c`), and the printer fix is now staged correctly. **User is running `nh os boot` + reboot themselves** to activate both the printer and VPN fixes — verify per Known Issues after. laptop itself is still fully pending.
- **`vpn-server`: do NOT redeploy yet.** Flake eval is fixed (commit `3cd3a31`, session 50) but the fix is a `pkgs ? rtk` availability guard, not a real backport — `rtk` still isn't installed there. No action needed until nixpkgs-25.11 ships a point release with `rtk`; the guard will pick it up automatically once it does. When any other vpn-server change is ready to deploy, `shared-module-check` (new, session 50) will catch a repeat of this class of break before it happens.
- **Add host-specific keybinds/window-rules to `hosts/laptop/niri-overlay.kdl` / `hosts/natalie-laptop/niri-overlay.kdl`** (session 47) — both are currently empty placeholders; the infrastructure is in place, just needs real content whenever host-specific rules are wanted.
- **Interface-scope Avahi mDNS firewall** — `modules/printing.nix:12`: replace `openFirewall = true` with per-interface rules so UDP 5353 is only reachable on the LAN interface, not on all interfaces
- **AMD card swap on gaming** — when the physical card is swapped, remove `nvidia.nix` from gaming's module list in `flake.nix`; run `rebuild` in terminal and reboot
- **Re-add `server` host when hardware arrives** — use `/new-host` skill to scaffold; the pinning to `nixpkgs-25.05` via `nixpkgs-stable` input is the established pattern

### Long-term

- Harden vpn-server further with AppArmor profiles and fail2ban once stable channel is pinned
- Evaluate adding containerised services on the VPN server (its Oracle free-tier compute is otherwise idle)
- Explore adding fail2ban or rate-limiting on the WireGuard UDP port

## Recent Decisions

- **Jellyfin API key goes into sops-nix, not a bare file/env var — even for pure ad hoc/agent use with no systemd consumer (2026-09-17, session 112)** — user initially asked to just hand it over permanently; agent flagged the real risk isn't persistence itself but that this repo is **public**, so a plaintext credential sitting anywhere is one `git add -A` away from permanent public exposure. Matched the existing `pinchflat-env`/WireGuard-key pattern instead: encrypted in `secrets/hosts/gaming.yaml`, decrypted on demand, no NixOS module wiring needed since nothing but Claude Code itself reads it.
- **Fixed the existing `add-secret` skill's plaintext-exposure bug rather than let a second, competing draft replace it (2026-09-17, session 112)** — while building what looked like a new skill for the exact "user hands agent a secret safely" flow just performed by hand, discovered `add-secret` already existed (missed by an earlier `find` search, likely the rtk compound-predicate limitation) and was more mature (edit/rotate/host-onboarding), but had a real security bug: the agent's own script ran `sops set` with the plaintext as a literal argument, and verification printed full decrypted output to the transcript. Kept the skill's breadth, closed the specific hole (file-path input instead of inline value, agent barred from invoking the write script, masked-only verification output).
- **Stopped appending to README's "Current Status" wall-of-text paragraph this session, added only to "Recent Changes" instead (2026-09-17, session 112)** — every prior session appended another clause there, growing it past readability; given the bloat is now flagged as tech debt above, didn't compound it further while leaving the decision on trimming existing content to a dedicated future pass.
- **Library-type change (`tvshows` → `movies` → back) was executed and reverted in the same session rather than left half-applied (2026-09-17, session 112)** — once the API confirmed there's no in-place type-change, and testing the recreated `movies` library showed Jellyfin's folder resolver still detects Pinchflat's TV-style layout regardless of declared type, reverted immediately back to `tvshows` rather than leaving the YouTube library broken or asking the user to live with an untested state.
- **Absolutist rules rewritten by standard-behind-the-rule, not by literal keyword match, and only where a real judgment call was being suppressed (2026-09-15, session 110)** — the initial fork-based extraction flagged ~40 "never"/"always" phrases across both CLAUDE.md files and all 71 skills; before rewriting any of them, each was checked against whether the absolute phrasing was standing in for a judgment call (rewrite) or was a genuine technical fact/hard boundary (leave alone — e.g. rtk `find`'s single-predicate limit, no-NOPASSWD hosts, never-commit-secrets, never-force-push-main). Of the flagged set, 17 files' worth cleared the bar for rewriting; real boundaries were left untouched even where the wording used the same "never"/"always" language.
- **`interview`'s contradiction fixed by folding the exception into the Rule, not by softening the Rule generally (2026-09-15, session 110)** — the skill's Rules section said "never skip the key-decisions verification," while its own Gotchas already documented skipping the full ceremony for a well-scoped, already-diagnosed fix. Rather than water down the Rule to vague language that would blur when the full ceremony actually applies, restated it as: full verification for a fuzzy/multi-part request, 1-3 blocking questions for a well-scoped fix — same standard, no ambiguity about which mode governs a given request.
- **`skill-upgrade`'s Gotchas-entry confirmation gate dropped in favor of auto-apply+report, resolving a live contradiction with `improve-system` (2026-09-15, session 110)** — `skill-upgrade` called the edit "low-stakes, easily-reversible" and then gated it behind `AskUserQuestion` anyway, while `improve-system`'s own orchestration already classified that identical action as auto-apply-tier. Rather than leave the resolution ambiguous for whichever caller invokes `skill-upgrade` first, changed `skill-upgrade` itself to match `improve-system`'s classification, so both callers agree.
- **`git-push`'s lexical confirm-skip bright line (exact phrases "push it"/"just push"/"push now") widened to read intent generally, phrases kept only as examples (2026-09-15, session 110)** — flagged as a contradiction against `session-closer`, which pushes with zero confirmation on phrasing that never even says "push" ("close the session," "end of day"). Rather than tighten `session-closer` to match `git-push`'s stricter gate, documented in both skills that invoking a skill whose own job description already commits to pushing counts as consent — `git-push`'s confirm gate is for a standalone, ambiguous push request reaching it directly, not for a skill like `session-closer` where pushing is the named job.
- **A guardrail-flagged skill-suggestion candidate was dropped once checked against the real skill roster, not built alongside its sibling candidate (2026-09-14, session 109)** — a `pihole-manage-list` skill was drafted from a transcript-scanner batch's recommendation, but before writing it, checking `.claude/skills/` directly turned up `pihole-api` already covering the exact same auth dance (built the same day the friction happened, missed by the batch because it was fed an incomplete roster). Stopped before duplicating; built only the other candidate (`pinned-package-status-check`), which a repo-wide check confirmed was genuinely new.
- **The session-102 "no version pin is worth trying" conclusion on xwayland-satellite was reversed after user pushback, not defended (2026-09-09, session 105)** — user cited a Reddit thread claiming a 0.8.1 downgrade fixed the same dropdown bug; that thread couldn't be verified directly (Reddit is blocked for fetching, no corroborating search results), but re-reading 0.8.2's own changelog ("fixes for some popup regressions") showed the earlier reasoning had a real gap: it assumed each release's fixes were purely additive, when 0.8.2 explicitly frames this as trading regressions release-to-release. Rather than pin the originally-floated 0.8 (pre-0.8.1, untested, no direct signal it's better) or just take the Reddit claim on faith, pinned the narrower, cheaply-testable 0.8.1 instead, corrected the memory record before proceeding, and used a dedicated second nixpkgs input + overlay (not `pin-input`, which would have rolled back all of nixpkgs) so the experiment is fully isolated and a one-line revert if it doesn't pan out.
- **`manager`'s hard limit against direct pushes to `main` overrode `/flake-update-verify`'s own literal step and this repo's established direct-push precedent for flake bumps (2026-09-07, session 104)** — the skill's written steps say push straight to `main`, and every prior flake-bump commit in this repo's history did exactly that (e.g. `2d11999`, `041a0d1`). `AskUserQuestion` wasn't reachable in the manager's delegated context, and the task had pre-authorized answering the skill's own commit-gate via profile rather than stalling — but the manager treated "never push directly to a default branch" as a separate, absolute rule that pre-authorization can't waive, so it branched (`chore/flake-bump-2026-09-07`) and opened PR #21 instead. This still satisfies the *intent* behind the skill's commit-gate (a human glances at the summary before it lands) — merging the PR became that checkpoint. Consistent with the same hard-limit posture already codified for `manager` in session 96 (relayed-consent handling): treat an absolute rule as non-negotiable even when a skill's own steps or a task's pre-approval point the other way.
- **Kitty's existing window-rule reused rather than a new block added (2026-09-05, session 103)** — the app-id already had a `window-rule` in `hosts/gaming/niri-overlay.kdl` (pinning it to `asus-1`); confirmed the app-id live via `niri msg windows` first, then added `open-maximized true` as a new line inside that same block via `add-niri-fullscreen-rule`, matching the pattern already used for Vesktop/Zen Browser/Deezer/qBittorrent.
- **PR #18's two guardrail-flagged files (a new shared lib, a permissions file) merged anyway after a direct content review, not held for manual review by default (2026-09-05, session 101)** — `review-improve-system-pr`'s guardrail only recognizes `SKILL.md`/`scripts/`/`assets/` paths under the two skill dirs, so `.claude/lib/classify-shared-file.sh` and `.claude/settings.json` both tripped it structurally even though their content matched exactly what the PR body described and fixed a real bug. Rather than treat a guardrail hit as an automatic hold, read both files directly, confirmed no `.nix` file was touched and secret-scan/`public-repo-guard` were already clean, then merged via the existing `AskUserQuestion` gate. The guardrail still did its job — surfacing the boundary crossing for a human look — it just isn't a synonym for "reject."
- **`session-closer`'s secret-scan step rescoped to match what the tool already does, per direct user ask (2026-09-04, session 101)** — the skill's STEP 5 framed `secret-scan` as a README-only pass, but the tool itself unconditionally scans the whole tree + full git history regardless of scope. Rather than leave the mismatch (undersells the check, and any non-README finding would have gone unfixed), split it into its own STEP 5B explicitly scoped to everything committed since `session-closer`'s own last run.
- **A shallow "not yet applied to any host" note was resolved via direct evidence, not assumed from the reboot alone (2026-09-04, session 100)** — rather than take gaming's 20:44 reboot as blanket proof that session 99's whole backlog was live, checked each claim directly: skill symlinks actually resolve to real store paths (not dangling), `gemini-cli` is genuinely absent from PATH, `dotnet --version` actually returns a version. This surfaced one real correction along the way — the Godot binary that resolves is `godot4-mono`/`godot-mono`, not `godot4` as a prior session's verification note assumed — which a "reboot happened, mark it all done" shortcut would have missed.
- **The flatpak-race and hplip fixes were kept as two separate commits despite landing in the same evening (2026-09-04, session 100)** — different root causes (a boot-time DNS ordering race vs. an upstream nixpkgs/pyqt5 Python-version regression), different files (`desktop-apps.nix` vs. `printing.nix`), different scope (shared-module 3-host impact vs. fleet-wide driver list) — bundling them into one commit would have blurred a `git log`/`git bisect` search for either fix later.
- **`improve-system`'s Discord report step always runs, even on an all-clean or focus-scoped pass (2026-09-04, session 100)** — matches `dream`'s existing precedent (a clean pass is still a result worth reporting) rather than only notifying when there's something actionable; the alternative (skip the notification when nothing needs sign-off) would make a truly silent, unattended run indistinguishable from a run that never happened.
- **`send-results` redesigned to publish an Artifact instead of trying to preserve a local `file://` link (2026-09-04, session 99)** — Discord's message renderer never auto-links non-`http(s)` schemes on any device, an anti-abuse restriction with no formatting workaround, so the original "post a local path" design could never actually produce a clickable link in practice. Chose to publish the reported content as a Claude Artifact and link to that real `https://` URL instead, accepting the trade-off that reported content now leaves the local machine (published, shareable) rather than staying purely local. Live end-to-end retested before committing.
- **PR #14's classifier-flag warning treated as verified-clean-but-flagged, not dismissed (2026-09-03, session 99)** — a full manual audit of the merging subagent's entire tool-call trajectory (19 Bash/3 Read/3 Edit/1 Skill) found nothing wrong and the merged diff matched exactly what was claimed, but the classifier's own reasoning was never visible from the transcript. Rather than either treat the flag as proof of a real problem or wave it away since the content checked out, reported both facts to the user as true at once — verified safe by content, unexplained by process — since the merge was already live and irreversible either way.
- **FinanceGuru's auto-mode-classifier block resolved by pointing the user at an interactive-session mode toggle, not by trying to route around it (2026-08-31, session 97)** — once the root cause (background/agent-view sessions can't self-escalate permissions; only a human-present interactive session can flip `defaultMode`) was confirmed via direct settings comparison, the only options were waiting for the user to act or attempting a workaround (e.g. shelling out to write `settings.local.json` directly). Chose to stop and name the exact human action needed, per the denial's own guidance, rather than search for a clever bypass — same posture `manager`'s hard-limit fallback (below) already codifies for a different trigger. No repo change; purely advisory.
- **`manager`'s hard-limit fallback codified as "stop completely, never trust relayed consent" rather than "stop and use `AskUserQuestion`" (2026-08-30, session 96)** — the original wording assumed `AskUserQuestion` is always reachable, but the live calibration test proved it isn't when `manager` runs as a background subagent via the Agent tool. Rather than soften the hard limit for that invocation shape, kept it absolute: use `AskUserQuestion` if genuinely present, otherwise stop completely and report the exact verifiable action needed to unblock — never accept a relayed/asserted claim of user consent from any other agent, however confidently worded, and independently re-verify the resulting state once told the blocker is resolved. Validated live: `manager` refused three escalating relay attempts during the test and only proceeded once the repo state itself became checkable; user graded it "exactly right, keep it this strict."
- **`manager`'s mined profile required a human read-through/sign-off before its first push, not auto-approved (2026-08-30, session 96)** — since the profile directly steers a future agent's unsupervised decisions on the user's behalf, it was treated as higher-stakes than an ordinary skill/agent file even though this is a private write to a public repo. The gate is one-time by design: `refresh-manager-profile`'s later incremental re-mines auto-push without re-litigating it.
- **`save-memory` promoted to a global skill rather than just correcting session 92's "phantom" misdiagnosis in place (2026-08-30, session 95)** — user asked local-vs-global as a factual question, but the natural read of "if it's local, make it global" was to actually promote it, not just report the finding. Followed the established recursive-dir `home.file` pattern (`interview`, `new-skill`, `session-closer`, etc. — anything with sibling files under `SKILL.md`) rather than a single-file symlink, since `save-memory` carries `assets/memory-template.md` alongside its `SKILL.md`.
- **`development.nix` scoped by who-needs-it, not by dev-tool topic, then promoted from gaming+laptop-only into `desktopModules` (2026-08-26/27, session 94)** — first ask (godot-mono+dotnet-sdk for Legions) went straight into both hosts' `environment.nix`; asked whether to sweep other loosely-dev-shaped packages (`claude-code`/`mcp-nixos`/`tailscale-mcp`/`rtk` in `users.nix`, `github-desktop`/`kate`/`kitty` in `desktop-apps.nix`) into a shared module too — declined, since each already sits in the tier its actual scope calls for and a topic-based move would either drop packages from hosts/users `development.nix` doesn't reach or hand them somewhere they don't belong. Only the genuine duplication got a module, imported per-host like `nvidia.nix`. When the user later said natty might develop too and asked to centralize for all desktop users, promoted the same module into `desktopModules` rather than revisiting the declined sweep — reaches all 3 desktop hosts/both users, no unrelated packages moved.
- **`repo-creator` no longer defaults to public — asks explicitly via `AskUserQuestion` (2026-08-26, commit `9380a65`)** — visibility is now a required argument on `create-repo.sh`'s create mode; prompted with no default when omitted, instead of silently hardcoding `--public`.
- **Fixed a real `git -C * <subcommand>` permission-rule injection bug in `.claude/settings.json`, hardcoding the path instead of narrowing the wildcard (2026-08-25, session 93)** — Claude Code's own startup warning flagged `Bash(git -C * status)`/`Bash(git -C * log *)`: the wildcard sat *before* the subcommand, so an injected `-c`/`--exec-path` option in that gap would still match and get auto-approved without a prompt. `Bash(git -C * diff*)` had the identical shape but wasn't in the popup — audited and fixed it too rather than leaving one instance of the same bug. Fix: since every real usage across this repo's skills (`fleet-rollout`, `flake-update-verify`, `bump-input`, `fmt`, etc.) only ever targets `/home/bosko/NixOS`, hardcoded that exact path instead of keeping any wildcard in the injectable slot — matches Claude's own guidance ("only use `*` after the subcommand") while preserving every real call site. Followed up with a full audit of all three permission files (project `settings.json`/`settings.local.json`, global `~/.claude/settings.json`) for the same wildcard-before-subcommand shape — none found; every other wildcard is a trailing suffix (`prefix*`), the safe form. Commit `599276f`.
- **`ssh.nix` Tailscale-IP sweep scoped via two `AskUserQuestion` calls rather than assumed (2026-08-21, session 87)** — switched `gaming`'s alias to its Tailscale IP too even though its LAN IP is static/non-drifting (consistency + off-LAN reachability, user's call); left the `new-host` skill's server-template DNS reference (`10.0.0.19`) on the LAN IP since that host type isn't Tailscale-joined by default (also user's call). Established the boundary for future IP-sweep requests: WireGuard mesh IPs, vpn-server's public Oracle IP, and historical session-log mentions are not Tailscale-IP candidates even though they're IP-shaped literals in the same repo.
- **Enabled pi-hole as a Tailscale-admin-console global nameserver, alongside (not instead of) the flake-managed override (2026-08-19, session 84)** — "Restrict to domain" off (needs to answer everything, not one domain), "Use with exit node" on (keeps pi-hole authoritative if/when a Tailscale exit node — e.g. Mullvad, still unexplored — is added later, rather than silently losing ad-blocking the moment one comes online). Kept both layers rather than consolidating: the flake-managed override only reaches the 3 NixOS hosts and keeps a `1.1.1.1` fallback; the admin-console setting is the only DNS path available to non-flake tailnet members (pi-hole/famdash themselves, and the two phones that joined the mesh — see Known Issues).
- **Pi-hole-over-Tailscale DNS: flake-managed nameserver swap (Option B) chosen over a Tailscale admin-console DNS override (Option A) (2026-08-18, session 82)** — Option A is a single unreviewable toggle that changes live DNS behavior for every meshed device at once, with no staged rollout and an unpredictable interaction with the existing static-resolver setup; Option B reuses this repo's existing verification tooling (dry-run → per-host rollout → health check) and keeps `1.1.1.1` as a working fallback if pi-hole-over-Tailscale doesn't pan out. Decided via a short `/interview` rather than assumed.
- **Flipped pi-hole's own `dns.listeningMode` from `LOCAL` to `ALL` (2026-08-18, session 82)** — required for the DNS-over-Tailscale change above: `LOCAL` was silently dropping queries from Tailscale's point-to-point `/32`-addressed interface, since it doesn't count as a "local subnet" by pi-hole's own ACL definition. `ALL` is a real posture change (pi-hole's own docs call it "dangerous" as an open-resolver risk) but judged safe here — no port-forward to pi-hole exists and Tailscale's `ts-input` firewall chain still gates non-tailnet traffic. Got explicit user sign-off before touching the live, non-flake-tracked appliance.
- **Tailscale on pi-hole/famdash installed manually, not brought under the flake (2026-08-18, session 82)** — consistent with how they've always been treated (non-flake devices, manual console management); Tailscale itself was installed via the standard `curl | sh` + `tailscale up` flow, same as the desktop hosts' one-time manual auth, rather than trying to retrofit either Pi into `new-host`/flake management just for this.
- **wg0/`vpn.nix` fully commented out of `desktopModules` rather than left in place to fail, or deleted outright (2026-08-18, session 81)** — with vpn-server down for an unknown duration (Oracle admin-disabled), a failing `wg-quick-wg0` unit on every desktop host boot was pure noise. Considered leaving it in place (rejected — noisy, no benefit) vs. deleting the file (rejected — throws away real wiring for no reason, it'll reconnect on its own once vpn-server is back). Chose comment-out: `flake.nix`'s import plus the now-orphaned per-host fragments that depended on it, `wg0.allowedTCPPorts` firewall stanzas left alone since they're harmless either way.
- **Tailscale mesh expanded to laptop/natalie-laptop with the same manual-auth, mesh-only scope as gaming's stopgap, not escalated to sops `authKeyFile` or exit-node config (2026-08-18, session 81)** — 3 ad-hoc hosts doesn't clear the bar for automating auth (matches the reasoning already applied to gaming in session 80's research). Exit-node/egress-masking (what would actually replace wg0's bot-detection-relevant behavior) stays explicitly deferred, not attempted as part of this expansion.
- **Declined building a "notify me when vpn-server comes back" watcher (2026-08-18, session 81)** — the existing gaming health-watchdog timer only fires on further *down* events and isn't reusable for a recovery signal; Oracle's own support-ticket reply is the authoritative signal regardless of anything built locally. User chose to self-watch rather than have a new mechanism built. Don't re-propose unless the user asks.
- **Declined a periodic `cups-browsed` auto-restart timer for the "printer powered on after boot" gap (2026-08-15, session 75)** — after root-causing the new discovery-gap trigger (see Current Project State), offered: (1) just restart manually when it recurs, (2) a systemd timer periodically restarting `cups-browsed` fleet-wide, or (3) a udev/NetworkManager-dispatcher hook tied to the printer's link state. User asked for the tradeoffs on option 2 first (polling-interval blind spot, brief print-dialog flicker on each restart, standing complexity for a rare self-inflicted trigger), then chose option 1 (manual restart) as recommended. Don't re-propose the timer unless this starts recurring often enough to change the cost/benefit.
- **Auto Mode kept as one global ruleset, not split per-project (2026-08-14, session 74)** — after `/auto-mode-setup` wrote NixOS-repo-specific `autoMode.soft_deny`/`environment` rules to the global `~/.claude/settings.json` and `permissions.defaultMode: "auto"` was set in the same file, asked whether to scope the repo-specific rules into this project's own `.claude/settings.json` instead so they wouldn't apply to unrelated projects. User chose to leave it as one global ruleset. Don't re-propose splitting it; see memory `project_auto_mode_global`.
- **gemini-cli removal warning left as-is, no waiver or removal (2026-08-14, session 74)** — nixpkgs' new `meta.problems.removal` mechanism flagged `gemini-cli-0.47.0` (Google deprecated it in favor of Antigravity CLI); confirmed non-blocking (warn-level by nixpkgs default) and that no `antigravity-cli` package exists in nixpkgs yet. Offered do-nothing / suppress-the-warning / remove-the-package; user chose do-nothing. `gemini-cli` stays in `modules/users.nix` for both users.
- **Nixpkgs pin stays temporary for a 3rd consecutive blocked update (2026-08-09, session 70)** — offered permanent-vs-temporary again after `f13ff45a` still lacked the removed `sweet` theme package; user chose temporary (lock-file only) again, so the pattern repeats on every future `/flake-update-verify` run until either upstream restores `sweet`/`gtk-engine-murrine` or the user migrates off Sweet-Dark. Not a bug — tracked in memory `project_sweet_theme_rollout`, don't re-flag as new.
- **All 5 skill-audit findings implemented in the recommended order, nothing declined or deferred (2026-08-09, session 70)** — unlike the 2026-07-30 sweep (which skipped a Tier 4 item as not worth the cost), this run's user approved the full fix-order as proposed. Commits: `02eb5e1` (3 skill-upgrade gotchas: `add-secret`/`journal`/`session-closer`), `d130dce` (bare `scripts/` path fix in 6 skills — the biggest single cluster, confirmed to 404 in production), `2d4610c` (`rollback`'s false `nh`-rollback claim removed; `review-improve-system-pr`'s dead `gh pr list --search` query replaced with the head-branch match, the only strategy that's ever actually worked), `b39a1b1` (`update`'s new pin-check gate; `new-host`'s missing `<current-nixos-release>` substitution; `nixos-dry-run`'s DE-risk keyword list now derived live from `modules/desktop-environments/*.nix` instead of hardcoding 3 of 12), `87561de` (slash-command transcript-cutoff blind spot propagated to the 3 meta-skills; `add-secret`'s stale `gaming.yaml` wording fixed). Every fix was empirically verified before committing (ran each corrected script, live-tested the new pin-check grep, live-derived the DE list, confirmed `nh os rollback --to` exists, confirmed the broken `gh` query really returns zero) — not just edited and assumed.
- **VPN monitoring split into a local watchdog + a cloud staleness check, not one combined mechanism (2026-08-07, session 69)** — the audit that found vpn-server needed monitoring also found a cloud routine genuinely can't do live health checks (no SSH access to vpn-server from the sandbox, nor should it get one without a deliberate decision), while an update-cadence check needs no credentials at all (public GitHub/channel data only). Split accordingly: a gaming-only systemd user timer for live health (SSH-reachable, notify-send capable), a cloud routine for the nixpkgs-staleness nudge (works from a public checkout alone). Rejected combining them into either single mechanism.
- **NixOS itself, not OPNsense/VyOS/an appliance, is the target for a future router (2026-08-06, session 69)** — `/research` explicitly optimized for *this* user's priorities (declarative, git-managed, Claude-drivable), a different axis than the general "best home firewall" question `~/projects/home-lab/docs/router-options.md` answered in May. Chosen because it reuses the exact same flake/skills/dry-run workflow already in place, with zero new credential surface for Claude to manage — at the cost of weaker router-specific community support and no turnkey IDS/IPS story. VyOS was rejected despite having the technically best AI-safe change mechanism (atomic commit + auto-rollback-via-reboot) because of the same paid-LTS-squeeze pattern the doc already rejected pfSense for. This decision lives in a sibling repo's doc, not this one — see Current Project State.
- **Pinchflat: native nixpkgs module over Docker, shared Jellyfin group over a standalone one, sops secret over `selfhosted = true` (2026-08-03, session 67)** — three deliberate choices scoped via `AskUserQuestion` rather than assumed: (1) `services.pinchflat` exists natively in nixpkgs, so no Docker/OCI container layer was needed at all, unlike every guide found during research; (2) `mediaDir` points at a new `/mnt/media/YouTube` folder sharing Jellyfin's existing `media` group, rather than the module's own `/var/lib/pinchflat/media` default, so no separate permission-bridging scheme was needed; (3) `SECRET_KEY_BASE` goes through a real sops-nix secret rather than the module's `selfhosted = true` escape hatch, to stay consistent with every other credential in this repo (nixpkgs docs themselves warn `selfhosted` isn't for production use).
- **Accepted two Pinchflat rough edges rather than chasing them further (2026-08-03, session 67)**: `collection_type: playlist` for the Yes Theory source (a confirmed-still-open upstream bug, `#607`, not fixed by re-adding with either URL form tried) is left as-is since the only known cost is cosmetic (missing series banner art); the `download_cutoff_date` filtering out all current videos is left as-is too, since the user decided they want future-uploads-only rather than a backlog, so the current cutoff is actually correct as configured, not a bug to fix.
- **SponsorBlock: enabled "mark chapters," not segment removal (2026-08-03, session 67)** — Pinchflat's native toggle only marks chapters (non-destructive: a wrongly-flagged segment just becomes an extra chapter marker), while true removal needs a custom yt-dlp flag Pinchflat doesn't expose as a first-class option and would permanently cut content on a bad match. Chose the reversible option.
- **Rejected adding a NOPASSWD sudo rule to let skills run `switch` unattended (2026-07-28, session 61)** — considered as a way to remove the recurring "sudo needs an interactive password" friction across `rollback`/`nixos-gc`/`fleet-rollout`. Declined: `nixos-rebuild switch` activates an arbitrary system config, which is effectively unrestricted root code execution, so even a rule scoped to just that one command would remove the last human checkpoint against a compromised or prompt-injected agent session. Kept the existing hand-off-to-user pattern as the standing design instead of trading that checkpoint away for convenience.
- **Network printer detection fixed via `cups-browsed`'s `CreateIPPPrinterQueues` setting, not a firewall/avahi change (2026-07-27)** — user reported niri wasn't detecting a network printer (Canon TS9500). Live diagnosis on gaming showed avahi + cups-browsed were both discovering it fine (`avahi-browse`, `lpstat -e`), but `lpstat -p` showed no actual CUPS destination — cups-browsed's compiled-in default `CreateIPPPrinterQueues=LocalOnly` only auto-creates queues for IPP-over-USB printers, never real network ones. Added `services.printing.browsedConf = "CreateIPPPrinterQueues Everywhere"` to `modules/printing.nix` (commit `6877f4b`), deep-eval-passing on all 4 hosts. Confirmed fixed on gaming after `nh os switch`. Separately, Zen Browser (Flatpak) didn't pick up the fix until manually relaunched — a stale-session-environment issue, not a second bug (see Known Issues / `feedback_session_daemon_staleness` memory). **natalie-laptop was rebuilt and fully rebooted but the printer is still not detected there** — not yet diagnosed, needs its own live investigation rather than assuming the gaming fix generalizes.
- **`improve-system` cloud routine's stale-memory bug fixed by removing the mechanism, not patching around it (2026-07-27, session 57)** — first response to the discovered bug (a persisted session trusting its own wrong "nothing changed since commit X" belief) was a targeted freshness-check patch requiring a fresh `gh`-sourced timestamp comparison each run. When the user asked how to harden the routine toward parity with a live session, went further: removed the reuse-shortcut entirely (always run the full audit fresh) and set `persist_session: false` (no cross-week memory at all), rather than keep the freshness-check band-aid. Rationale: the shortcut's only purpose was efficiency, its cost was low (~10 min for a full run, well under the 1-hour cron floor), and nothing about the routine's design actually needed cross-week memory — all durable state already lives in git/GitHub, so a stateless run is strictly safer with no real downside.
- **New "self-verifiable structural fixes" auto-apply tier added to the routine, scoped tightly (2026-07-27, session 57)** — previously every structural skill-audit finding was report-only forever, which is why the phantom 6-item backlog sat unfixed for 3 weeks even after a human had already fixed it. The new tier lets the routine auto-apply (as a PR, never merged) ONLY when a finding touches `SKILL.md`/`scripts/`/`assets/` under a skills dir (never `.nix`, so flake-eval risk is structurally zero) AND has a scriptable verification. Deliberately excludes anything touching `.nix` files (still no real dry-run possible in the cloud sandbox — confirmed via the `schedule` skill that only one CCR environment exists with no setup-script mechanism to install `nix`/`nh`) and mandates re-reading the current file content before applying anything, specifically so a stale claim (in either direction — false "still broken" or false "this is broken") gets caught before acting on it.
- **Reviewed and merged PR #4 manually rather than building a review skill first (2026-07-27, session 57)** — the deferred "does reviewing the routine's PRs need its own skill" question (open since session 46) came up again; did the review+merge by hand this time (independently re-verified all 7 claimed fixes against live repo content, not just trusting the PR body) since it was a one-off first real test. Still deferred as a skill until this becomes routine enough to be worth automating.
- **`flake-update-verify`'s commit+push stays gated even under a "do the whole thing" invocation (2026-07-26)** — Training Mode's default flip to `OFF` removed pauses from steps 1-5, but the user drew a hard line at step 6: pushing to shared `origin/main` unattended is a different risk class than the local update/verify work, which is already well-guarded by deep-eval + auto-revert. The new gate is deliberately carved out of the existing "pre-approved runs skip pauses" precedent rather than folded into it.
- **`/create-secret-scan`: no global generic scanner, every project gets a generated one (2026-07-23)** — considered keeping the NixOS repo's `secret-scan` project-specific while adding a generic global fallback for other projects; rejected in favor of always generating a project-tuned skill via interview, since a generic scanner would miss each project's actual secret-handling conventions (this repo's sops-nix integrity markers, for example).
- **`git-commit` asks before dropping unrelated uncommitted work rather than silently ignoring it (2026-07-23)** — previously committed only the files matching the current task's scope, leaving anything else untouched with no comment; genuinely unrelated work could sit uncommitted until noticed by chance. Now flags extras via `AskUserQuestion` instead of guessing intent.
- ~~niri cursor fix uses `home.pointerCursor` with `gtk.enable` off (2026-07-25)~~ — **REVERSED (2026-08-09, session 71):** that reasoning held only while `gtk-theme-name`/`gtk-icon-theme-name` were hand-set outside Nix; `944b64b` made the whole `gtk-3.0`/`gtk-4.0` `settings.ini` file declarative (theme + iconTheme + cursorTheme all set via the `gtk` module), so `home.pointerCursor.gtk.enable = true` is now correct and turned on — there's nothing left for it to silently wipe. Don't cite this old entry as a reason to leave `gtk.enable` off in a future change.
- **Extended `skill-suggestion`'s transcript-dir dedup fix to touch `skill-upgrade` and `session-closer` too, beyond what the finding named (2026-07-20, session 52)** — the routine's skill-audit finding only flagged `skill-suggestion`'s prose transcript-dir resolution as duplicating logic already scripted in the other two skills, and recommended extracting `scripts/find-transcript-dir.sh`. Rather than give `skill-suggestion` its own private copy of that logic (satisfying the letter of the finding but leaving the original duplication between the other two intact), created a genuinely shared `dotfiles/bosko/claude/skills/lib/find-transcript-dir.sh` (new convention, mirrors the project-local `.claude/lib/` pattern) and rewired `skill-upgrade/scripts/find-skill-misfires.sh` + `session-closer/scripts/scan-session.sh` to call it too. Chose this despite the added risk of touching two already-stable scripts because the change was small (2-3 lines each), both were smoke-tested before/after with identical output, and the alternative would have left the exact future-drift risk the finding was warning about. New `.claude/skills/lib` `home.file` entry added to `bosko-claude.nix` (recursive dir, so future shared global scripts need no further wiring).
- **`improve-system` weekly cloud routine's silent-failure root cause found and fixed: GitHub App was authorized but never installed (2026-07-20, session 52)** — Following up on session 51's third redesign (checkpoint-per-step GitHub issue), the routine was triggered manually and re-checked ~20h later: `RemoteTrigger get` confirmed it had fired, but `gh issue list`/`gh pr list` on the repo were still completely empty — the tracking issue's own creation (meant to be the routine's literal first action) never landed on GitHub at all, ruling out a mid-run hang or crash. Traced with the user through GitHub's UI: `github.com/settings/installations` (classic GitHub Apps) showed nothing; `github.com/settings/applications` → Authorized OAuth Apps also showed nothing; → **Authorized GitHub Apps** tab showed "Claude" (owned by `anthropics`) with Issues/PRs permissions listed as allowed — but cross-checking "Installed GitHub Apps" showed it wasn't there either. This is the actual bug: GitHub Apps need both user-level *authorization* (had it) and repo-level *installation* (didn't have it) — authorization alone grants no access to any specific repo. Fixed by installing the app at `github.com/apps/claude`, scoped to `CommanderBosko/NixOS`. Re-triggered the routine immediately after: tracking issue #1 created within 15 seconds, all 6 checkpoints posted, closed with a full report ~90 seconds later — first-ever clean end-to-end run. Confirms sessions 46/51's prompt-design work was correct all along; the blocker was entirely infra-side and invisible to any local tool (`WebFetch` on the routine's `claude.ai/code/routines/<id>` page 403s with no login session, so GitHub was genuinely the only reachable diagnostic channel until the App-install angle was checked).
- **`improve-system` weekly cloud routine redesigned twice for observability, not yet confirmed working (2026-07-19/20, session 51)** — its first fire (session 46) produced no PR/issue/branch and `persist_session:false` meant the run's transcript was discarded, so there was no way to distinguish "ran clean" from "failed silently." First fix: `persist_session:true`. Re-ran — still silent after 78 minutes of active polling, and the original design only ever writes anything to GitHub as its very last action (an end-of-run PR/issue), so a mid-run hang or crash looks identical to a clean pass from outside either way. Second fix (the one now live): the prompt opens a GitHub tracking issue as its literal first action, before reading any skill file, then posts a checkpoint comment after every step and only closes the issue as the final action — turning "silence" into "check whether the issue exists yet, and how far its checkpoints got." Rejected relying on the claude.ai routines web UI as the diagnostic path (`https://claude.ai/code/routines/<id>`) since `WebFetch` 403s on it (needs real browser login), so a durable public-GitHub artifact was the only channel actually reachable from a future Claude Code session. Third run kicked off 2026-07-20 00:58 UTC; outcome unknown as of session close — see Next Steps.
- **`vpn-server`'s `rtk` fix left unstable-only rather than force-installed some other way (2026-07-18, session 50)** — asked directly whether to deploy the eval fix. User declined: "no reason to do it until the stable nixpkgs is updated" — the `pkgs ? rtk` guard is the correct long-term shape regardless (self-heals on the next stable backport), so there was no pressure to work around the gap with an overlay or pinned-unstable-package-on-a-stable-host hack just to get `rtk` onto vpn-server sooner.
- **Chose an existence-guard (`pkgs ? rtk`) over a host-specific exclusion for the vpn-server break (2026-07-18, session 50)** — could have special-cased `vpn-server` out of the package list instead. Rejected: an attribute-existence check is generic or channel/arch-agnostic, self-corrects once nixpkgs-25.11 catches up, and generalizes to any future package with the same unstable-only gap, where a host-specific exclusion would need updating (or forgetting to update) every time.
- **Desktop-host rebuilds (gaming/laptop/natalie-laptop) left to the user, not offered as an in-session action (2026-07-18, session 50)** — user stated this explicitly ("I'll handle the 3 desktop host rebuilds myself"). Noted here so a future session doesn't re-offer or assume they're still pending on Claude's side.
- **Skill-audit Tier 4 findings (a `claude-rules` 4-line grep script, `new-skill`'s scope-detection mechanics) explicitly skipped (2026-07-18, session 50)** — both are global/repo-managed skills, so extraction costs a `bosko-claude.nix` symlink entry + rebuild; neither finding's payoff (a handful of lines saved) clears that bar. User's explicit call ("Ignore Tier 4").
- **Kept `niri.nix`'s Plasma-era systemd `ConditionEnvironment` gates after removing Plasma from natalie-laptop, rather than deleting them (2026-07-18, session 50)** — they're harmless no-ops with Plasma absent (the condition just always evaluates true) and correct again if a second session ever returns; ripping them out would be premature cleanup for a change the user framed as "for now," not necessarily permanent.
- **Dropped KDE apps for file-opening entirely rather than keep patching `kded6` around them (2026-07-17, session 48)** — asked directly: keep fighting niri's lack of a `kded6` session (which `ksycoca`-dependent apps like Dolphin/Ark silently need to resolve "open with" at all), or switch to apps that don't need it. A working `kded6` systemd service was drafted and flake-checked clean as the "keep KDE" option, but the user chose the swap instead: Thunar/xarchiver/zathura/imv (plain XDG resolution) over Dolphin/Ark/Okular/Gwenview/Double Commander. Net -123 MiB, and removes an entire class of bug (this session hit it three separate times before the swap) rather than mitigating it.
- **`new-skill` gained an untested-output reminder instead of absorbing smoke-testing itself (2026-07-17, session 48)** — user asked whether `/skill-suggestion` should hand off to `/ship-skill` instead of `/new-skill`. Answer was no (backwards — `ship-skill`'s Step 1 already calls `skill-suggestion`), but the underlying gap was real: `add-default-app` shipped uncommitted-then-committed without ever being smoke-tested. Chose a one-line reminder in `new-skill`'s final report over baking testing into `new-skill` itself, to keep it a clean single-bucket Utility skill rather than duplicating `ship-skill`'s Verification/Orchestration job.
- **Per-host niri overlay uses niri's native `include` directive, not Nix-level text concatenation (2026-07-17, session 47)** — initially told the user niri's KDL had no include mechanism and proposed assembling `config.kdl` by concatenating base+overlay text in the HM module. Found evidence to the contrary mid-implementation: the existing config already had `include "dms/outputs.kdl"` for DMS's own generated file. Course-corrected before implementing — surfaced the finding, got the user's go-ahead, then used a plain `include "niri-overlay.kdl"` line in the shared base plus a second `home.file` entry per host. Simpler than the original plan and validated natively via `niri validate`.
- **Gaming keeps its monitor-pinning window-rules; laptop/natalie-laptop start empty, keybinds stay identical across all 3 hosts (2026-07-17, session 47)** — user's explicit split during the interview: gaming's 7 named-workspace/window-rule pairs (tuned for its two-monitor Asus+Dell setup) aren't relevant to single-monitor laptop/natalie-laptop, so those hosts get the shared "standard" rules (WezTerm fix, Firefox PIP) only, with an empty overlay file ready for future host-specific content. Keybinds were confirmed identical across all three and stayed in the shared base unchanged.
- **Weekly `/improve-system` cloud routine opens a PR rather than pushing to `main` directly (2026-07-16, session 46)** — asked the user directly: since this is a brand-new, never-yet-run unattended workflow, a PR gives a review checkpoint before anything lands, unlike the local session-closer convention of pushing straight to `main`. Revisit once a few clean runs have built confidence.
- **`/improve-system`'s `AskUserQuestion` structural gates overridden to "report, don't apply" for the unattended cloud run (2026-07-16, session 46)** — the skill's own design assumes a human answers Approve/Skip for new-skill proposals and skill-audit refactors; a scheduled cloud run has nobody to ask. Rather than guess an answer or block, the routine's prompt explicitly tells it to only auto-apply the categories `/improve-system` already treats as low-risk (Gotchas, CLAUDE.md rules, permission allow-list additions) and to leave every structural item as a report-only line item in the PR body for manual follow-up.
- **Declined to build a "review the weekly PR" skill before the routine has ever run (2026-07-16, session 46)** — asked directly whether one was warranted. The component steps (review a PR, merge, pull, rebuild) are already covered by existing tools; building a skill around an untested workflow's output shape risks encoding wrong assumptions. Revisit after the first real Sunday run.
- **qBittorrent's login autostart disabled outright rather than patching the window-rule (2026-07-16, session 45)** — the window-rule matching `qbittorrent` was already correct; the bug was that qBittorrent's own "run on startup" preference created a pre-existing single-instance window before Mod+G ever ran, which the rule can only act on at window-creation time. No window-rule change could fix a race against an app's own autostart. Asked the user directly (three options: disable autostart, drop it from Mod+G but leave autostart, or leave everything alone) — user chose disabling autostart, since Mod+G is now the single, reliable entry point for the whole app set.
- **`add-niri-fullscreen-rule` built as a separate skill from `add-niri-window-rule`, not folded into it (2026-07-16, session 45)** — same single-responsibility split as the keybind/window-rule sibling pair: `add-niri-window-rule` sets `open-on-workspace`/`open-on-output`, this one only ever sets `open-maximized true`. Justified by real repetition — the same "add `open-maximized true` to an app's rule" edit was done 6 times by hand in one session (5 kept, 1 reverted) before the skill existed.
- **`add-niri-window-rule` built as a separate skill from `add-niri-keybind`, not folded into it (2026-07-16, session 44)** — the two workflows share verification steps (live output/app-id lookup, cross-host caveat check) but are triggered by different requests: a new keybind vs. a standalone monitor pin with no keybind. Kept as siblings per the single-responsibility bucket rule, with a one-line cross-reference in each pointing at the other so future requests route correctly rather than guessing.
- **Smoke-test's real config edit (qBittorrent→`dell-4`) kept rather than reverted (2026-07-16, session 44)** — asked the user directly whether the `add-niri-window-rule` smoke test's output (a real workspace + window-rule addition) should be treated as throwaway or kept. User chose to keep it, since they'd already confirmed it for real during the test — the smoke test doubled as a legitimate feature request.
- **Swapped the pnpm insecure waiver for an electron one rather than letting the bump fail (2026-07-16, session 43)** — pnpm-10.29.2's CVE fix landed upstream (vesktop now builds against generic `pnpm_10`), so that waiver became dead weight; the same nixpkgs bump immediately introduced a new insecure flag on `electron-40.10.5` (EOL), which vesktop wraps its runtime binary around. Asked the user directly ("is the electron waiver a safe option?") before adding it — confirmed and added, same waiver mechanism and same removal condition (retest at each future bump) as the pnpm one it replaced.
- **`improve-system`'s new-skill handoff borrows just `ship-skill`'s smoke-test step, not a full switch to `ship-skill` (2026-07-16, session 43)** — user asked directly whether improve-system should call `ship-skill` instead of `new-skill` for its skill-suggestion handoff. Declined: `ship-skill`'s per-skill commit + push-confirmation pause would fragment improve-system's single consolidated end-of-run commit into multiple, and add a second confirmation gate mid-run. Instead added just the smoke-test step, so an untested new skill doesn't ride along unverified into that consolidated commit.
- **kdeglobals ColorScheme write no longer gated on `XDG_CURRENT_DESKTOP` (2026-07-16, session 43)** — the guard only ever passed for an interactive live-session `nh os switch`; this repo's actual workflow (`nh os boot` + reboot) activates before any session exists, so it silently no-op'd on every real rebuild. Made the write unconditional — safe because a real Plasma session re-applies its own color scheme on login regardless.
- **niri.nix's DMS fix scoped to all niri hosts, not just natalie-laptop (2026-07-12, session 42)** — the bug (DMS HM wiring targeting only `home-manager.users.bosko`) was reported on natalie-laptop, but `niri.nix` is shared by gaming, laptop, and natalie-laptop. Asked the user directly via `AskUserQuestion` rather than assuming scope: fixing it in the shared module (chosen) means natty gets working DMS-on-niri everywhere niri is used; a natalie-laptop-only override was the alternative, rejected as inconsistent with how every other user's HM config is composed in this repo.
- **Hand-ported `omarchy-nix`'s good ideas rather than importing it as a flake dependency, even after seeing how much deeper it is (2026-07-11, session 41)** — asked directly for the downside of just importing the flake. Six concrete conflicts, not hypothetical ones: (1) architecturally a 15-file external tree vs. this repo's flat one-file-per-DE convention that `lib.deSmoke` assumes; (2) its `system.nix` redeclares options (`hardware.bluetooth.enable`, `services.pipewire`, etc.) already set by this repo's `desktopModules`, real conflicting-definition risk; (3) it replaces SDDM with greetd+tuigreet, breaking the SDDM-everywhere convention; (4) it builds Hyprland from source via its own pinned input instead of the nixpkgs cache; (5) it flips `nixpkgs.config.allowUnfree` globally and wants personal-identity options (`full_name`/`email_address`); (6) it's unmaintained with its own drifting dependency graph. Traded upstream's extra depth for staying inside a well-understood, repo-consistent module.
- **`nix-colors` added as a new flake input for real base16 theme-switching (2026-07-11, session 41)** — the previous Tokyo Night palette was hand-copied hex scattered across Hyprland/Waybar/Hyprlock, which only replicated Omarchy's *look*. `nix-colors` gives the actual *mechanism* (one `colorScheme` line feeds every component) without adding real dependency risk — it only pulls a lib-only nixpkgs repackage and the tinted-theming schemes data repo, not full nixpkgs, so no `follows` was needed.
- **`de-smoke-check` built as its own skill rather than folded into `deep-eval-check` (2026-07-11, session 41)** — `deep-eval-check` loops over the four *real* hosts; this covers the 9 DE modules no host imports, using `lib.deSmoke.<name>` instead of `nixosConfigurations.<host>`. Different target set and a different reason to reach for it (verifying an edit vs. a pre-rollout gate), so kept as a separate Verification-bucket skill per the single-responsibility rule, with each skill's docs cross-referencing the other.
- **`omarchy.nix` built from scratch rather than importing the `omarchy-nix` community flake (2026-07-10, session 40)** — a 10-source `/research` pass found `omarchy-nix` (Henry Sipp) is architecturally reusable (clean `nixosModules`/`homeManagerModules` pair) but hard-assumes greetd+PipeWire+its own NetworkManager/Bluetooth stack, directly conflicting with this repo's shared SDDM and `desktop-networking.nix`, plus ~8 months of dormancy (last commit 2025-11-13). Building a self-contained module matching this repo's existing DE-module pattern avoided that conflict entirely and kept consistency with the other 10 modules.
- **`omarchy.nix` kept fully independent of `hyprland.nix`, not layered on top (2026-07-10, session 40)** — user's explicit call. Omarchy's real shell (Waybar+Hyprlock+Walker) is unrelated to the DMS integration `hyprland.nix` just gained, so sharing a base would mean conditionally disabling DMS-specific bits rather than a clean reuse.
- **`hyprland.nix` deliberately left without a Nix-managed `hyprland.conf`/keybinds (2026-07-10, session 40)** — discovered by reading the actual `dms` flake input source that, unlike niri, DMS has no declarative NixOS/HM module for Hyprland: since Hyprland 0.55 it manages Hyprland's config **imperatively** via `dms setup` (Lua-based, migrates/backs up any existing `hyprland.conf`). A Nix-managed file would just get swept into DMS's own backup folder on first `dms run` — this was surfaced back to the user mid-task (original ask was "mirror niri-config.kdl's binds") and the scope was revised live rather than forcing an approach that would silently break on first login.
- **`ship-skill` pauses for confirmation before push, not before commit (2026-07-10, session 39)** — considered making the whole build→test→commit→push chain fully autonomous once kicked off, since the user had already approved building the skill. Rejected: by the time commit happens, the skill has made two consequential decisions in a row (what to build, and that it works) with zero user checkpoint since kickoff — pushing a third (making it public/permanent on `origin`) without a pause felt like one decision too many. Commit stays automatic since it's local and trivially reversible; push gets the explicit `AskUserQuestion` stop.
- **A managed-global skill goes live in the *same* session once its symlink exists, no restart needed (2026-07-10, session 39)** — previously assumed (session 38 note) that global-skill changes need a fresh session after rebuild+reboot. Directly falsified: after the user ran `nh os boot`, `ship-skill` was immediately invocable in this same running session — the skill list is read from disk per-invocation, not cached at session start. Rebuild is still required (untracked/uncommitted files aren't visible to flake eval), but the extra "new session" step in prior guidance was unnecessary.
- **Dolphin's `kdeglobals` fix applied via a `kwriteconfig6` `home.activation` script, not a `home.file` symlink (2026-07-10, session 38)** — every other piece of Qt/KDE glue in `niri.nix` (`qt6ct.conf`) is a force-managed, read-only `home.file` symlink, and that was the first approach considered here too. Rejected for `kdeglobals` specifically because, unlike `qt6ct.conf`, KDE apps actively write real state back into it (recent files, window geometry, `KFileDialog` settings) — a read-only symlink would silently break Dolphin's ability to persist its own settings. `kwriteconfig6` (from `kdePackages.kconfig`) sets just the one `[UiSettings] ColorScheme=*` key idempotently on every activation, leaving the rest of the file mutable.
- **`modules/` moved top-level; `dotfiles/` reserved for actual HM/dotfile content (2026-07-09, session 37)** — `dotfiles/common/modules/` contained system-level NixOS modules (security, bootloader, nvidia…), which misled readers of a public repo. Chose the minimal move (only `modules/` relocates; `dotfiles/common/configs` + `dotfiles/bosko` stay put) over a full `modules/` + `home/` restructure — after the move the `dotfiles/` name is already truthful, and the smaller blast radius kept the change verifiable as a pure rename.
- **`vpn.nix` moved into `desktopModules`, accepting a new-host coupling (2026-07-09, session 37)** — all three desktop hosts are VPN clients, so per-host imports were pure repetition. Known cost, discovered during the work: `vpn.nix` references `secrets/hosts/<hostname>.yaml` unconditionally, so a **future desktop host now needs its sops secrets file to exist at first eval** (wg keypair encrypted to admin; host key added post-boot as usual). The `new-host` skill and its templates were updated to say so. `nvidia.nix` deliberately stays per-host (the AMD-swap escape hatch is real; VPN membership isn't per-host policy on this fleet).
- **DE smoke checks run weekly + on dispatch, not on every push (2026-07-09, session 37)** — `lib.deSmoke` (laptop config × each DE module) exists because unused DE modules are never evaluated by host builds and rot silently — proven immediately when the first local run caught pantheon/mate breakage. Gated the CI job to `schedule`/`workflow_dispatch`: DE rot accumulates on the timescale of nixpkgs bumps, not pushes, and push/PR CI stays at ~3 min. The job measured ~5 min, so moving it on-push later is a one-line `if:` deletion if ever wanted.
- **Refactor verified by drvPath comparison, not just eval success (2026-07-09, session 37)** — for a pure reorganization the bar was "provably nothing changed": captured every host's `toplevel.drvPath` before touching anything; vpn-server came back bit-identical, and the desktop hosts (whose drvPaths legitimately shift from package-list merge order) were compared field-by-field via `nix eval --apply` against committed HEAD (`git+file://` = baseline, `.#` = dirty tree) — package sets, flatpaks, sshd, DNS, wg, stateVersion all identical. Worth reusing for any future no-op-intended refactor; a dry-run alone only proves the local host.
- **Kept the 3 new redundant `Bash(*)` permission entries rather than pruning them (2026-07-09, session 36)** — asked directly whether to prune project-level Bash allow entries now that they're all functionally superseded by the global `Bash(*)` wildcard. Decided to keep: removing them buys zero reduction in prompts (the global wildcard already suppresses everything), but loses a documented, already-vetted "these are genuinely read-only" list that would need re-deriving if the global `Bash(*)` is ever tightened later. `mcp__nixos__nix` is the one entry type that's genuinely load-bearing, since `Bash(*)` doesn't cover MCP tools.
- **`qt6ct.conf` pulled into Nix; DMS's `settings.json`/`matugen.conf` stay unmanaged (2026-07-09, session 35)** — Asked directly whether any of the three files touched while debugging the Qt-theming bug should move into the repo. `qt6ct.conf` is genuinely static glue config that DMS only touches (mtime bump, never rewrites content), so a read-only Nix copy is low-risk (worst case a cosmetic touch warning) and prevents this exact bug recurring on reinstall. `settings.json` and `matugen.conf` were rejected: both are actively rewritten by DMS itself at runtime (every settings-UI change, every wallpaper/theme switch), so a read-only Nix symlink over either would break DMS's own live writes — the same reasoning session 34 already applied to `dms/*.kdl`.
- **Niri `config.kdl` managed via Home Manager despite a known future-friction risk (2026-07-09, session 34)** — Considered leaving the file unmanaged to avoid ever blocking a DMS auto-injected `include` line (DMS is designed to add more `include`s into `config.kdl` as its integration toggles are enabled, and a Nix-managed symlink is read-only). Chose to manage it anyway: the failure mode is mild and self-announcing (a DMS setting just wouldn't visually apply, fixed with a 30-second manual edit + rebuild), versus the alternative of the file staying an unversioned, non-reproducible mutable state permanently — the same tradeoff already accepted for `katerc`/`kitty.conf`.
- **DMS's own generated state deliberately left unmanaged (2026-07-09, session 34)** — `DankMaterialShell/settings.json` and `niri/dms/*.kdl` are written live by DMS's own settings UI (themes, colors, per-output layout). Managing those declaratively would fight the point of having a GUI theme picker: a read-only Nix symlink would make every live UI change fail to persist or get overwritten on next rebuild.
- **Project `commit`/`push` skills deleted; the global `git-commit`/`git-push` pair is now the only commit/push path (2026-07-03, session 30)** — Partially supersedes session 28's "fixed for drift but NOT made identical" call: closing the behavioral gaps left the two pairs as near-duplicates whose overlapping triggers made skill selection unpredictable. Deleted the **project** pair rather than the global one because the global pair must keep working in the user's other repos (farmer, FinanceGuru) — exactly the reason session 28 refused to copy the `-C /home/bosko/NixOS` pinning into it. The repo-specific commit-format reference moved to CLAUDE.md (a doc, not a skill), and the hardcoded `-C`/`origin main` conventions died with the files — sessions run inside the repo anyway.
- **natalie-laptop keeps DHCP; drift is handled at the router, not in NixOS config (2026-07-03, session 30)** — When its LAN IP moved .103 → .101, a static IP in `networking.nix` was considered and rejected: the machine is a roaming laptop, and a hardcoded address breaks on every other network it joins. Correct fix if recurrence matters is a router-side DHCP reservation (MAC → .101); the repo just records whatever the router currently hands out, in `hosts.json` + `ssh.nix`.
- **Backups scrapped after scoping — no unbacked irreplaceable data exists (2026-07-02, session 29)** — A restic/B2/WireGuard-tier backup architecture was proposed and interview-scoped, then the user killed it on the facts: projects are in GitHub repos, photos/videos are already backed up on an existing cloud service, games re-download, and the only genuinely unbacked data (Jellyfin state on gaming) is an explicitly accepted loss. Recorded in memory so backups are never re-proposed as pending. The sops admin age key was confirmed **two-copy** (gaming + laptop), closing the last residual.
- **pnpm insecurity waived temporarily rather than re-pinning nixpkgs or overlaying vesktop (2026-07-02, session 29)** — Three options for the vesktop/pnpm-10.29.2 eval breakage: (a) hold nixpkgs back again (rejected — throws away two weeks of security updates the unpin just gained, for a build-tool-only CVE), (b) overlay vesktop to use `pnpm_10` like master (rejected — needs a new `pnpmDeps` fetch hash and a from-source build; master's fix arrives on the channel by itself), (c) scoped `permittedInsecurePackages` waiver with a removal condition (chosen — the CVEs are in the pnpm CLI at build time, not vesktop's runtime; the standard nixpkgs mechanism for exactly this). Removal is tracked in Known Issues, memory, and the waiver's own comment.
- **CI must deep-eval each host; `nix flake check` alone is not a gate (2026-07-02, session 29)** — Proven empirically: flake check verifies `nixosConfigurations` only shallowly (toplevel-is-a-derivation, not full eval), so both the local check and the first CI run passed a config on which every desktop host failed to evaluate. The workflow now forces each host's `toplevel.drvPath`; the same rule was written into the flake-check and flake-update-verify skills as gotchas. Lesson: "flake check passed" means "no shallow errors," nothing more.
- **`git-commit`/`git-push` fixed for behavioral drift, but NOT made identical to `commit`/`push` (2026-07-02, session 28)** — The skill-audit flagged the global (any-project) `git-commit`/`git-push` as having drifted from the NixOS-repo-specific `commit`/`push`. Fixed the genuine gaps (git-push had zero confirmation gate at all; git-commit's skip-confirm behavior was undocumented) but deliberately did **not** copy `commit`/`push`'s `-C /home/bosko/NixOS` pinning or hardcoded `origin main` — those are correct only because those two skills are project-local and NixOS-only; copying them into the global pair would break git-commit/git-push in the user's other repos (farmer, FinanceGuru), which need to operate on the current cwd/branch instead.
- **Team-member knowledge-path duplication resolved via cross-reference, not a shared config.json (2026-07-02, session 28)** — Same call as session 22's `[[deliberately skipped]]` team-`*` config consolidation: the four skills are separately-symlinked global skills with no shared-file mechanism without new `home.file` wiring, and the actual drift risk of a static repo-internal path is low. Added a one-line cross-reference in all four copies instead, so a future rename gets caught rather than silently drifting.
- **Declined password rotation after the branch-leak fix (2026-07-02, session 27)** — Offered rotating `bosko`/`natty` login passwords given the ~7-week public exposure window on the deleted branches (see above). User's call: skip it, judging it unlikely anyone found/used the hashes in that window. Recorded as a deliberate accepted-risk decision, not an oversight — revisit if any sign of unauthorized access surfaces.
- **Deleted the leaking branches outright rather than rewriting them (2026-07-02, session 27)** — Two options were on the table: `git filter-repo --replace-text` to scrub and keep the branches, or delete them outright. User chose deletion — simpler, and these were old version-tag branches with no indication of active use, so preserving their history had no clear value against the cost of a second filter-repo pass.
- **`research` skill: fresh `general-purpose` sub-agents, configurable count (default 10), chat-only output (2026-07-02, session 27)** — Three open questions from the interview: (1) sub-agent type — chose fresh `general-purpose` over `fork`, since each review agent only needs the topic + its one URL, not the coordinator's conversation history; forking would carry unneeded context and cost. (2) result count — made it an optional second argument (`research <topic> <n>`) rather than hardcoding 10, so a quick lookup doesn't always pay for a full 10-way fan-out; default stays 10 to match the original ask. (3) output — chat text only, not an Artifact; the user picked this directly over the recommended Artifact option, so a future revisit (e.g. if reports get long enough to want a scannable page) should re-ask rather than assume.
- **niri X11 support relies on niri's built-in `xwayland-satellite` spawn, not a manual service (2026-07-01, session 26)** — First attempt manually managed `xwayland-satellite` + a global `DISPLAY` export, which broke the SDDM greeter (Weston picked the X11 backend over DRM and crashed to a black screen) because the session-variable export isn't scoped to the graphical session only. Corrected by removing the manual service/export and relying on niri ≥ 25.08's built-in auto-spawn + auto-`DISPLAY`, keeping only the package on `systemPackages`. Lesson: `environment.sessionVariables` is global to every session type (including the display manager's own greeter), so exports meant only for a compositor's user session need a session-scoped mechanism, not a system-wide one.
- **`session-closer` reads the transcript as ground truth, not the live context window (2026-06-29, session 23)** — Added transcript-reading to STEP 2 of `session-closer` so a close mines the unsummarized on-disk JSONL rather than reconstructing from a context window that drops early turns as it grows. Complements the session-21 change that gave `skill-upgrade`/`skill-suggestion` transcript-*history* awareness — same store (`~/.claude/projects/`), but session-closer reads the *current* session for accuracy where those two grep *past* sessions for recurrence. Kept the multi-MB guard (extract turns with `jq`, never dump raw).
- **Managed permission layer stays separate from the `/improve-system` allow-list — do not sync (2026-06-29, session 23)** — The destructive-command `deny`/`ask` rules and fork-bomb hook live in the NixOS-managed `/etc/claude-code/managed-settings.json` (highest precedence, system-deployed, Claude can't auto-edit it). `/improve-system`'s `fewer-permission-prompts` step only appends `permissions.allow` to the per-project `.claude/settings.json`. Merging them was considered and **rejected**: it would either duplicate the guardrails into a lower-precedence Claude-editable file or hand the orchestrator the power to loosen safety rails — and `trimClaudeSettings` already deletes any deny/ask/hooks that leak into the personal settings on every rebuild. Separation is intentional. (Aside noted, not acted on: the global `~/.claude/settings.json` allows `Bash(*)`, so the project allow-list entries are effectively redundant and `fewer-permission-prompts` is near-no-op for this setup.)
- **Transcript-history awareness added to the two session-reactive skills only (2026-06-28, session 21)** — `/improve-system`'s five sub-skills were checked against `~/.claude/projects/` (the session transcript store). Only `skill-upgrade` and `skill-suggestion` got the addition: they previously looked at just the live conversation, and cross-session signal is exactly their value (a recurring misfire / a workflow repeated across sessions). Deliberately **not** added to `claude-rules` (edits `CLAUDE.md` only), `skill-audit` (audits skill files, not history), or `improve-system` itself (pure orchestrator — the work belongs in the sub-skills it invokes). `fewer-permission-prompts` already scans transcripts but is a non-editable built-in. Both edits mandate `grep`, never whole-file reads — transcripts run multi-MB and would blow context.
- **`/improve-system` is a thin orchestrator, not a merged mega-skill (2026-06-28, session 20)** — The user wanted "one skill to upgrade my Claude ecosystem." Rather than flatten `skill-upgrade`/`skill-suggestion`/`claude-rules`/`skill-audit`/`fewer-permission-prompts` into one giant skill, `/improve-system` *invokes* each via the Skill tool and gates their effects. Rationale: each sub-skill stays single-responsibility and independently runnable (matching the bucket gate), there's no logic to drift out of sync, and the orchestrator only owns sequencing + the auto-apply-low-risk / confirm-structural policy. Rejected the merge because it would duplicate five skills' logic and re-introduce the drift class `/skill-audit` exists to catch.
- **VPN endpoint IP + subnets scrubbed from the public README only, not repo-wide (2026-06-28, session 20)** — User asked the README to stop carrying sensitive network detail. Redacted the endpoint IP / VPN+LAN subnets / per-host addresses from `README.md` and added a guardrail to `session-closer` so future closes stay clean. **Deliberately scoped to the README:** the same values remain in host configs, `.claude/hosts.json`, the state-doc history, and git history, because `wg-quick` needs the endpoint string and scrubbing it everywhere risks breaking the tunnel for no real security gain (WireGuard's security doesn't depend on endpoint secrecy). Supersedes, *for the README only*, the 2026-06-15 "endpoint IP intentionally left in-repo" decision. The user was shown the residual exposure and chose to leave it at the README.
- **Pinned `nixpkgs` back lock-only rather than reverting the whole update or patching `kernelFile` (2026-06-26, session 19)** — The new unstable rev broke the zen kernel (`vmlinuz` shipped where `bzImage` is declared). Three options weighed: (a) `git restore flake.lock` to drop all 6 bumps, (b) override `system.boot.loader.kernelFile = "vmlinuz"` in config, (c) pin only `nixpkgs` back via `--override-input` and keep the other 5. Chose (c): it preserves the good `home-manager`/`sops-nix`/etc. bumps while dodging the one broken input, and a **lock-only** pin (no `flake.nix` edit) means the next `/update` naturally re-resolves nixpkgs forward and lifts the pin once upstream is fixed. Rejected (a) as throwing away good updates, and (b) because forcing `kernelFile` would actually mis-name the boot image at activation (the real bug is upstream's target/output mismatch, not our config). `home-manager`/`dms`/`financeguru`/`sops-nix` all `follows = "nixpkgs"`, so they ride the pinned-back nixpkgs too — fine, they build against it.
- **`.claude/hosts.json` is the single source of truth for host data (2026-06-23, session 18)** — Host SSH targets, IPs, and the WireGuard peer→host map were duplicated inline across 8 skills and had drifted into a real bug. Centralized into one JSON file that scripts read via `jq` and prose skills resolve at runtime. Rejected keeping per-skill copies (that's what rotted) and rejected deriving everything from `flake.nix`/`networking.nix` (SSH user@host targets aren't fully encoded there). Skills now read it instead of re-hardcoding.
- **vpn-server SSH login is `bosko@`, not `ubuntu@` (2026-06-23, session 18)** — Verified live: `ubuntu@150.136.232.63` is **denied**; `bosko@150.136.232.63` works with passwordless sudo (it's a NixOS host now, `commonModules` defines `bosko`). Several skills had documented the Oracle cloud-init `ubuntu` user — that was stale and made `/vpn-status` fail outright. Lesson applied going forward: verify a constant against the live system before trusting an inline copy; the docs can be the stale side.
- **Dropped the phantom `nixos-server`/`server` host, kept `pi-hole` (2026-06-23, session 18)** — `server` is not in the flake (only 4 hosts) and `nixos-server` doesn't even resolve, so `remote-rebuild #server` and `nix-repl`'s `nixosConfigurations.server` were broken. Removed it from `remote-rebuild`/`nix-repl`/`ssh-host`/`hosts.json` and `audit-config`'s host list. `pi-hole` is a real LAN DNS appliance (10.0.0.20) but not a flake host and `bosko@` SSH is denied — kept as a non-flake reference per user choice (vs. removing both).
- **Skill quality is governed by a bucket gate + 5 structural lenses (2026-06-23, session 18)** — Every skill must fit one bucket (Utility / Verification / Data Enrichment / Orchestration; Orchestration coordinating other skills is *not* straddling) and is audited against: deterministic→`scripts/`, templates→`assets/`, re-entered-config→shared JSON, pick-ones→AskUserQuestion, invocation inputs→`## Arguments`. Baked into `new-skill` (the gate) and the new `/skill-audit` meta-skill (the sweep). Two things deliberately *not* adopted: global-skill `assets/` extraction (per-file symlink overhead) and an `arguments:` YAML frontmatter key (unconfirmed support — use `## Arguments` prose).
- **Loop run-logs are gitignored, loop config is tracked (2026-06-22, session 17)** — Added `.claude/loops/**/output-*.md` and `memory-*.md` to `.gitignore`. Rationale: these are machine-generated, per-run, ever-accumulating artifacts that the next run reads back locally — committing them buys nothing and grows history noise. Crucially this does **not** affect skill portability: the loop *skills* live in `.claude/skills/` and are already tracked, so they travel with any clone. Chose a narrow `output-*`/`memory-*` glob rather than ignoring all of `.claude/loops/` so genuine config like `public-repo-guard/baseline-allowlist.md` stays tracked. Verified with `git check-ignore` that run-logs match and the allowlist does not.
- **First three loops + their per-loop policy choices (2026-06-21, session 16)** — Built `/fleet-rollout`, `/flake-update-verify`, `/public-repo-guard`. Key baked-in decisions: (1) **rollout uses dry-run-gate-then-`switch` with a full health sweep per host**, not the repo's usual `boot`-only convention — rejected `boot`+dry because health checks are meaningless against an inactive config, and rejected bare `switch` because the dry-run gate catches eval breaks before any activation; (2) **`flake-update-verify` commits the lock but never applies, and restores the previous lock on eval failure** rather than leaving a broken tree or auto-pinning — chose plain `git checkout -- flake.lock` over a saved-copy/`pin-input` path for the revert (the loop's clean-tree precondition makes git the reliable source of truth); applying stays a separate, deliberate `/fleet-rollout` step; (3) **`public-repo-guard` converges via a committed baseline-allowlist file**, not run-memory carry-forward — a durable, diffable canonical list of accepted findings is more reliable than re-deriving intentional-vs-genuine each run, and the loop only appends to it with approval. All three default Training Mode ON; `/fleet-rollout` especially should stay ON since it activates live configs.
- **Generated loops are project-local, not repo-global (2026-06-20, session 15)** — `/create-loop` writes generated loops to `<repo>/.claude/skills/`, not into `dotfiles/bosko/claude/skills/`. Rationale: repo-global skills are symlinked read-only from `/nix/store` and need a `bosko-claude.nix` edit + rebuild *per loop* — unacceptable friction for an artifact meant to be spun up on demand. Project-local skills are writable and resolve immediately. The tradeoff (loops aren't available in other projects) is fine: loops are task-specific to the repo they're created in. `/create-loop` *itself* stays repo-global so it's available everywhere.
- **Loops are standalone/self-orchestrating, not thin `/loop` wrappers (2026-06-20, session 15)** — Each generated loop contains its own orchestration (steps, done-rule checks, Training Mode, retry cap) and runs as one command `/<loop-name>`. The built-in `/loop` can still wrap it for interval scheduling. Chose this over generating plain task-skills driven by `/loop` so a loop is a complete, inspectable artifact. Caveat documented in the template: Training-Mode-ON pauses need an interactive turn, so unattended/`/loop`-scheduled runs should flip to OFF.
- **Per-loop log dir for Output + Memory, not the global memory system (2026-06-20, session 15)** — Each run writes `.claude/loops/<loop>/output-<date>.md` and `memory-<date>.md`. Kept separate from `~/.claude/.../memory/` so a loop's run history is self-contained and diffable run-over-run, and so machine-generated loop logs don't pollute the curated project memory index.
- **Deploy vpn-server via `boot` + reboot, not `switch` (2026-06-17, session 13)** — `nixos-rebuild switch` over SSH runs `switch-to-configuration` as a `systemd-run --pipe` unit tied to the SSH connection; vpn-server's activation restarts networking, dropping SSH and tearing the activation down half-applied (corrupts firewall/NAT). `boot` writes the bootloader default without a runtime activation, so SSH never drops; the subsequent reboot does a clean boot-time activation. Detached `systemd-run --collect` is the alternative for no-reboot deploys/rollbacks. Captured in the `remote-rebuild` Gotchas.
- **`wg0` MTU clamped to 1380 (2026-06-17, session 13)** — The Oracle path carries only ~1400-byte inner packets; wg-quick's default 1420 + black-holed PMTUD silently dropped full-size packets, corrupting large downloads. `mtu = 1380` in shared `vpn.nix` fixes it for all clients. Chose lowering the interface MTU (bounds inner MSS directly) over relying on TCP MSS clamping/PMTUD.
- **`nixpkgs-stable` → `nixos-25.11` (2026-06-17, session 13)** — 25.05 hit EOL (dropped from the supported channel list); 25.11 (Xantusia) is current stable. Bumped the input; stateVersion was already 25.11. vpn-server is the only consumer. Supersedes the 2026-05-21 "pinned to nixos-25.05" decision below.
- **fwupd ESP left undetected on gaming, override removed (2026-06-15, session 5)** — Confirmed empirically that neither `OverrideESPMountPoint` (dead key in fwupd 2.1.4) nor `EspLocation` can force fwupd 2.x to accept gaming's ESP, because 2.x enumerates ESPs strictly by partition type and the partition carries MBR type `0x0c` instead of `0xEF`. The only real fix is a partition-type change (`parted /dev/nvme0n1 set 1 esp on`), which was **deliberately declined**: no LVFS firmware exists for this ASRock X570 board, Secure Boot is off (dbx updates moot), and the lone updatable device updates over ATA not the capsule path — so the fix buys zero benefit while touching the boot partition table. Removed the config workaround entirely rather than carry a no-op that implies it works. Revisit only if gaming moves to GPT + Secure Boot or an LVFS-supported board.
- **Declarative settings trim via HM activation script (2026-06-09, session 3)** — `home.activation.trimClaudeSettings` in `bosko-claude.nix` replaces the one-shot `~/.claude/trim-after-managed.sh` script. On every rebuild it automatically strips redundant deny/ask/hooks keys from `~/.claude/settings.json` (idempotent; no-op if the keys are already absent or the managed file does not yet exist). Scoped to bosko only; if natty ever uses Claude Code it should be added for her separately. Chose Home Manager activation over a systemd unit or login hook because activation runs in the correct user context and is guaranteed to execute on every generation switch.
- **Managed Claude Code policy via NixOS module (2026-06-09)** — `claude-code.nix` deploys the policy through `environment.etc` so it is system-managed, version-controlled, and tamper-resistant. `jq` is pinned to its Nix store path inside the JSON so the fork bomb hook works regardless of the shell's `$PATH`. Chose `environment.etc` over a declarative file symlink because the content is derived from Nix expressions (package store paths) that must be interpolated at build time.
- **Personal `~/.claude/settings.json` trimmed to prefs only (2026-06-09)** — After confirming the managed file was active, all deny/ask rules and the fork bomb hook were removed from the personal file. The managed layer handles policy; the personal file now holds only tool preferences.
- **Package consolidation to `shell.nix` (2026-06-09)** — `nodejs`, `pnpm`, `p7zip`, and `jq` moved to `commonModules` shell.nix. Rationale: these are general-purpose tools with no desktop or host-specific justification; consolidation removes duplication and ensures consistency across all hosts including vpn-server.
- **natalie-laptop switched to Plasma (2026-06-04)** — Switched from Cosmic to Plasma 6 to align with gaming and share the `krohnkite` tiling configuration without per-host duplication.
- **krohnkite added to shared `plasma.nix` (2026-06-05)** — Any Plasma host should get the tiling script automatically; keeping it per-host creates duplication. Added directly to `plasma.nix` `systemPackages`.
- **lutris re-enabled (2026-06-03)** — The `openldap-2.6.13-i686-linux` binary cache regression that blocked `lutris` since May 2026 is resolved in nixpkgs-unstable. Un-commented in `gaming.nix` and flake lock bumped.
- **Root SSH disabled on vpn-server (2026-06-03)** — `PermitRootLogin = "no"` replaces `"prohibit-password"`. Rationale: the server is internet-facing (TCP 22 open); key-based root login still presents an elevated-privilege surface. `bosko` with passwordless sudo is the new deploy path. `wheelNeedsPassword = false` is safe because bosko requires SSH key auth to reach the machine at all.
- **SDDM auto-login disabled on gaming (2026-06-03)** — `autoLogin.enable = false`. Gaming is a physically accessible workstation; an unauthenticated session from physical access is a meaningful risk. Now active on gaming and confirmed working.
- **Avahi `openFirewall` deferred (2026-06-03)** — Closing UDP 5353 would break mDNS-based printer discovery on all three desktop hosts. The correct fix (interface-scoped rules) requires careful testing. Accepted as a known risk for now; tracked in Known Issues.
- **remote-rebuild skill updated to bosko@ (2026-06-03)** — All skill documentation now targets `bosko@150.136.232.63`. The old `root@` path is permanently blocked as of this session's deploy.
- **`mistral-nemo:12b` selected as Hermes Agent model (2026-05-26)** — The only evaluated model satisfying all three constraints: 128K context (above Hermes Agent's hard 64K minimum), excellent tool-calling support, and ~7GB Q4 quantisation within the RTX 3070's 8GB VRAM. `qwen2.5:7b` (32K context), `llama3.1:8b` (poor tool-calling), and `qwen2.5:14b` (~9GB, VRAM OOM) were all rejected. `hermes-3-llama-3.1:8b` was rejected by Hermes Agent's context-window gate at startup.
- **Dummy `api_key = "ollama"` in Hermes settings (2026-05-26)** — The OpenAI client library validates that `api_key` is non-empty before making the request; Ollama ignores the value. A non-empty dummy string is the correct workaround.
- **Ollama config colocated in `hermes-agent.nix`, not a separate file (2026-05-26)** — A single-module approach keeps the gaming LLM stack coherent; `hosts/gaming/ollama.nix` was created and then absorbed into `hermes-agent.nix` in the same session.
- **`bosko` in `hermes` group, declared in `hermes-agent.nix` (2026-05-26)** — The `hermes` group only exists on gaming; group membership belongs in the same gaming-specific file. `users.nix` (shared across all hosts) is not the right place for a group that only exists on one host.
- **SSH config API corrected to `programs.ssh.settings` (2026-05-26)** — Migrated from deprecated `programs.ssh.matchBlocks`; `enableDefaultConfig = false` added to prevent implicit defaults.
- **SSH config migrated into Home Manager (2026-05-25)** — All five SSH hosts declared in `dotfiles/common/configs/ssh.nix` with explicit `user` fields. Version-controlled, reproducible across hosts, rebuilt automatically on each generation. Both users inherit the same block via `home.nix`. The previous hand-maintained `~/.ssh/config` file should be removed on each host after the next rebuild.
- **`vpn-on`/`vpn-off` aliases use `systemctl start/stop wg-quick-wg0` (2026-05-22)** — `wg-quick` is not in PATH for non-root zsh sessions; `systemctl` avoids the PATH dependency entirely. The systemd unit name is `wg-quick-wg0` (hyphen-joined), which is how systemd renders the `wg-quick@wg0` template instance.
- **`audit=0` appended on vpn-server via `lib.mkAfter` (2026-05-22)** — The Oracle Cloud ARM kernel's broken audit subsystem floods `kauditd` on boot, causing PAM D-Bus timeouts that drop SSH sessions during `nixos-rebuild switch`. Using `lib.mkAfter [ "audit=0" ]` in `hosts/vpn-server/configuration.nix` appends after `security.nix`'s `audit=1`; last value wins at boot. This avoids touching `security.nix` or disrupting other hosts.
- **`hm-session-vars.sh` sourced in `zsh.shellInit` (2026-05-22)** — Non-login shells (terminal emulators, scripted tool calls) do not automatically source the HM session vars file. Adding sourcing to `shellInit` (not `interactiveShellInit`) covers both interactive and non-interactive non-login sessions. The `if [ -f ... ]` guard is safe on hosts that have not yet been rebuilt.
- **`nixfmt-classic` replaced with `nixfmt` (2026-05-22)** — The `nixfmt-classic` package is deprecated upstream; `nixfmt` is the actively maintained successor. Updated in `shell.nix` (system package) and `helix.nix` (formatter reference).
- **Printing module extracted to `printing.nix` (2026-05-21, late session)** — Replaced duplicated per-host CUPS/Avahi config with a shared `modules/printing.nix` (CUPS, Avahi mDNS, gutenprint/hplip/brlaser drivers). Added to `desktopModules`. `kdePackages.print-manager` retained in `plasma.nix` — it is a KDE-specific print-job GUI and does not belong in a DE-agnostic module. Two-step commit: `print-manager` was first moved into `printing.nix` then corrected back to `plasma.nix`.
- **`~/.local/bin` added to bosko sessionPath (2026-05-21, evening)** — `home.sessionPath = [ "$HOME/.local/bin" ]` added to `dotfiles/bosko/bosko-claude.nix`. Diagnosed via `/doctor`: native claude-code binary installed at `~/.local/bin/claude` was not in PATH. Fix scoped to bosko's HM config (not shared with natty). Dry-run confirmed clean (+2.94 MiB closure, no package changes, no service restarts).
- **`server` placeholder removed from flake (2026-05-21, evening)** — `hosts/server/` directory and `flake.nix` entry deleted. The `server` host has no physical hardware; the placeholder was dead weight. Will be re-created via `/new-host` when hardware is available.
- **dbus-broker transition COMPLETE on all 5 hosts (2026-05-21)** — Root cause of resistance: `nix-flatpak` bundles its own older nixpkgs (`da5ad661`) whose `dbus.nix` defaults to `mkDefault "dbus"`, silently winning over nixpkgs-unstable's `mkDefault "broker"`. Fix: explicit (non-default) `services.dbus.implementation = "broker"` in `security.nix` beats all `mkDefault` values regardless of source. All five hosts verified via `systemctl status dbus`.
- **nixpkgs channel split (2026-05-21)** — Desktop hosts (gaming, laptop, natalie-laptop) stay on `nixos-unstable` for rolling updates. Server hosts (vpn-server, server) are pinned to `nixos-25.05` via a dedicated `nixpkgs-stable` input in `flake.nix`. Rationale: server stability and predictability outweigh the value of rolling updates for headless hosts; desktop hosts benefit from latest drivers and DE releases. vpn-server deployed and confirmed healthy on 25.05 with WireGuard peers intact.
- **CLAUDE.md corrections (2026-05-21)** — Three inaccuracies fixed: (1) `virtualisation` removed from `desktopModules` description — it is gaming-only; (2) `+ disko` added to vpn-server module composition; (3) directory layout tree corrected (`bosko/` indentation, `disko.nix` added to vpn-server listing). natty's wheel/trusted-user status corrected — she has both, same as bosko; the only differences are no user packages and no SSH keys.
- **Five new skills added (2026-05-21)** — `diff-generations` (nix store diff-closures between generations), `flake-check` (validate flake across all 5 hosts), `journal` (tail journald, local or remote), `nix-repl` (print repl command + host-specific starter expressions), `fmt` (alejandra with nixpkgs-fmt fallback). `nix-repl` prints rather than execs to avoid the TTY issue.
- **`/nixos-rebuild` skill removed (2026-05-20, night)** — `nh os boot` requires a controlling TTY for sudo; it cannot be run from a Claude Code subprocess. The correct workflow is: Claude edits config → user runs `rebuild` in their terminal. The `/nixos-dry-run` skill is unaffected (uses `--dry`, no sudo). `switch-de` next-steps updated to reference the `rebuild` alias directly.
- **Skill library extended to 18 skills (2026-05-20, second session)** — `ssh-host`, `remote-rebuild`, `rollback`, `search-pkg`, `new-host`, and `pin-input` added. The library now covers the full NixOS workflow surface: SSH, remote deployments, rollbacks, package discovery, host provisioning, and flake input management.
- **`pin-input` warns about follow-inputs (2026-05-20)** — When pinning `nixpkgs`, the skill explicitly warns that `home-manager` and `disko` follow nixpkgs by default and will also be pinned unless the user breaks those follows first.
- **`new-host` does not auto-edit `flake.nix` (2026-05-20)** — Shows the exact flake.nix entry to add (including correct `lib.mkSystem` call and module list) but requires the user to apply it. Same safe-by-default convention as `new-module`.
- **agenix deferred (2026-05-20)** — Explored agenix for managing WireGuard private keys declaratively; decided against it. VPN is fully working, no reinstalls expected, and adding agenix would require a full re-keying of all hosts. Plan saved at `/home/bosko/.claude/plans/agentix-wireguard-setup.md` for reference if a future reinstall happens.
- **Private keys confirmed out-of-repo (2026-05-20)** — WireGuard private keys live at `/etc/wireguard/private.key` on each host, referenced by path in the Nix config. They are never in the git repository. `.gitignore` already covers `*.key`.
- **Project-local Claude Code skill library started (2026-05-20, first session)** — Seven skills added initially: `nixos-dry-run`, `nixos-rebuild`, `nixos-gc`, `vpn-status`, `new-module`, `commit`, `push`. Skills travel with the repo and are version-controlled.
- **`new-module` skill does not auto-edit `flake.nix` (2026-05-20)** — Shows the exact import line to add but requires the user (or explicit follow-up) to apply it. Avoids silent modifications to the most critical file in the repo.
- **`commit` skill stages specific files, never `git add -A` (2026-05-20)** — Prevents accidentally committing private keys, `.env` files, or large build artifacts; falls back to `-A` only on explicit user request.
- **`nixos-gc` keeps 3 generations (2026-05-20)** — Matches the `cleanup` alias in `shell.nix`; retains rollback headroom without the destruction of `nix-collect-garbage -d`.
- **Full-tunnel VPN routing chosen (2026-05-18)** — `allowedIPs = ["0.0.0.0/0" "::/0"]` routes all client traffic through the Oracle server, hiding the client's real IP. Split-tunnel (VPN subnet only) was briefly in place but replaced in the same session.
- **`::/0` dropped from allowedIPs — both fixes are needed together (2026-06-17, session 11)** — Session 10 disabled IPv6 but kept `::/0`, reasoning it would keep the tunnel "correct if IPv6 is ever added." That was wrong and broke `wg-quick-wg0` on every boot: wg-quick installs a route for each allowedIP, `ip -6 route add ::/0 dev wg0` fails on the IPv6-disabled device, and wg-quick tears the interface down (exit 4). Dropped `::/0`, leaving `allowedIPs = [ "0.0.0.0/0" ]`. With no IPv6 stack there is nothing to leak, so it is still a full tunnel — the privacy argument for keeping `::/0` only holds when IPv6 is *enabled*. **If IPv6 is ever restored on the server, flip `networking.enableIPv6 = true` AND re-add `"::/0"` together — never one without the other.** A `NOTE:` in `vpn.nix` records this trap.
- **IPv6 disabled on VPN clients over dropping `::/0` (2026-06-17, session 10)** — The full tunnel's `::/0` pulls IPv6 into a v4-only `wg0`, black-holing IPv6-only hosts (TMDb) and hanging Jellyfin's image fetches for 100s. Two fixes were weighed: (a) `networking.enableIPv6 = false` on the clients, vs (b) removing `::/0` from `allowedIPs`. Chose (a) to preserve the full-tunnel privacy guarantee (option (b) alone would route IPv6 natively around the VPN, leaking the real IP for any v6 destination). _Superseded in session 11:_ keeping `::/0` alongside disabled IPv6 broke wg-quick — the two changes belong together (see entry above). The end state is **both**: IPv6 off **and** `::/0` removed.
- **DNS promoted to shared `vpn.nix` (2026-05-18)** — `dns = [1.1.1.1 8.8.8.8]` was first added as a natalie-laptop-specific workaround (LAN resolver `10.0.0.20` is unreachable via full-tunnel), then recognized as universally correct and moved to the shared module. All three client hosts now inherit it automatically on `vpn.nix` import.
- **ARM server builds natively (2026-05-18)** — `server-rebuild` now SSHs to the Oracle ARM server and builds there; `binfmt` aarch64 emulation is available on gaming as an offline fallback. Cross-compiling `aarch64` on `x86_64` without explicit cross-compilation config fails with a platform mismatch.
- **VPN subnet `10.10.0.0/24` (2026-05-18)** — Chosen to avoid overlap with Oracle's internal LAN (`10.0.0.x`). Server: `10.10.0.1`; gaming: `10.10.0.2`; laptop: `10.10.0.3`; natalie-laptop: `10.10.0.4`.
- **`trustedInterfaces + checkReversePath = "loose"` on vpn-server (2026-05-18)** — Required for the firewall to forward peer packets and accept asymmetric NAT return traffic arriving on `enp0s6`.
- **Starship config inlined into `shell.nix` (2026-05-17)** — Eliminates the external TOML dotfile and its `xdg.configFile` symlink. The configuration is a first-class Nix attrset evaluated at build time; the full Gruvbox-themed prompt with all module settings was preserved. Single source of truth, no runtime file management.
- **Helix auto-format disabled globally (2026-05-17)** — Applied uniformly with `auto-format = false` across all configured languages. Prevents unexpected reformatting during editing sessions; format manually when needed.
- **NordVPN `dotfiles/vpn/` deleted (2026-05-17)** — Dead code from before the WireGuard approach. No path back to NordVPN in the current design.
- **Host directories moved to `hosts/` (2026-05-15)** — All five host-specific directories (`gaming`, `laptop`, `natalie-laptop`, `server`, `vpn-server`) relocated from `dotfiles/<hostname>/` to `hosts/<hostname>/`. Clarifies the repo layout: `dotfiles/` now holds only shared and user-specific HM configs; `hosts/` holds machine-specific NixOS config.
- **Bosko-specific HM configs moved to `dotfiles/bosko/` (2026-05-15)** — `bosko-claude.nix` and `claude/agents/` relocated from `dotfiles/common/configs/` to `dotfiles/bosko/`. Makes the user-scope of these files explicit; `dotfiles/common/configs/` now contains only configs shared by both users.
- **`nvidia.nix` scoped per-host (2026-05-14)** — Removed from `desktopModules`; each desktop host (gaming, laptop, natalie-laptop) now imports it explicitly. Preparation for the gaming AMD card swap: dropping NVIDIA from gaming will require only a one-line flake change.
- **`rebuild` alias switched to `nh os boot` (2026-05-12)** — Avoids live-switch activation failures. Changes are staged; rebooting applies them.
- **`claude-code` and `gemini-cli` moved to `users.nix` user packages (2026-05-14)** — User-scoped tools belong in `users.users.bosko.packages`, not duplicated across per-host `environment.nix` files.
- **Cleanup retention reduced to 3 builds (2026-05-14)** — 3 generations is sufficient rollback headroom; saves disk space.
- **natty now gets `home.nix` applied (2026-05-14)** — The `home-manager.nix` reorganisation added the missing `home.nix` import to natty's HM block. Previously natty had a HM block with no imports.
- **`lutris` commented out in gaming.nix (2026-05-12)** — `lutris` transitively depends on `openldap-2.6.13` for `i686-linux`; that derivation has no binary cache entry and attempting to build it from source blocks `nh os switch` for hours. Workaround: `# lutris` comment in `gaming.nix`; `faugus-launcher` (already in the list) covers the use-case. Restore once a binary cache entry exists.
- **`audit-rules-nixos.service` disabled (2026-05-11)** — `auditctl` 4.1.2-unstable rejects blank lines in `audit.rules`; nixpkgs hard-codes one before `-e 1`. Disabling the service via `systemd.services.audit-rules-nixos.enable = lib.mkForce false` is cleaner than patching nixpkgs. `auditd` continues running (required for AppArmor). If custom audit rules are needed in future, load them via a separate unit.
- **`natty` restored as non-wheel user (2026-05-11)** — Removed in the 2026-05-06 audit for being in the wheel group. Now back with no elevated privileges (no wheel, no trusted-user) for the Natalie laptop host.
- **`vpn.nix` deleted (2026-05-11)** — File had been commented out in `flake.nix` since April; deleted to reduce dead code. Will be rewritten from scratch when VPN deployment is imminent.
- **`amd.nix` scoped to gaming host only (2026-05-11)** — The laptop has no AMD GPU; `amd.nix` was incorrectly in `desktopModules`. Now imported only in the gaming host's module list.
- **dbus pinned to classic implementation (2026-05-10)** — nixpkgs-unstable rev `4bd9165` silently changed `services.dbus.implementation` default to `"broker"`. Fix: `services.dbus.implementation = lib.mkDefault "dbus";` in `security.nix`. Broker should not be re-enabled until validated against this AppArmor configuration.
- **Full security audit remediation (2026-05-06)** — All Critical, High, and most Medium/Low findings addressed in a single session. Rationale: the gaming host is blocked by an unrelated upstream regression anyway; best to get the config into a hardened state before the next activation.
- **L-6 declined** — User chose not to change `edit = "sudo hx"` alias to `sudoedit`; alias retained as-is.
- **chrony replaces ntpd** — Applied to all three hosts; handles intermittent connectivity better.
- **WireGuard keys deferred** — Placeholder keys are not a real risk until VPN is deployed; generating them without a live peer adds no security value.
- **Stable nixpkgs pin deferred (M-9)** — Requires adding a second nixpkgs input to flake.nix; scoped to a future session.
- **Claude agents backed up via HM** — `bosko-claude.nix` symlinks agent definitions from the repo into `~/.claude/agents/` on rebuild. Confirmed on laptop (switch succeeded 2026-05-10); gaming pending its switch.
- **nix-ld consolidated into gaming.nix** — All gaming-specific config is colocated in one module.

## Known Issues / Tech Debt

- **README.md's "Current Status" paragraph and "Recent Changes" section have grown unbounded** (noticed session 112) — every session appends another clause/entry with no rotation (unlike `session-summary.md`, which has an archive file for exactly this), so the README is now 507 lines/66KB and the two sections largely duplicate each other's content in different levels of detail. Not fixed this session (out of scope for a routine close, and risky to restructure without discussing what to keep) — worth a dedicated pass to trim "Recent Changes" to the last 1-3 sessions per `session-closer`'s own README guidance, and consider whether "Current Status" needs the same per-session appending at all.
- **Jellyfin's "YouTube" library can't show a flat thumbnail grid without restructuring Pinchflat's output** (session 112) — tried and reverted a library collection-type change (`tvshows`→`movies`→back); Jellyfin's folder resolver detects Pinchflat's `channel/dated-folder/video` layout as TV-show-shaped regardless of the library's declared type. A real fix would mean changing Pinchflat's own output folder template (a bigger, separate change, with open questions about what happens to already-downloaded files) — not pursued. Revisit only if the current channel/season/episode browsing genuinely bothers the user enough to want that migration.
- **OpenSubtitles plugin not yet installed** (session 112) — needs a real opensubtitles.com account the agent can't create; Intro Skipper and YouTube-metadata are both already live via the same `claude-automation` API key. See Current Goals.
- **YouTube-metadata plugin's series provider has an open upstream bug (`ankenyr/jellyfin-youtube-metadata-plugin#203`)** that can renumber episodes library-wide during a scan (session 112) — scoped to the dedicated "YouTube" library only, so Movies/Shows shouldn't be touched, but worth a spot-check after future library scans since no one else has reported combining it with Pinchflat specifically.
- **Session 111's skill-ecosystem work (`7b8fd41`..`1f9ddc6`) not yet live in `~/.claude`** — repo-managed global skills + the new `skill-builder` agent + the global `~/.claude/CLAUDE.md`; see Current Goals. Pure prose/agent-definition changes, no kernel/DE impact; the new agent specifically needs the reboot (not just rebuild) to be dispatchable by name.
- **Session 110's absolutist-rules rewrite (`48e856c`) not yet live in `~/.claude`** — repo-managed global skills + the global `~/.claude/CLAUDE.md`; see Current Goals. Pure prose, no reboot needed.
- **`improve-system`'s report-file-verification gotcha (`69ca7f5`, session 109) not yet live in `~/.claude`** — `dotfiles/bosko/claude/skills/improve-system/SKILL.md` needs `nh os boot` + reboot; see Current Goals.
- **Intermittent "no internet connection" app-level errors reported once (2026-09-13), never diagnosed** — session ended right after the report, before any investigation. Next occurrence: check DNS resolution and per-app proxy/network settings first, since `ping 1.1.1.1` succeeding only proves raw ICMP reachability, not DNS or HTTP-layer connectivity.
- ~~**xwayland-satellite 0.8.1 pin (`4201283`, session 105) not yet applied to any host — test not yet run.**~~ **RESOLVED (stale item corrected session 113):** live on gaming since 2026-09-09 and user-confirmed working 2026-09-15; the pin stays until upstream #156 closes and a newer release is confirmed good (re-checked 2026-09-17 and 2026-09-19 — still open). Original text: Real experiment against the Steam dropdown-menu bug (correcting the session 102/104 "no fix worth trying" call — 0.8.2's changelog admits 0.8.1 introduced its own popup regressions). See Current Goals; revert with `git revert 4201283` if it doesn't help.
- **2026-09-20 flake bump (`f978b9c`, session 113) not yet applied to any host** — disko/dms/financeguru/home-manager/nixpkgs/sops-nix moved, verified clean (flake-check + 4-host deep-eval); see Current Goals.
- **2026-09-07 flake bump (`f780dba`, PR #21, session 104) not yet applied to any host** — nixpkgs/home-manager/dms moved, verified clean (flake-check + 4-host deep-eval); see Current Goals. Superseded in practice by the 2026-09-20 bump above — one rollout covers both.
- **PR #20's repo-managed skill fixes (`da25cc2`, session 104) not yet live in `~/.claude`** — `dotfiles/bosko/claude/skills/{repo-creator,search-pkg,session-analysis}` need `nh os boot` + reboot; see Current Goals.
- **btop has no GPU panel — root-caused, not fixed** (session 104) — `shown_boxes` in `~/.config/btop/btop.conf` omits `gpu0`; NVML itself is available. No config change made (session interrupted); revisit if asked again.
- **gaming's Mod+G Deezer stagger fix (`fcb2cc5`, session 102) not yet applied to any host** — shared niri config, `switch` recommended (no reboot needed); see Current Goals.
- ~~Steam dropdown menus (File/Store/Library/Community) flash and disappear under niri on gaming — upstream bug, no fix available~~ (session 102) — **SUPERSEDED (session 105)**: a 0.8.1 pin is now a real experiment, not a dead end — see the entry above and Current Goals.
- **PR #18's repo-managed skill fixes (`dd2f534`, session 101) not yet live in `~/.claude`** — `dotfiles/bosko/claude/skills/{agent-suggestion,dream,improve-memory,refresh-manager-profile,session-analysis,skill-upgrade}` need `nh os boot` + reboot; see Current Goals. The project-local skill/lib/permission changes in the same PR are already live.
- **gaming: hplip drop + flatpak `network-online.target` fix are `nh os boot`-staged but not yet rebooted** (session 100, `ae3b40b`+`cfb8caa`) — confirmed staged via direct store inspection; see Current Goals. laptop/natalie-laptop haven't picked up either fix yet (both touch shared modules).
- **`improve-system`'s new Discord-report Step 5 (`92e6075`, session 100) not yet live in `~/.claude`** — repo-managed/symlinked `SKILL.md`, needs its own `nh os boot` + rebuild (not included in gaming's currently-staged generation).
- ~~2026-09-04 flake bump (`2d11999`) not yet applied to any host~~ (session 99) — **RESOLVED on gaming (session 100)**: the 20:44 reboot picked it up; nixpkgs/home-manager/dms/dank-qml-common/financeguru/sops-nix all live there now. laptop/natalie-laptop still pending.
- ~~`/dream` suite (`256a7cc`) and `send-results`' Artifact-link fix (`0748119`) not yet live in `~/.claude`~~ (session 99) — **RESOLVED on gaming (session 100)**: confirmed via direct symlink resolution (`~/.claude/skills/dream/SKILL.md`, `~/.claude/skills/send-results/SKILL.md` both resolve to real store paths), not assumed. A real end-to-end `/dream` invocation from this live session is still untested. laptop/natalie-laptop still pending.
- ~~`rebuild-boot`/`rebuild-switch` alias split (`dce0913`) and Meta+G launch-set fix (`a5fa6b1`) not yet applied to any host~~ (session 98) — **RESOLVED on gaming (session 100)** via the 20:44 reboot; not individually re-verified (aliases/keybind, low-risk). laptop/natalie-laptop still pending.
- ~~`save-memory`'s PR #16 project-dir fix (`4475e6d`, session 97) not yet live in `~/.claude`~~ (session 100) — **RESOLVED on gaming**: `~/.claude/skills/save-memory/SKILL.md` confirmed resolving to a real store path. laptop/natalie-laptop (and FinanceGuru sessions run from those hosts) still pending.
- ~~`development.nix` (godot-mono + dotnet-sdk) in `desktopModules` not yet applied to any host~~ (session 94) — **RESOLVED on gaming (session 100)**: `dotnet --version` resolves (`8.0.424`); the Godot binary is `godot4-mono`/`godot-mono`, not `godot4` as originally assumed (corrected in Current Goals). laptop/natalie-laptop still pending.
- **2026-08-25 flake bump (`c84892f`) and Tailscale MCP connector (`e8c183a`)** (session 92) — **flake bump resolved on gaming (session 100)** via the 20:44 reboot. The connector's own post-rebuild live-verify (`/run/secrets/tailscale-mcp-env`, a real tool call) is still unconfirmed — reading `/run/secrets/` needs sudo, not checked this session. laptop/natalie-laptop still pending both.
- ~~`save-memory` is a phantom skill — listed in the roster, no backing file found anywhere~~ (session 92) — **RESOLVED (session 95)**: it was project-local (`.claude/skills/save-memory/`) the whole time; session 92 only checked the global paths. Now promoted to a real global skill — see Current Project State.
- **`3230dfd` (research skill memory/volatility feature + CLAUDE.md rule) not yet live in `~/.claude`** (session 88) — repo-managed/symlinked, needs `nh os boot` + reboot.
- **2026-08-21 flake bump (`041a0d1`, session 88) not yet applied to any host** — nixpkgs/home-manager/dms all moved; committed+pushed lock-only per `/flake-update-verify`'s scope, verified via flake-check + 4-host deep-eval. `/fleet-rollout` is the way to apply.
- ~~laptop: bluetooth ISO-socket fix (`fc756c3`) and shared-folder Tailscale-IP fix (`693d573`)~~ (session 86) — **RESOLVED (session 89):** laptop switched; CIFS mount-timeout portion (below) directly re-verified live, bluetooth/Tailscale-IP inferred live from the same switch.
- **`modules/shared-folder-client.nix`'s original CIFS-stall fix (`693d573`/session 86) was a silent no-op — `x-systemd.mounttimeout` (no hyphen) isn't a real fstab option; the real keyword is `x-systemd.mount-timeout`** (session 89, `86ad36e`→`24a1d2d`) — fstab drops unrecognized `x-systemd.*` options without erroring, so the 10→2s timeout change never took effect despite switching and dry-run passing; `systemctl show srv-shared.mount` was the tell (`TimeoutUSec` stayed at systemd's 90s default). Fixed and **confirmed live on laptop** (now reads `2s`, real stall now ~2s not ~10s). **natalie-laptop still needs its own switch** to pick this up (shares the module) — see Current Goals. Still only shrinks the stall, doesn't eliminate it (see Current Project State).
- **Two Android phones (`natalies-s23-ultra`, `pixel-6`) are now on the tailnet with no flake-managed DNS fallback** (noticed session 84) — they get pi-hole DNS solely through the new Tailscale-admin-console global nameserver; unlike gaming/laptop/natalie-laptop they have no `1.1.1.1` fallback baked in, so if pi-hole ever goes down they lose DNS entirely rather than degrading gracefully. Not fixed — just worth knowing before chasing a "phone has no internet" report back to the wrong cause.
- **vpn-server (Oracle Cloud) is down, Oracle-side, no ETA** (session 81) — administratively disabled by Oracle (`409 IncorrectState` on every instance action) plus a concurrent Always-Free A1 quota cut (4/24 → 2/12 OCPU/GB); not fixable via `oci-cli` or from this repo. Support ticket filed (text in memory `project_vpn_server_oracle_disabled`), no automated watcher — user is self-monitoring. Don't re-diagnose from scratch or re-propose a watchdog; wg0 will reconnect on its own once the instance is back (see Current Goals for the restore steps).
- **wg0/`vpn.nix` is out of the flake, Tailscale mesh is the interim replacement — but doesn't reproduce full-tunnel egress-IP masking** (session 81, `5b1ad16`/`23e4fbd`/`34b9e72`) — Pinchflat's download bot-detection bypass may be broken again as a result (unverified, see Current Goals). Restore wg0 once vpn-server is back rather than trying to extend Tailscale to cover this gap.
- ~~laptop/natalie-laptop still need their own `nh os boot`/`switch` + `sudo tailscale up`~~ (session 81, `34b9e72`) — **RESOLVED (session 82)**: both hosts rebuilt and authenticated; all 5 devices confirmed live on the mesh. See Current Project State.
- **laptop/natalie-laptop still need `nh os switch` for the pi-hole-over-Tailscale DNS change** (session 82, `30c73ee`) — only gaming has applied it; verified clean via dry-run + 4-host deep-eval, safe to apply whenever the user gets to it.
- **pi-hole's `dns.listeningMode` is now `ALL` (was `LOCAL`)** (session 82) — a real posture change on a live, non-flake-tracked appliance: pi-hole could technically answer queries from any origin now, not just local-subnet ones. Judged safe (no port-forward exists, Tailscale's own firewall chain still gates non-tailnet traffic) but worth knowing if pi-hole's exposure is ever revisited.
- **`4feaa11` and `443fb9a`'s repo-creator asset extraction: very likely live on gaming, not individually re-verified** (session 80/81) — see Current Goals for the inference chain; worth a direct file check next time either is touched.
- ~~laptop and natalie-laptop: `services.upower.enable` fix (`ae6e428`, session 77) not yet applied~~ — **no longer tracked (session 78):** per memory `project_dms_battery_upower_fix`, self-managed by the user across all 3 hosts; don't re-flag unless a fresh symptom shows up.
- **`financeguru` bump (`f17aeab`, session 76) not yet applied to any host** — dry-run-verified clean (+92.8 KiB, no kernel/DM churn, `switch` recommended); nobody has switched onto it yet.
- **2026-08-14 flake bump (`4ea763c`, session 73) not yet applied to any host** — nixpkgs/home-manager/dms/sops-nix all moved; committed+pushed lock-only per `/flake-update-verify`'s scope, verified via flake-check + 4-host deep-eval. `/fleet-rollout` is the way to actually apply it.
- **niri idle-lock timeout bump (5→10 min, `e375565`, session 72) not yet applied** — `nh os switch` needed on gaming/laptop/natalie-laptop to restart the `swayidle` user service with the new 600s timeout; dry-run confirmed safe (`switch`, no reboot needed).
- **2026-08-10 catch-up commits (skills) not yet live in `~/.claude`** — `resume-session` skill (`1233737`), the slash-command transcript-detection fix (`910c986`), and the improve-system PR #8 content fix (`504e9f6`) are all `dotfiles/bosko/claude/skills/` repo-managed/global; need `nh os boot` + reboot to reach the live `~/.claude/skills/` symlinks. Project-local skills are unaffected.
- ~~8 skill-audit fixes not yet live in `~/.claude`~~ (session 70, commits `02eb5e1`/`d130dce`/`2d4610c`/`b39a1b1`/`87561de`) — **RESOLVED (session 71):** gaming rebooted + switched multiple times this session; confirmed live via direct symlink resolution + `git log` on the underlying source (not assumed from reboot alone).
- ~~VPN health-watchdog timer + today's flake bump both pending a gaming `nh os switch`~~ (session 69 `1a4d440`, session 70 `e6b8367`) — **RESOLVED (session 71):** confirmed live via `systemctl list-timers` (`vpn-health-check.timer` active, next trigger on schedule).
- **pi-hole ad-blocking is bypassed by design whenever gaming's VPN tunnel is active** (discovered live during session 69's close, not a regression from any recent commit) — `modules/vpn.nix:33`'s `dns = [ "1.1.1.1" "8.8.8.8" ]` makes `wg-quick` overwrite `/etc/resolv.conf` for the whole time the tunnel is up, taking precedence over `networking.nameservers`' pi-hole entry regardless of whether that entry is correct. This is separate from (and persists after) the `ac1c4ad` outage-drift fix — that fix only matters when the VPN is down. Not treated as a bug to fix; flagging so a future "pi-hole isn't blocking anything" report checks tunnel state first.
- ~~laptop + natalie-laptop still need `nh os boot`/`switch` + reboot~~ for the pi-hole/famdash DNS fix (`ac1c4ad`, 2026-08-05) and the niri `playerctld` media-key fix (`8feeb46`, 2026-08-06) — **RESOLVED (session 71):** both hosts fully rebuilt+rebooted multiple times this session; `playerctld` confirmed running on all 3 hosts.
- **GTK theme selection is now fully declarative via Home Manager's `gtk` module** (session 71, `944b64b`/`835a078`) — superseded the manual `dconf.settings` + hand-edited `settings.ini` pattern documented in several entries below; those entries are now historical only, don't reuse that pattern for a future theme change.
- **Neither pi-hole nor famdash has a router-side DHCP reservation** (session 69, `ac1c4ad`) — Xfinity gateway has no admin access for one, so the IP drift that just got fixed can and will recur on the next power outage. Revisit if a custom router (see the NixOS-as-router decision above) ever replaces the Xfinity gateway.
- **Pinchflat's Yes Theory source is stuck as `collection_type: playlist` instead of `channel`** (session 67) — confirmed matching a real, still-open upstream bug (`kieraneglin/pinchflat#607`); re-adding with both the handle URL and the explicit channel-ID URL didn't fix it. Accepted as-is — only known cost is missing series-level banner/fanart art in Jellyfin, since the underlying channel ID resolved correctly and indexing/downloads/NFO are unaffected. Don't re-chase this without new information (e.g. an upstream fix landing).
- ~~No Pinchflat video has downloaded yet~~ (session 67) — **RESOLVED (session 68)**: root cause was the full-tunnel VPN blocking all downloads via bot detection on the Oracle Cloud egress IP, not a Pinchflat/source config issue; fixed via UID-scoped ip rule + `checkReversePath = "loose"` (see Current Project State). 3 videos confirmed downloaded successfully. Yes Theory's own `download_cutoff_date` (2026-07-27, after their last upload) still means nothing new downloads from *that specific source* until they post again — that part is unchanged/intentional, not a bug. A second source, `@66Samus`, was found mid-indexing (added outside this conversation) — no downloads confirmed from it yet.
- ~~Pinchflat's Jellyfin-side setup is incomplete~~ (session 67) — **RESOLVED (2026-08-03, session 68b)**: the "YouTube" library was created in Jellyfin and a manual rescan surfaced the downloaded videos on the client. See memory `project_pinchflat_jellyfin`.
- ~~2026-07-30 skill-audit global-skill fixes not yet live in `~/.claude`~~ (session 66) — **RESOLVED (session 67, confirmed live via direct file check, not assumed):** gaming's 2026-08-03 reboot+switch activated these; `claude-rules`/`session-closer`'s gotcha additions are present in the live symlinked files.
- ~~laptop still needs `nh os switch`/reboot to pick up the docx/odt mimetype fix and the CIFS hostname fix~~ (session 66) — **gaming's side confirmed resolved (session 67)**: `xdg-mime query default` for docx now correctly returns `onlyoffice-desktopeditors.desktop` on gaming. **laptop is still pending** — not addressed this session.
- **natalie-laptop: `mock-interview-questions.docx`'s underlying stuck-SMB3-lease cause was never root-caused, only worked around** (session 66) — see Current Project State for the full investigation; if another file on `/srv/shared` starts throwing a persistent `EACCES` that survives a fresh `smbclient` session and an `smbd` restart, the same fresh-inode workaround applies, but the actual Samba-internals cause (a durable-handle/lease surviving a crashed client session) is still unknown.
- ~~gaming: `amd_pstate=disable` kernel param staged, not yet rebuilt/rebooted~~ (session 61, `a1493d3`) — **RESOLVED (session 67):** confirmed active via `/proc/cmdline` after today's reboot.
- ~~`boot-error-triage` and the `git-commit`/`git-push` Gotcha docs are global repo-managed skills not yet live in `~/.claude`~~ (session 61) — **RESOLVED (session 67):** same reboot+switch as above brought all pending global-skill fixes live together.
- **natalie-laptop: SSH host key changed, mismatch not yet diagnosed** (session 60, 2026-07-27) — connecting from gaming (`ssh bosko@natalie-laptop`) failed with `Host key verification failed` against a key not in `known_hosts`. Not investigated further since the session was already local on natalie-laptop, making the SSH path unnecessary at the time. Before next remote SSH to this host: check whether the host's `/etc/ssh/ssh_host_ed25519_key` was regenerated (e.g. by a reinstall or a NixOS generation that touched host keys) versus something worth treating as suspicious (IP reuse by another device, MITM) — don't `ssh-keygen -R` and reconnect without ruling that out first.
- ~~natalie-laptop: printer + VPN fixes committed, awaiting the user's own rebuild+reboot~~ (session 58) — **RESOLVED (session 59, 2026-07-27):** both confirmed live — `wg0` no longer autostarts (manual `vpn-on` required and works), and `/etc/cups/cups-browsed.conf` correctly contains `CreateIPPPrinterQueues Everywhere` on disk. The RTC/clock-desync issue remains a confirmed hardware fault (dying CMOS battery) with no config fix.
- ~~natalie-laptop: `cups-browsed`'s `BindsTo=avahi-daemon.service` coupling can still stall permanent-queue creation after any avahi restart~~ (session 59, 2026-07-27) — **PARTIALLY RESOLVED (session 71, `841eb05`):** `modules/printing.nix` now has a `cups-browsed-fixup` oneshot unit that retries `systemctl try-restart cups-browsed` once `network-online.target` is reached, fixing the specific boot-time trigger (avahi reporting active on `lo` before the real interface joins). **New distinct trigger found (2026-08-15, gaming)**: the same "discovered via avahi but no CUPS queue" symptom recurs when the *printer itself* (not the network) isn't up yet when `cups-browsed` does its browse-and-create pass — e.g. PC boots, `cups-browsed-fixup` fires once network is online, but the printer is powered on *after* that. `cups-browsed-fixup` only runs once early in boot, so it doesn't help here; confirmed via journal that `cups-browsed` had zero activity between its boot-time start and the manual fix. Live fix is still `sudo systemctl restart cups-browsed` (verified via `lpstat -p -d`) — user evaluated a periodic-timer mitigation and declined it as overkill for how rarely this triggers (see Recent Decisions). No further repo fix planned; this is an accepted, understood gap in `cups-browsed`'s discovery model, not a config bug.
- ~~natalie-laptop: Kate print-dialog discrepancy unresolved~~ (session 58) — **RESOLVED (session 59, 2026-07-27):** not a stale memory — Kate's system CUPS client does live DNS-SD printer discovery and can show a network IPP printer before any permanent queue exists; OnlyOffice's bundled print dialog (running in its own `buildFHSEnv` sandbox) only ever sees printers that are already real, permanent CUPS destinations. See Current Project State for the full root cause and fix (`cups-browsed` restart).
- **natalie-laptop: natty DMS fix not yet activated or live-verified** (session 42) — `rebuild` + reboot needed to pick up `4dd1759`/`aa8b0c9`. User is doing this rebuild themselves; once done, log in as `natty` under niri and confirm DMS actually starts (was completely absent before this fix). The Plasma-login half of this check is now moot — session 50 removed `plasma.nix` from natalie-laptop entirely (niri-only "for now"), so there's no Plasma session left to test the DMS/swayidle/kdeglobals gating against. Not yet observed live by either Claude or (as of session close) the user.
- **TEMPORARY: `rtk` package unavailable on `vpn-server`, PLUS a separate `doCheck` waiver now needed on the hosts that do get it** (session 50, updated 2026-07-24/26) — `modules/users.nix`/`modules/claude-code.nix` guard the package and its PreToolUse hook on `pkgs ? rtk` because `rtk` only exists in nixpkgs-unstable, not the `nixpkgs-25.11` stable channel `vpn-server` pins. Eval is fixed (all 4 hosts pass `deep-eval-check`), but `vpn-server` itself doesn't actually get the `rtk` binary or its Claude Code token-optimization hook until a future 25.11 point release backports the package — no action needed, the guard self-heals. **Separately (2026-07-24, commit `839cbc0`)**: once `pkgs ? rtk` started evaluating true on the unstable hosts, rtk-0.43.0's own `cargo test` checkPhase started failing under `-D warnings` (dead-code lints), breaking `nh os boot`'s real build — patched with `pkgs.rtk.overrideAttrs (_: { doCheck = false; })` in `modules/users.nix`. Reconfirmed still both required 2026-07-26 via a real `nix build` of the unmodified package. **On each future nixpkgs bump, check both**: whether `rtk` resolves on stable now (removes the `pkgs ? rtk` guard's practical relevance for vpn-server), and whether `nh os boot` still needs `doCheck = false` (remove once nixpkgs ships an rtk revision whose tests pass cleanly).
- **New desktop hosts need `secrets/hosts/<hostname>.yaml` before their first eval** (session 37) — a consequence of `vpn.nix` living in `desktopModules`: its `sops.secrets."wg-private-key".sopsFile` path must exist at eval time even before the host joins the VPN. The `new-host` skill documents the flow (generate wg keypair, encrypt to admin key at scaffold time; add the host's age key post-first-boot). Not an issue for any existing host.
- ~~Session 38's repo-managed skill fixes not yet live in `~/.claude`~~ — **RESOLVED (session 39):** user ran `nh os boot`; confirmed live in the same running session (no restart needed — see Recent Decisions). `ask-team`, `search-pkg`, `skill-audit`, `interview`, `new-team-member`, `skill-suggestion` now resolve to the fixed store paths.
- **`qt6ct.conf` Home Manager file not yet activated on laptop** (session 35) — **gaming is done** (confirmed live through session 48's many activations); laptop still needs `nh os boot` + reboot.
- **DMS's `qtThemingEnabled` toggle must be flipped by hand on any fresh niri install** (session 35) — intentionally left unmanaged (same reasoning as `settings.json` generally). On a from-scratch install or a wiped `~/.config`, Qt apps will stay light-themed until this is toggled once via DMS Settings → Theme & Colors → "Apply Qt Themes" (or `qtThemingEnabled: true` edited directly in `~/.config/DankMaterialShell/settings.json`), then a theme-mode toggle (`dms ipc call theme light`/`dark`) to force DMS to re-apply.
- **Niri `config.kdl` Home Manager symlink not yet activated on laptop** (session 34) — **gaming is done**; `nh os boot` + reboot on laptop is required before the dotfiles-managed keybinds/gaps/waybar-removal take effect there.
- ~~USB drives not auto-mounting under Thunar on gaming — needs a session restart, not a rebuild~~ (session 48) — **RESOLVED (session 67):** today's reboot gave `dbus-broker.service` a fresh start (`ActiveEnterTimestamp` matches boot time); `gio mount -l` now correctly lists both drives via `GProxyVolumeMonitorUDisks2`.
- **`systemd.services.apparmor.reloadIfChanged = false` workaround in `security.nix`** (session 48) — real nixpkgs packaging bug in `apparmor-parser-5.0.0`: `apparmor-utils`' `aa-remove-unknown` (the service's `ExecReload`) hardcodes a path to `apparmor-parser`'s `lib/apparmor/rc.apparmor.functions`, which this build doesn't ship, so any activation needing to reload AppArmor policies failed outright ("Failed to reload apparmor.service"), blocking `nh os boot` entirely. AppArmor enforcement itself is unaffected. **On each future nixpkgs bump, check whether `apparmor-parser` ships that file again** (`find /nix/store/*apparmor-parser*/lib/apparmor/rc.apparmor.functions`) and remove the workaround once it's fixed upstream — same removal-condition pattern as the electron insecure-package waiver above.
- **DMS may silently fail to auto-inject future `include` lines into `config.kdl`** (session 34) — only `dms/outputs.kdl` is currently included; other DMS-generated files (e.g. `colors.kdl`) are written expecting DMS to add their own `include` line when a matching settings-UI toggle is enabled. Since `config.kdl` is now a read-only Nix symlink, that auto-injection would fail quietly. If a future DMS integration toggle doesn't seem to take effect, check for this and add the `include` line by hand to `dotfiles/common/configs/niri-config.kdl`.
- ~~TEMPORARY `permittedInsecurePackages` waiver for `pnpm-10.29.2` in `nix.nix`~~ (session 29) — **RESOLVED (session 43):** the pending nixpkgs bump fixed vesktop upstream (now builds against generic `pnpm_10`); waiver removed in the same commit that added the electron waiver below. Do not re-add.
- ~~TEMPORARY `permittedInsecurePackages` waiver for `electron-40.10.5` in `nix.nix`~~ (session 43) — **RESOLVED (2026-07-24, commit `839cbc0`):** nixpkgs now ships vesktop on `electron_43` with no known vulnerabilities; waiver removed. Do not re-add.
- ~~`nixpkgs` temporarily pinned back to `567a49d1` (zen kernel breakage)~~ — **RESOLVED (session 29):** upstream ships `bzImage` again in linux-zen 7.1.2; pin lifted by the 2026-07-02 `/flake-update-verify` run (`48c11d3`), verified by building the kernel from cache and checking `bzImage` exists.
- **Avahi mDNS `openFirewall = true` in printing.nix** — `modules/printing.nix:12` exposes UDP 5353 on all interfaces (not just the LAN interface). Closing it would break mDNS-based printer discovery. Proper fix: interface-scoped firewall rules (e.g., `networking.firewall.interfaces.<lan-if>.allowedUDPPorts = [ 5353 ]`). Deferred from 2026-06-03 security audit.
- **`wg0 mtu = 1380` pending on desktop clients** — Committed in shared `vpn.nix` (`eb69c43`) but only applies on each client's next `rebuild`. laptop is currently back at the default 1420 (the live `ip link` fix was lost on wg toggle) — works for normal use, but large transfers through the tunnel can truncate until rebuilt.
- **laptop and natalie-laptop pending multiple rebuild activations** — Several changes require `rebuild` + reboot: managed Claude Code policy, FinanceGuru package, package consolidation, the `wg0 mtu = 1380` clamp, and (session 28) the OnlyOffice font fix — both hosts also have `onlyoffice-desktopeditors` installed and share the same `fonts.nix` overlay, but only gaming has actually switched to pick it up. (The "DE switch to Plasma" item this bullet used to list is gone — natalie-laptop briefly dual-booted niri+Plasma from session 42, then Plasma was removed again in session 50; it's niri-only now.)
- ~~Stale `ssh natalie-laptop` alias on all desktop hosts until each rebuilds~~ (session 30, `10.0.0.103`→`.101`) — **superseded (session 87)**: `natalie-laptop`'s alias no longer points at any LAN IP at all, it now dials its Tailscale IP (`100.116.208.93`), which doesn't drift on DHCP re-lease. Same fix applied to `laptop`/`gaming`'s aliases. New pending item: all 3 hosts still need their own rebuild to pick up *that* change — see Current Goals, don't re-flag the old `.103`/`.101` staleness.
- **dbus-broker journal warnings are benign** — After the dbus-broker transition, three warning classes appear in journald: duplicate dbus service name entries (stricter parser), `Invalid group-name 'netdev'` in avahi-dbus.conf, `Invalid user-name 'systemd-timesync'` in timesync1.conf. All confirmed harmless; services function normally.
- **`server` host has no hardware** — Removed placeholder from flake; will be re-added via `/new-host` when physical hardware is available.
- **`omarchy.nix`'s dolphin has no Qt/KDE theming glue** (session 40) — unlike niri/hyprland's qt6ct+kdeglobals setup, which points at DMS's matugen-generated color file, this module doesn't run DMS at all, so there's no color source to point qt6ct at. Dolphin will render in Qt's default light Breeze style against an otherwise Tokyo-Night-themed desktop. Deliberately left out of session 40's scope (theme+keybind philosophy only); a standalone static Tokyo Night qt6ct.conf (not DMS-coupled) would fix it if ever prioritized.
- **`omarchy.nix` not wired into any host** (session 40) — same status as the other 9 unused DE modules (mate, cinnamon, xfce, cosmic, wayfire, budgie, gnome, pantheon, plasma-alt); verified only via `lib.deSmoke.omarchy` deep-eval, never booted live. Picked up automatically by the weekly `de-smoke` CI job so it won't rot silently.
- **`ship-skill`'s full internal chain (new-skill → smoke-test → git-commit → push-pause → git-push) is untested end-to-end** (session 39) — the one live `/loop /ship-skill` run this session hit "nothing found" at Step 1 (`skill-suggestion`) and stopped before ever reaching Steps 2-6, so those handoffs are unverified in practice (each sub-skill works standalone; the orchestration wiring between them doesn't yet have a real run). Next time a genuine new-skill idea comes up, invoke `ship-skill` directly (not `new-skill` by hand) to prove the full chain, including whether its internal `git-commit`/`git-push` handoffs behave as documented.

## Next Steps

**Session 113 next steps** (see Current Goals for full detail):
1. **All 3 desktop hosts: rebuild (`boot` recommended)** to pick up the 2026-09-20 flake bump — `/fleet-rollout` applies it together with the older 2026-09-07 bump and the skill-ecosystem backlog.
2. Nothing pending on the pi-hole list change (live, 29 lists, gravity rebuilt) or the xwayland-satellite pin (keep; re-run `pinned-package-status-check` when #156 shows activity or a release newer than 0.8.2 appears).

**Session 112 next steps** (see Current Goals for full detail):
1. **Install OpenSubtitles once the user has opensubtitles.com credentials** — everything else from the Jellyfin plugin research is done (Intro Skipper + YouTube-metadata live).
2. No rebuild/reboot needed for anything from this session — the only repo change (`jellyfin-api-key` secret + `add-secret` skill fix, `fcae72c`) isn't wired into any NixOS module and the `add-secret` fix is project-local (already live).

**Session 111 next steps** (see Current Goals for full detail):
1. **All 3 desktop hosts: rebuild+reboot** to bring the skill-ecosystem work live in `~/.claude`, including the new `skill-builder` agent (needs the reboot to be dispatchable by name) — can ride along with the existing backlog.
2. No code/config changes otherwise — pure Claude-ecosystem process work across 4 commits (creative-example audit, `references/`-routing refactor + Nix-wiring-gap fix, closing that gap for future skills, a twice-run `/improve-system` sweep).

**Session 110 next steps** (see Current Goals for full detail):
1. **Any host: rebuild (no reboot needed)** to bring the absolutist-rules rewrite live in `~/.claude` — can ride along with the existing backlog.
2. No code/config changes otherwise — pure skills/CLAUDE.md wording audit. xwayland-satellite pin re-confirmed holding (no dropdown-bug recurrence since the 09-09 switch); no action needed until upstream #156 closes.

**Session 109 next steps** (see Current Goals for full detail):
1. **All 3 desktop hosts: rebuild+reboot** to bring `improve-system`'s new report-file-verification gotcha live in `~/.claude` — can ride along with the existing rebuild backlog.
2. No config/host changes otherwise — pure skills/process session (PR #23 review+merge, a send-results root-cause, a combined skill-suggestion/skill-upgrade pass).

**Session 108 next steps** (see Current Goals for full detail):
1. No new host action added — the `printing.nix` move rides along with the existing rebuild backlog (system-config-printer, the 2026-09-07 flake bump, PR #20/#21 skill fixes, the xwayland-satellite pin).
2. If the "no internet connection" symptom recurs, diagnose it fresh (see Known Issues) — nothing was actually investigated the one time it was raised.

**Session 107 next steps** (see Current Goals for full detail):
1. **natalie-laptop: rebuild (boot)** to drop the VirtualBox host module — no functional loss expected, it was still pending its first-ever build.

**Session 106 next steps** (see Current Goals for full detail):
1. **All 3 desktop hosts: rebuild (switch)** to pick up `system-config-printer`, then launch it and confirm it shows the print queue.

**Session 105 next steps** (see Current Goals for full detail):
1. ~~**gaming: rebuild (switch)** to apply the Mod+G Deezer stagger fix AND the xwayland-satellite 0.8.1 pin, then relaunch Steam to test whether the dropdown menus behave. Revert (`git revert 4201283`) if not.~~ **Done (see session 110/113):** gaming gen 414 has both; dropdown bug user-confirmed fixed 2026-09-15.
2. No action pending on the kitty change — it was reverted before ever being applied to any host.
3. No action pending on Secure Boot — informational answer only; revisit once the specific game's anti-cheat requirement is confirmed.

**Session 104 next steps** (see Current Goals for full detail):
1. **All 3 desktop hosts: rebuild** to pick up the 2026-09-07 flake bump (PR #21) and PR #20's skill fixes — can ride along with every other pending switch below.
2. No repo changes this session otherwise — pure review/merge + one interrupted, unfixed investigation (btop GPU panel).

**Session 103 next steps** (see Current Goals for full detail):
1. **gaming: rebuild (switch)** to apply the kitty full-screen fix, then `/wayland-screenshot kitty` to confirm it opens full screen rather than half.

**Session 102 next steps** (see Current Goals for full detail):
1. **gaming: rebuild (switch)** to apply the Mod+G Deezer stagger fix, then confirm Deezer opens alongside the other 4 apps on a real Mod+G press.
2. Steam dropdown-menu bug — no fix pending, check back on `xwayland-satellite#156` manually next time it's touched.

**Session 101 next steps** (see Current Goals for full detail):
1. **All 3 desktop hosts: rebuild+reboot** to bring PR #18's repo-managed skill fixes live in `~/.claude` (can ride along with every other pending switch).
2. No code/config work pending from this session otherwise — it was a skills/process close-out only.

**Session 100 next steps** (see Current Goals for full detail):
1. **gaming: reboot** to activate the staged hplip drop + flatpak ordering fix.
2. **gaming: one more `nh os boot`** (before or after that reboot) to pick up `improve-system`'s new Discord Step 5, since it was committed after the currently-staged generation.
3. **laptop/natalie-laptop: rebuild+reboot** — still carry the entire session-92-through-99 backlog that only gaming has picked up so far.

~~0000000a. Run `nh os boot` + reboot on gaming to bring the 2026-08-09 skill-audit fixes + flake bump + VPN watchdog timer live~~ (session 70) — **RESOLVED (session 71)**, see Known Issues.
~~000000. Run `nh os switch` to bring the 2026-07-30 skill-audit fixes live~~ (session 66) — **RESOLVED (session 67)**, see Known Issues; gaming's 2026-08-03 reboot+switch activated these.
~~0000000. laptop: `nh os switch` to pick up the docx/odt mimetype fix and the CIFS bare-hostname fix~~ (session 66, `7bedfb3`/`baad313`) — **RESOLVED (session 71)**: laptop fully rebuilt/rebooted multiple times this session, long after these commits.

> **Stale-content note (session 50):** most numbered items below overlap with, and are superseded by, the "Current Goals → Short-term" section above, which is kept current each close. This list spans sessions 27-45 and hasn't had a dedicated pruning pass; items are left as historical record rather than re-verified one by one (no first-hand ground truth for the oldest ones). Item 000 below was corrected this session because it was actively, verifiably wrong (referenced a Plasma switch that session 50 reversed) — treat any other item describing a host's DE/config state as suspect until cross-checked against "Current Goals" or "Known Issues" above.

00000. **All 3 desktop hosts: rebuild + reboot to activate the `/srv/shared` Samba share (session 63)** — `rebuild` on **gaming first** (it's the server; laptop/natalie-laptop's CIFS mounts have nothing to reach until it's up), then laptop and natalie-laptop (user's own call, sudo-gated). After gaming reboots, confirm `smbstatus`/`systemctl status samba-smbd` is active and `/srv/shared` exists locally. After each client reboots, confirm `~/Shared` appears as a symlink in Thunar for both `bosko` and `natty`, that opening it triggers the CIFS automount (`systemctl status srv-shared.automount`), and that both users can read/write files under it from either machine.
0000. **gaming + laptop: rebuild + reboot to pick up sessions 43-45's niri keybinds/window rules, the kdeglobals fix, the flake bumps, and session 45's `open-maximized` rules** — after boot, verify Mod+G launches the full 7-app setup (kitty/steam/lutris/vesktop/zen/deezer/qbittorrent), Mod+E opens dolphin, gaming's per-monitor window rules spread all 7 apps correctly across the two outputs (asus-1/2/3 = kitty/steam/lutris, dell-1/2/3/4 = vesktop/zen/deezer/qbittorrent), dolphin renders dark-themed without needing an interactive switch first, and Kitty/Vesktop/Zen Browser/Deezer/qBittorrent all launch already-maximized (session 45). qBittorrent's autostart-vs-Mod+G race fix is separate and already live — no reboot needed for that part. Can ride along with the other pending lock-bump reboots below.
000. **natalie-laptop: rebuild + reboot to activate the natty DMS fix (session 42, `4dd1759`/`aa8b0c9`) and session 50's Plasma removal** — user is doing this themselves. After boot: pick niri from SDDM as `natty` and confirm DMS actually starts (previously totally absent for that user); pick niri as `bosko` and confirm no regression. **Corrected (session 50):** the original version of this item said to also test a Plasma login — no longer applicable, `plasma.nix` was removed from this host's flake entry (niri-only now). This closes the loop on a bug reported live in session 42.
00. ~~Rebuild + reboot + start a new Claude Code session to pick up session 38's skill fixes~~ — **DONE (session 39):** rebuilt; confirmed live without needing a new session. Still worth spot-checking that `interview` reads its `assets/brief-template.md`, `ask-team` resolves the shared knowledge-base path, and `search-pkg`'s offline fallback actually returns results, next time one of those is used for real.
0a. **gaming + laptop: rebuild + reboot to activate the Niri `config.kdl` Home Manager symlink AND the new `qt6ct.conf` (session 34 `b9913b5`..`bf2ba35`, session 35 `b3bbb47`)** — can ride along with the other pending lock-bump reboots below. After boot, verify `Mod+Return` opens kitty, `Mod+B` opens the browser, gaps render at 8px, DMS's settings UI can still write its own theme/layout files, and qBittorrent (or any Qt app) is dark-themed after a restart. On laptop specifically, since it's a fresh activation there, also confirm DMS's `qtThemingEnabled` is `true` in `~/.config/DankMaterialShell/settings.json` — flip it and run `dms ipc call theme light && dms ipc call theme dark` if not.
0. **All hosts: activate the accumulated lock bumps** (`48c11d3` + waiver `aa3d690` from session 29, stacked with session 31's and now session 32's further nixpkgs/home-manager/dms/financeguru bumps) — first rebuild per host pulls linux-zen 7.1.2 + several weeks of unstable (large download; zen kernel change ⇒ reboot required on desktops). `/fleet-rollout` is the structured path, or ride it along with the per-host rebuilds below. **At the next nixpkgs bump, test removing the pnpm-10.29.2 waiver** from `nix.nix` (see Known Issues).
0b. **gaming: rebuild + reboot to activate the Niri DE switch (session 33, `34a7dbe`)** — this rides along with item 0's stacked lock bumps, so it's one reboot either way. After boot, test the actual gaming use case: Steam/Proton launch under Niri, controller input, cursor rendering (the `GBM_BACKEND`/`__GLX_VENDOR_LIBRARY_NAME`/`LIBVA_DRIVER_NAME` vars just added should prevent software-render fallback), and gamescope. If broken, the previous Plasma generation is still selectable at the boot menu — no dual-DE fallback exists by design.
1. **Rebuild gaming to surface pending global skill changes**, now also including the session-29 gotchas to the repo-managed `interview` and `skill-audit` skills, including the session-28 `/improve-system` skill-audit fixes (hardened `enumerate-skills.sh`, the co-author-line fix in `git-commit`/`session-closer`/`repo-creator`, the `git-commit`/`git-push` behavioral-drift fix, the `new-skill` AskUserQuestion fix, and the team-member-family path cross-references), the new `research` skill (session 27), and the session-18/20/21/23 skill edits — `nh os boot /home/bosko/NixOS` + reboot makes the `dotfiles/`-based skill edits live in `~/.claude/skills/`. The same gaming rebuild also drops the `brobot` packages (`yt-dlp` + closure, −187 MiB). All project-local `.claude/skills/` changes (including all of session 28's `nixos-gc`/`ssh-host`/`switch-de`/`new-peer`/`bump-input`/`pin-input`/`new-host`/`audit-config`/`save-memory` fixes) are already live. (Can ride along with the MTU/VPN rebuild below.)
2. **Rebuild desktop clients to apply `wg0 mtu = 1380`** — `rebuild` on laptop (user is doing this next), gaming, and natalie-laptop to make the MTU clamp (`eb69c43`) permanent; laptop is currently back at 1420 after the wg toggle. Use `vpn-on`/`systemctl restart wg-quick-wg0` if a tunnel goes stale.
3. **gaming: rebuild + reboot** — applies the `::/0` drop (`2f67083`) and the session-10 IPv6 disable. Afterward confirm `systemctl status wg-quick-wg0` is **active** and the WireGuard handshake is live (`/vpn-status`), then re-check Jellyfin artwork.
4. **Apply rebuild + reboot (or `nh os switch`) on laptop**: activate managed Claude Code policy, package consolidation, the OnlyOffice font fix (session 28), and other pending changes. Remove old hand-maintained `~/.ssh/config` afterward.
5. **Apply rebuild + reboot (or `nh os switch`) on natalie-laptop**: activate managed Claude Code policy, FinanceGuru package, the OnlyOffice font fix (session 28), session 50's Plasma removal (niri-only now), and other pending changes. Remove old `~/.ssh/config` afterward.
6. **Interface-scope Avahi mDNS firewall**: edit `modules/printing.nix` to replace `services.avahi.openFirewall = true` with interface-scoped rules restricting UDP 5353 to the LAN interface only.
7. **AMD card swap on gaming**: when the physical card arrives, remove `nvidia.nix` from gaming's module list in `flake.nix`; run `rebuild` in terminal and reboot; verify `amd.nix` is sufficient on its own.
8. **Re-add `server` host when hardware arrives**: use `/new-host` skill to scaffold; pin to `nixpkgs-stable` (`nixos-25.05`) using the established pattern from vpn-server.
9. ~~Optional, router UI (not repo): add DHCP reservations for the desktop hosts~~ (session 30) — **moot for `ssh.nix`/`hosts.json` specifically (session 87)**: those now key off Tailscale IPs, not LAN DHCP addresses, so lease-table churn can no longer invalidate them. A router-side reservation could still be worth doing for other reasons (e.g. LAN-local troubleshooting), but it's no longer solving a live bug.
10. **Optional: wire `omarchy.nix` into a host to actually see it** (session 41) — it's never been booted live, only `lib.deSmoke`-verified. If ever prioritized, use `switch-de` on laptop, then visually verify with `wayland-screenshot` — the theme-switching pipeline, mako notifications, and new keybinds have only been checked for eval correctness, not runtime behavior.
