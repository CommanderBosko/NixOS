# NixOS Configuration

Bosko's single-flake NixOS configuration for four hosts. Shared system modules live under `modules/`; Home Manager configs live under `dotfiles/` (`common/` shared by both users, `bosko/` user-specific); host-specific files (hardware config, environment, networking) live under `hosts/<hostname>/`.

## Current Status

Active development, state version `25.11`. Four active hosts (gaming, laptop, natalie-laptop, vpn-server); `server` host placeholder removed pending physical hardware. Desktop hosts (gaming, laptop, natalie-laptop) track `nixos-unstable`; vpn-server is pinned to `nixos-25.11` (Xantusia) for stability (bumped from EOL 25.05 on 2026-06-17). WireGuard full-tunnel VPN is fully operational. A Jellyfin media server runs on gaming (NVENC transcoding, LAN+VPN-only) and is confirmed working across all devices. dbus-broker active on all hosts. SSH config managed declaratively via Home Manager. A managed Claude Code policy is deployed system-wide via `modules/claude-code.nix` (active on gaming; laptop and natalie-laptop pending rebuild+reboot). `bosko-claude.nix` now includes a declarative HM activation script that automatically trims redundant deny/ask/hooks keys from `~/.claude/settings.json` on every rebuild once the managed policy file is present. natalie-laptop gained a VirtualBox host + extension pack for a Windows VM; briefly dual-booted Plasma 6 and Niri as SDDM session choices (2026-07-11), then dropped Plasma again on 2026-07-18 (niri-only for now — the dual-boot was a leftover that was never actually used). A bug where Dank Material Shell never started for the `natty` user under niri (any niri host, not just natalie-laptop) was fixed 2026-07-12 — all pending rebuild. FinanceGuru installed on gaming and natalie-laptop. lutris re-enabled on gaming. Security hardening completed 2026-06-03: root SSH disabled on vpn-server, SDDM auto-login disabled on gaming (active/confirmed). niri (laptop) now relies on its own built-in Xwayland spawn for X11-only apps rather than a manual service. OnlyOffice's font detection on NixOS is now **fixed and confirmed working** on gaming (`nh os switch`, 2026-07-02) — a `buildFHSEnv` override dereferences the symlinks the sandbox's rootfs-merge step was re-creating; laptop and natalie-laptop still need their own rebuild/switch to pick it up. **GitHub Actions CI is live** (2026-07-02): every push deep-evaluates all four hosts' system derivations. The zen-kernel nixpkgs pin is lifted (upstream fixed). The vesktop build-time `pnpm` insecure-package waiver was resolved and removed 2026-07-16 (fixed upstream); the same flake bump flagged vesktop's Electron runtime (EOL) insecure instead, so an equivalent temporary `permittedInsecurePackages` waiver now covers that until nixos-unstable moves vesktop off it. gaming switched from Plasma 6 to Niri (2026-07-08, pending reboot) ahead of a gaming test on that GPU; `nvidia.nix` gained Niri-specific session variables (`GBM_BACKEND`, `__GLX_VENDOR_LIBRARY_NAME`, `LIBVA_DRIVER_NAME`) since Niri doesn't inherit KWin's automatic nvidia vendor-lib selection. 2026-07-16: new niri keybinds (`Mod+G`, now a 7-app full-setup launcher; `Mod+E` dolphin) and per-monitor window-placement rules on gaming (kitty/steam/lutris/vesktop/zen/deezer/qbittorrent, one app per named workspace slot), plus a fix for a `kdeglobals` dark-theme bug that silently never applied under this repo's real `nh os boot`+reboot workflow. `financeguru` also bumped the same day. 2026-07-17: the shared niri config was split into a common base plus a per-host `niri-overlay.kdl` (via niri's native `include` directive), so gaming keeps its monitor-pinning window rules while laptop and natalie-laptop start with just the standard rules; a second qBittorrent placement bug (a stale app-id in the window-rule regex) was found and fixed. Later the same day: declarative default-application associations (`xdg.mimeApps`) were added, then the whole KDE-app file-opening stack (Dolphin/Ark/Okular/Gwenview) was replaced with Thunar/xarchiver/zathura/imv after the KDE apps proved unreliable under niri (their "open with" resolution depends on `kded6`, which niri never runs) — Kate stays for code/text. Two unrelated live bugs were also found and fixed: USB drives not auto-mounting under Thunar (needed `gvfs`+`thunar-volman`) and `nh os boot` failing outright on a real nixpkgs packaging bug in `apparmor-parser-5.0.0` (worked around via `reloadIfChanged = false`, AppArmor enforcement itself unaffected). **Gaming has been rebuilt and switched live through all of this**, confirmed working (one outstanding item: USB auto-mount needs a reboot to pick up a stale session D-Bus daemon). Laptop and natalie-laptop still need their own rebuild + reboot to pick up the overlay split and the full backlog since session 34. **2026-07-27**: natalie-laptop's clock desync (dying CMOS/RTC battery — hardware, unfixable in config), VPN-kills-internet (root-caused to `wg-quick-wg0` autostarting at boot and racing WiFi association; fixed via `autostart = false` on that host only), and printer detection (previous fix hadn't actually landed in the running generation; now corrected) were all root-caused directly via SSH — natalie-laptop's next rebuild + reboot picks up both fixes. Also 2026-07-27: natalie-laptop's MX350 (Pascal) GPU failing to bind under nvidia's default driver was fixed (closed kernel module + pinned to the legacy 580.xx branch, since the open module needs GSP firmware/Turing+ and 595.84 dropped Pascal support) and **confirmed working live** via a real boot's `journalctl -k -b 0`/`nvidia-smi` output — no config pending. **2026-08-03**: Pinchflat (self-hosted YouTube archiver) added to gaming via nixpkgs' native `services.pinchflat` module, sharing Jellyfin's `media` group and firewall scoping — confirmed live (service active, web UI responding) after gaming's same-day reboot+switch. **2026-08-05 to -08**: pi-hole/famdash DHCP addresses corrected after a power outage (also fixing a fleet-wide pi-hole-bypass bug); niri media keys now target the most-recently-active MPRIS player via `playerctld`; vpn-server's `wg0` MTU clamped to 1380 to stop random gaming stutter caused by Oracle's route cache over-reporting MTU 9000 (confirmed live); a gaming-only VPN health-watchdog timer was added (dry-run clean, pending `nh os switch`) alongside a weekly cloud routine flagging nixpkgs-pin staleness on vpn-server. All fixes confirmed live on gaming; laptop and natalie-laptop still need their own rebuild for the DNS and media-key changes. **2026-08-09**: a full 58-skill audit (5 parallel sub-agents) found and fixed 5 real correctness bugs, all committed, pushed, and **confirmed live** in `~/.claude` after gaming's rebuild later the same day; `dms`/`financeguru`/`home-manager` flake inputs bumped and nixpkgs itself unpinned — the theme (not nixpkgs) migrated instead of waiting on an upstream fix that was never coming (`sweet` was permanently removed in nixpkgs' GTK2 end-of-life purge). GTK theming across all niri hosts is now **Colloid-Teal-Dark**, fully declarative via Home Manager's own `gtk` module (both `settings.ini` and the equivalent `dconf` keys generated from one set of options, eliminating the manual-propagation gap the old `sweet`/`Sweet-Dark` setup relied on), confirmed live on gaming, laptop, and natalie-laptop for both users — also confirmed live: the VPN health-watchdog timer and the pi-hole/famdash DNS + niri media-key fixes from prior sessions, all activated by the same round of rebuilds. **2026-08-14**: routine `/flake-update-verify` run bumped `nixpkgs`, `home-manager`, `dms`, and `sops-nix`, verified clean via `flake-check` plus a 4-host deep-eval, and committed+pushed the lock bump — not yet applied to any host (`/fleet-rollout` is the documented next step). **2026-08-16**: `financeguru` bumped to latest (verified, not yet applied); `services.upower.enable` added to `niri.nix`/`hyprland.nix` so DMS's battery pill/Settings page has a real data source (Plasma auto-enables `upowerd`, bare compositors don't) — live on gaming, pending switch on laptop/natalie-laptop.

## Features

- Single flake managing four active hosts (`gaming`, `laptop`, `natalie-laptop`, `vpn-server`) with shared module composition; `server` host deferred pending physical hardware
- **CI on every push/PR** (`.github/workflows/check.yml`): `nix flake check` plus a per-host deep evaluation of each `nixosConfigurations.<host>` system derivation — eval-only (no builds, no secrets needed; the aarch64 vpn-server evaluates on the free x86 runner), catching cross-host eval breakage that a shallow flake check provably misses
- Home Manager integrated as a NixOS module for both users (`bosko` and `natty`); both users receive the same `home.nix` config — includes Helix editor config and SSH configuration
- SSH config managed declaratively via `programs.ssh.settings` in `dotfiles/common/configs/ssh.nix`; all five SSH hosts (natalie-laptop, laptop, gaming, pi-hole, famdash) defined with explicit `Hostname` and `User` fields; `enableDefaultConfig = false` suppresses implicit-defaults warnings
- **Managed Claude Code policy** deployed via `modules/claude-code.nix`; generates `/etc/claude-code/managed-settings.json` at activation time via `environment.etc`; enforces deny rules for destructive commands, ask rules for sensitive operations, and a PreToolUse fork bomb guard (`jq` pinned to its Nix store path); part of `commonModules` so all hosts share the same policy; users cannot override managed rules in their personal `~/.claude/settings.json`
- Local LLM stack on gaming: Ollama with CUDA acceleration (`pkgs.ollama-cuda`, RTX 3070) serving `mistral-nemo:12b` (128K context, ~7GB Q4, fits in 8GB VRAM); Hermes Agent service configured to use the local Ollama OpenAI-compatible API at `http://localhost:11434/v1`; `hermes` CLI on system PATH; bosko in the `hermes` group for CLI access
- Jellyfin media server on gaming (`hosts/gaming/jellyfin-server.nix`): native `services.jellyfin` on a dedicated `/mnt/media` SSD with NVENC hardware transcoding (RTX 3070); firewall scoped to LAN + WireGuard only; shared `media` group with setgid library dirs; `jellyfin-media-player` client shipped to all desktop hosts via `desktopModules`
- Pinchflat YouTube archiver on gaming (`hosts/gaming/pinchflat.nix`): native `services.pinchflat` (yt-dlp-backed), downloads into `/mnt/media/YouTube` sharing Jellyfin's `media` group, firewall scoped to LAN + WireGuard, `SECRET_KEY_BASE` via a dedicated sops secret
- FinanceGuru personal finance app installed on gaming and natalie-laptop via the `github:CommanderBosko/FinanceGuru` flake input
- Declarative Flatpak management via `nix-flatpak`
- Swappable desktop environment modules (12 options under `desktop-environments/`, including a from-scratch Omarchy-style Hyprland module added 2026-07-10); gaming switched to Niri 2026-07-08 (pending reboot), laptop already on Niri, natalie-laptop is niri-only (briefly dual-booted Plasma 6 alongside it 2026-07-11 through 2026-07-18, then dropped)
- Declarative default-application (MIME type) associations via `xdg.mimeApps` in `dotfiles/common/configs/home.nix`: Thunar (file manager), xarchiver (archives), zathura (PDFs), imv (images), Kate (code/text), VLC (video/audio) — every mimetype verified against each app's real `.desktop` file, not guessed. Deliberately avoids KDE apps (Dolphin/Ark/Okular/Gwenview, used briefly then dropped 2026-07-17): their "open with" resolution depends on `kded6`, which niri never runs, making them unreliable outside a full Plasma session
- GPU modules correctly scoped: `amd.nix` gaming-only; `nvidia.nix` per-host explicit import (gaming, laptop, natalie-laptop) — ready for gaming AMD card swap by removing one line; also carries `GBM_BACKEND`/`__GLX_VENDOR_LIBRARY_NAME`/`LIBVA_DRIVER_NAME` session variables for Smithay-based compositors (Niri) that don't inherit KWin's automatic nvidia vendor-lib selection
- Gaming module with Steam, GameMode, Gamescope, MangoHud, lutris, faugus-launcher, nix-ld, and Steam hardware support — all gaming-specific config colocated in `gaming.nix`
- `claude-code` and `gemini-cli` declared as user-level packages in `users.nix` for both `bosko` and `natty`
- Claude agent definitions (`repo-creator-agent.md`, `session-closer.md`) backed up declaratively via Home Manager and symlinked into `~/.claude/agents/`; `home.activation.trimClaudeSettings` in `bosko-claude.nix` automatically strips redundant deny/ask/hooks keys from `~/.claude/settings.json` on every rebuild once the managed policy file is present (idempotent; no-op until the managed file exists; scoped to bosko only)
- Project-local Claude Code skill library under `.claude/skills/`: 40 skills covering the full NixOS workflow — dry-run (local host only, with a switch-vs-boot recommendation), deep per-host build-graph evaluation (catches shallow-check-only regressions), a forced 4-host sweep specifically after editing a shared module (`shared-module-check`), deep-eval of a single unwired DE module (`de-smoke-check`), GC, VPN status, module scaffolding, new host (sops-aware), new peer, commit, push, flake update, single-input bump, package/flatpak addition, declarative default-application associations (`add-default-app`), niri keybind/window-rule/fullscreen-rule management, desktop environment switching, SSH to any host, remote headless deployment, generation rollback, package search, flake input pinning, generation diff, flake validation, journal tailing, nix repl, .nix formatting, sops secret management, a fleet health sweep, leaked-secret scanning, post-rebuild service verification, CI run status/triage, printer/CUPS diagnostic triage (`printer-diagnose`), and memory writing. Host SSH targets / IPs / the WireGuard peer map are centralized in `.claude/hosts.json` (single source of truth, read by every host-touching skill); deterministic work is factored into per-skill `scripts/` plus shared `.claude/lib/` scripts (`flake-lock-diff.sh`, `list-flake-inputs.sh`, `resolve-host.sh`, `show-pin-state.sh`), large templates live in per-skill `assets/`, and roughly half of all 58 skills (project-local + global) are pinned to a cheaper model (`model: haiku` in frontmatter) based on whether their work is mechanical or judgment-heavy (2026-07-18), audited for quality on a recurring basis via `/skill-audit` (most recently 2026-08-09: 5 correctness bugs found and fixed across the 58-skill library, all committed and pushed)
- WireGuard full-tunnel VPN deployed (hub-and-spoke via Oracle Cloud free ARM VM): shared `vpn.nix` client module, per-host VPN addresses, full-tunnel routing (`0.0.0.0/0`), DNS override, keepalive=25 for Oracle's idle UDP timeout; all three client hosts configured
- Security hardening module (`security.nix`) active in `commonModules`: AppArmor MAC enforcement (`reloadIfChanged = false` works around a nixpkgs packaging bug in `apparmor-parser-5.0.0` that otherwise fails every activation needing an AppArmor reload — enforcement itself is unaffected), auditd (rules-loader service disabled due to nixpkgs/auditctl blank-line bug), kernel image protection, full ASLR, PAM wheel enforcement, SDDM PAM workaround, dbus-broker active on all hosts (explicit plain assignment overrides `nix-flatpak`'s bundled older nixpkgs)
- `~/.local/bin` in bosko's `home.sessionPath` (via `bosko-claude.nix`) so the native claude-code binary at `~/.local/bin/claude` is in PATH after rebuild
- Shared network folder for `bosko`/`natty` (`hosts/gaming/samba-shared.nix`, `modules/shared-folder-client.nix`): gaming serves `/srv/shared` via Samba (guest access, `openFirewall = true`); laptop and natalie-laptop CIFS-mount the same path, auto-mounting at boot (`nofail`, so a client boot never blocks on gaming being offline); both users' `~/Shared` symlinks to it on every host

## Getting Started

### Prerequisites

- NixOS installed on the target machine
- Flakes and `nix-command` enabled
- The `nh` helper tool (installed via the shell module)

### Installation

```bash
git clone https://github.com/CommanderBosko/NixOS /home/bosko/NixOS
cd /home/bosko/NixOS
```

### Rebuilding

```bash
# Stage a rebuild for next boot (also available as the `rebuild` shell alias)
nh os boot /home/bosko/NixOS

# Dry run — see what would change without applying
nh os boot /home/bosko/NixOS --dry

# Update all flake inputs
nix flake update

# Garbage collect old generations
sudo nix-collect-garbage -d
```

### Configuration

Each host has three files under `hosts/<hostname>/`:

- `hardware-configuration.nix` — generated by `nixos-generate-config`, hardware-specific
- `environment.nix` — packages, Flatpaks, display manager defaults
- `networking.nix` — hostname, DNS, firewall, SSH

Host-specific changes go in those files. Shared changes go in `modules/`.

## Project Structure

```
modules/                                # System-level NixOS modules shared across hosts
├── desktop-environments/               # 12 swappable DE modules (niri, plasma, cosmic, …)
├── security.nix                        # AppArmor, auditd, kernel hardening, PAM enforcement
├── amd.nix                             # AMD GPU drivers (gaming host only)
├── nvidia.nix                          # NVIDIA GPU drivers (all desktop hosts)
├── audio.nix                           # Pipewire
├── gaming.nix                          # Steam, GameMode, Gamescope, MangoHud, nix-ld, steam-hardware (gaming only)
├── emulation.nix                       # RetroArch
├── desktop-apps.nix                    # Apps + flatpaks shared by all desktop hosts
├── desktop-networking.nix              # NetworkManager, DNS, firewall, SSH shared by all desktop hosts
├── vpn.nix                             # Shared WireGuard client config (all desktop hosts)
├── sddm.nix                            # Display manager
├── bootloader.nix                      # GRUB + zen kernel (desktop hosts)
├── firmware.nix                        # fwupd
├── fonts.nix, localisation.nix, nix.nix, shell.nix, users.nix
└── home-manager.nix                    # HM module entrypoint for bosko and natty

dotfiles/
├── common/
│   └── configs/                        # Home Manager configs shared by both users
│       ├── home.nix                    # HM root — imported by both bosko and natty
│       ├── helix.nix
│       ├── ssh.nix                     # Declarative SSH config (programs.ssh.settings)
│       └── dotfiles (katerc, kitty.conf)
└── bosko/                              # bosko-specific HM configs (not shared with natty)
    ├── bosko-claude.nix                # Symlinks Claude skills into ~/.claude/skills/ + plugin/settings activation
    └── claude/skills/                  # Repo-owned global Claude Code skills (one dir per skill)

hosts/
├── gaming/                             # hardware-configuration.nix, environment.nix, networking.nix, niri-overlay.kdl
├── laptop/                             # same, niri-overlay.kdl currently an empty placeholder
├── natalie-laptop/                     # same (real hardware data from nixos-generate-config), niri-overlay.kdl currently an empty placeholder
└── vpn-server/                         # configuration.nix, disko.nix, hardware-configuration.nix (live on Oracle Cloud ARM)

.claude/skills/                         # Project-local Claude Code skills (40 total)
├── nixos-dry-run/                      # Preview config changes without applying (nh os boot --dry) — local host only
├── deep-eval-check/                    # Deep per-host build-graph eval — catches what flake-check's shallow check misses
├── shared-module-check/                # Forces the 4-host deep-eval sweep after editing a commonModules/desktopModules file
├── de-smoke-check/                     # Deep-eval a single unwired DE module via lib.deSmoke
├── nixos-gc/                           # Garbage-collect Nix store, keep last 3 generations
├── vpn-status/                         # SSH to VPN server and display WireGuard peer table
├── new-module/                         # Interactive NixOS module scaffolder (3 templates)
├── update/                             # nix flake update with readable lock diff
├── bump-input/                         # Bump a single flake input to latest, without touching the rest
├── pin-input/                          # Pin a flake input to a rev/tag (lock-only or permanent)
├── flake-update-verify/                # Update inputs, prove eval, commit lock-only (self-orchestrating loop)
├── add-package/                        # Add package to correct host or user scope
├── add-flatpak/                        # Declaratively add a Flatpak to a host's environment.nix
├── add-secret/                         # Add / edit / rotate a sops-nix secret
├── add-default-app/                    # Add/change a declarative default-application (MIME type) association
├── add-niri-keybind/                   # Add or check a niri keybind
├── add-niri-window-rule/               # Pin an app to a monitor/workspace slot via a niri window rule
├── add-niri-fullscreen-rule/           # Set open-maximized true on an app's niri window rule
├── switch-de/                          # Swap desktop environment import in flake.nix
├── new-peer/                           # Add a new WireGuard peer to vpn-server
├── new-host/                           # Scaffold a new host (desktop/server/remote-arm types)
├── ssh-host/                           # SSH to any known host by short name
├── remote-rebuild/                     # nixos-rebuild switch --target-host for headless hosts
├── rollback/                           # Show generations, confirm, then switch --rollback
├── search-pkg/                         # nix search nixpkgs wrapper with add-package nudge
├── diff-generations/                   # nix store diff-closures between current and previous generation
├── flake-check/                        # Validate flake across all 4 hosts before rebuild or commit
├── audit-config/                       # Structured security + correctness sweep of the whole flake
├── journal/                            # Tail journald for a named service, local or remote
├── nix-repl/                           # Print nix repl command + host-specific starter expressions
├── fmt/                                # Format changed .nix files (alejandra, nixpkgs-fmt fallback)
├── fleet-status/                       # Read-only health sweep across all four hosts
├── fleet-rollout/                      # Roll out a change host-by-host, gated on dry-run + health (self-orchestrating loop)
├── verify-service/                     # Post-rebuild health check for one named service
├── secret-scan/                        # Scan working tree + git history for leaked secrets
├── public-repo-guard/                  # secret-scan + audit-config, triaged against a baseline (self-orchestrating loop)
├── ci-status/                          # Check or watch the latest GitHub Actions flake-check run
├── save-memory/                        # Write one well-formed entry to the project memory system
└── wayland-screenshot/                 # Screenshot the live niri desktop to visually verify a change
```

### Module Composition

`flake.nix` defines a `mkSystem` helper (exported as `lib.mkSystem`) and two module lists. `mkSystem` takes `{ name, system ? "x86_64-linux", nixpkgs ? nixos-unstable, modules }`, sets `networking.hostName` from `name`, and injects the shared `specialArgs`, so each host entry holds only its unique module list:

- **`commonModules`** — base for all hosts: bootloader, firmware, fonts, localisation, nix settings, shell, users, security, sops
- **`desktopModules`** — `commonModules` + home-manager, nix-flatpak, audio, desktop-apps, desktop-networking, emulation, SDDM, vpn

Each desktop host then adds its own machine-specific modules. Notable per-host additions:
- **gaming**: `gaming.nix`, `nvidia.nix`, `virtualisation.nix`, `jellyfin-server.nix`, `pinchflat.nix` (YouTube archiver), `samba-shared.nix` (Samba server for `/srv/shared`), `niri.nix` (virtualisation is gaming-only, not in desktopModules)
- **laptop**: `nvidia.nix`, `shared-folder-client.nix` (CIFS mount of gaming's `/srv/shared`), `niri.nix`
- **natalie-laptop**: `nvidia.nix`, `virtualisation.nix`, `shared-folder-client.nix`, `niri.nix` (niri added 2026-07-11 as a second SDDM session choice alongside Plasma; Plasma dropped again 2026-07-18, niri-only for now)
- **vpn-server**: `commonModules` only (`aarch64-linux`, systemd-boot, disko, `nixpkgs-stable`)

`flake.nix` also exposes `lib.deSmoke` — the laptop config with each available DE module swapped in. CI's weekly `de-smoke` job deep-evaluates every one so unused DE modules can't rot silently across nixpkgs bumps.

### Users

| User | Groups | User-level packages |
|------|--------|---------------------|
| `bosko` | wheel, networkmanager, audio, video, input, kvm, libvirtd, lp, render | `claude-code`, `gemini-cli` |
| `natty` | wheel, networkmanager, audio, video, input, kvm, libvirtd, lp, render | `gemini-cli` |

Both users share the same Home Manager config (`home.nix`). `homeMode` is `"0700"` for both. Both users are wheel/sudo and Nix trusted users. `natty` has no user-level packages and no SSH keys. `mumble` is a system package on gaming only.

## Security

Security hardening is applied via `modules/security.nix`, which is part of `commonModules` and applies to all hosts.

Enabled hardening:

- **AppArmor** MAC enforcement (`security.apparmor.enable = true`, `killUnconfinedConfinables = false`) — processes without profiles are allowed, not killed (appropriate for desktop workloads)
- **auditd** — Linux audit daemon + `audit=1` kernel parameter; `audit-rules-nixos.service` is disabled via `lib.mkForce false` because `auditctl` 4.1.2-unstable rejects the blank line nixpkgs hard-codes in the generated `audit.rules`
- **D-Bus AppArmor mediation**
- **dbus-broker active** — `services.dbus.implementation = "broker"` (plain assignment, not `mkDefault`) in `security.nix` ensures dbus-broker is used on all hosts. The plain assignment is required because `nix-flatpak` bundles its own older nixpkgs that still defaults to `"dbus"` via `mkDefault`; a plain assignment beats any `mkDefault` regardless of source. All five hosts verified running `dbus-broker-launch` as of 2026-05-21.
- **PAM wheel-group enforcement** for sudo
- **Kernel image protection** — kexec disabled
- **Full ASLR** (`kernel.randomize_va_space = 2`)
- **SDDM PAM workaround** — gated behind `lib.mkIf config.services.displayManager.sddm.enable`; overrides the non-absolute module path that AppArmor's PAM integration rejects

SSH is hardened on gaming and laptop: `PasswordAuthentication = false`, `AllowUsers = [ "bosko" ]`, public key installed.

SSH is fully locked down on vpn-server: `PasswordAuthentication = false`, `PermitRootLogin = "no"` (set 2026-06-03). Remote deploys use `bosko@<vpn-endpoint>` (the endpoint address is kept out of the published README; resolve it from `.claude/hosts.json`) with `security.sudo.wheelNeedsPassword = false`.

## Secrets

Secrets are managed with [sops-nix](https://github.com/Mic92/sops-nix) and committed to the repo **encrypted**, which is what makes this configuration safe to publish. Plaintext secrets never live in the repo or the Nix store.

| Secret | File | Encrypted to |
|--------|------|--------------|
| `bosko` / `natty` login password hashes | `secrets/common.yaml` | admin + all hosts |
| Each host's WireGuard private key | `secrets/hosts/<host>.yaml` | admin + that host only |
| Pinchflat's `SECRET_KEY_BASE` (`pinchflat-env`) | `secrets/hosts/gaming.yaml` | admin + gaming only |

**How it works.** Each host derives its age identity from its existing SSH ed25519 host key (`/etc/ssh/ssh_host_ed25519_key`) — no extra key material is distributed. At activation, `sops-install-secrets` decrypts each secret to `/run/secrets/` (password hashes go to `/run/secrets-for-users/` via `neededForUsers = true`, so they exist before user accounts are created). The recipient map lives in `.sops.yaml`.

Wiring:

- `modules/sops.nix` — imports the sops-nix module and declares the shared password secrets (part of `commonModules`)
- `users.nix` — `hashedPasswordFile = config.sops.secrets."<user>-hashedPassword".path`
- `vpn.nix` and `hosts/vpn-server/configuration.nix` — `privateKeyFile = config.sops.secrets."wg-private-key".path`

**Admin key.** Editing secrets requires the personal admin age key at `~/.config/sops/age/keys.txt`, kept out of the repo. Back it up — it is the recovery path if a host's SSH host key is ever lost.

**Git history.** The `main` branch was rewritten with `git filter-repo` on 2026-06-15 to purge the old plaintext password hashes. Run the `secret-scan` skill before publishing or pushing new branches/tags — it scans across **all** refs (not just `main`), which is what caught (and led to deleting) five stray old branches still leaking the pre-sops hashes on 2026-07-02.

```bash
# Edit a secret (decrypts in $EDITOR, re-encrypts on save)
sops secrets/common.yaml

# Add a new host as a recipient:
#   1. derive its age key:   ssh-to-age < ssh_host_ed25519_key.pub
#   2. add it to .sops.yaml (keys: + the relevant creation_rules)
#   3. re-encrypt:           sops updatekeys secrets/<file>.yaml
```

**Intentionally left in plaintext** (not secrets): all SSH/WireGuard *public* keys — these are public by nature and cannot be used on their own to gain access.

**Kept out of this published README** (operationally sensitive, not committed to the public docs): the VPN server's public endpoint address, and the LAN/VPN subnets and per-host addresses. These are not cryptographic secrets, but the README — being world-readable — should not hand a reader a map of reachable machines. They still live where the config needs them (host modules, `.claude/hosts.json`); this section only governs the README.

## VPN

WireGuard hub-and-spoke full-tunnel VPN, fully deployed as of 2026-05-18.

- **Server**: Oracle Cloud free-tier ARM VM (`aarch64-linux`), VPN gateway address on the `/24` tunnel subnet, standard WireGuard port. The public endpoint address and the tunnel subnet are kept out of this README (see Secrets); they live in the host config and `.claude/hosts.json`.
- **gaming**: VPN client address — private key via sops, `wg-quick-wg0` active
- **laptop**: VPN client address — private key via sops, `wg-quick-wg0` active
- **natalie-laptop**: VPN client address — private key via sops, `wg-quick-wg0` active

All client traffic is routed through the server (`allowedIPs = ["0.0.0.0/0"]`). The tunnel is **IPv4-only** — `networking.enableIPv6 = false` in `vpn.nix` disables IPv6 on all clients so traffic to IPv6-only hosts falls back to IPv4 through the tunnel instead of black-holing in the v4-only `wg0` (this is what was breaking Jellyfin's TMDb artwork fetches). `allowedIPs` is deliberately **v4-only**: with IPv6 disabled there is no stack to leak, and an `"::/0"` entry would make `wg-quick` try to install an IPv6 default route that fails ("IPv6 is disabled on nexthop device") and tears the tunnel down — so IPv6-off and the v4-only `allowedIPs` must always change together. `dns = [1.1.1.1 8.8.8.8]` in `vpn.nix` updates `resolv.conf` on interface up to avoid LAN resolver timeouts under full-tunnel. `persistentKeepalive = 25` prevents Oracle from dropping idle UDP sessions.

Key files: `modules/vpn.nix` (shared client config), `hosts/vpn-server/configuration.nix` (server config with all peers and iptables MASQUERADE).

## Recent Changes

**2026-08-16 (latest)** — Fixed DMS's battery pill/Settings page showing no data on laptop and natalie-laptop: `services.upower.enable` had never been set on any DE module, since Plasma auto-enables `upowerd` as a hidden dependency but bare compositors like niri/Hyprland don't — the same class of gap the existing `accounts-daemon`/`power-profiles-daemon` workaround already covers. Confirmed live (`systemctl status upower` reported the unit didn't exist) before fixing. Added the option to `modules/desktop-environments/niri.nix` (live on all 3 desktop hosts) and mirrored it into `hyprland.nix` (same latent gap, unwired to any host yet). Verified via `nixos-dry-run`, `de-smoke-check`, and a 4-host `deep-eval-check`. gaming already has it live; laptop and natalie-laptop are pending their own switch. Also bumped the `financeguru` flake input to latest — verified clean, not yet applied to any host.

**2026-08-15** — Diagnosed and fixed a network printer (Canon TS9500) not appearing in Zen Browser's print dialog on gaming, and found a new gap in `cups-browsed`'s discovery behavior — no repo changes. The `printer-diagnose` skill confirmed avahi discovery and `cups-browsed.conf` config were both correct, but `cups-browsed` had no permanent CUPS queue for the printer, so every app's print dialog (not just Zen's) was empty. Fixed live with `sudo systemctl restart cups-browsed`, then set the printer as the default via `lpoptions -d` (a user-level setting GTK/Zen print dialogs read, no sudo required). Root cause was distinct from the existing `841eb05` boot-race fix already in `modules/printing.nix`: this time the printer was powered on *after* boot, once the fix's one-shot restart unit had already fired — a trigger that unit doesn't cover. Considered adding a periodic restart timer to close that gap permanently; declined as unwarranted standing complexity for how rarely it happens, in favor of repeating the manual restart if it recurs.

**2026-08-14** — Claude Code Auto Mode enabled globally (Claude tooling only, no repo changes). Explained a nixpkgs eval warning on `gemini-cli-0.47.0` (its new `meta.problems.removal` mechanism, non-blocking, surfacing Google's own deprecation of Gemini CLI for Antigravity CLI — no `antigravity-cli` package exists in nixpkgs yet) and left it as-is by request. Separately, walked through what the built-in `/auto-mode-setup` CLI command had configured in `~/.claude/settings.json` (`autoMode.soft_deny`/`environment` rules scoped to this repo — force-push and live `switch` still require confirmation) and set `permissions.defaultMode: "auto"` in that same global file, so Auto Mode is now the startup default for every project on this machine. Considered scoping the repo-specific rules into this project's own settings instead; kept as one global ruleset by explicit choice.

**2026-08-14** — Routine `/flake-update-verify` run: bumped 4 flake inputs — `nixpkgs` (`f13ff45a`→`0e251e24`), `home-manager` (`367f7ef8`→`83b7606d`), `dms` (`80bad610`→`fb9f00a6`, plus its `dank-qml-common` sub-input), `sops-nix` (`f1406619`→`a8627b21`); `financeguru`/`disko`/`nix-colors`/`nix-flatpak`/`nixpkgs-stable` held steady. Verified via both `flake-check` (shallow, all 4 hosts) and the loop's mandatory deep-eval (`toplevel.drvPath` per host, per its standing gotcha that flake-check alone isn't sufficient) — all 4 hosts resolved clean. One non-blocking eval warning surfaced (`gemini-cli-0.47.0`'s nixpkgs removal notice, not an insecure-package gate). Committed `flake.lock` alone (`4ea763c`) after the loop's mandatory step-6 approval gate, pushed to `origin/main`. No `switch`/`boot` performed anywhere — applying the bump is out of scope for this loop; `/fleet-rollout` is next.

**2026-08-10** — Bumped niri's swaylock idle-lock timeout from 5 to 10 minutes (`modules/desktop-environments/niri.nix`), applying to all 3 niri hosts; dry-run clean, not yet switched live. Also closed out 3 other sessions that ran unclosed earlier the same day: shipped a new global `resume-session` skill (locates and summarizes a project's own status docs) ported from a standalone draft written in a sibling `bitburner` repo, fixed a real bug where the skill-transcript-cutoff detector missed direct `/skill-name` slash-command invocations (it only matched `Skill`-tool-use entries), and reviewed and merged the weekly `improve-system` PR #8 — content review caught and corrected a wrong claim the PR itself reinforced about needing a brand-new Claude Code session after a rebuild (a reboot alone is sufficient; skill discovery reads from disk per invocation, not cached at session start).

**2026-08-09** — Finished the Sweet-Dark→Colloid-Teal-Dark theme rollout (resumed from a mid-session handoff) and, along the way, replaced the manual `dconf`-write + hand-edited-`settings.ini` pattern with a genuinely declarative fix. Verified gaming live, hit two real snags getting laptop and natalie-laptop rebuilt (laptop transiently lost WAN routing; natalie-laptop's actual graphical session belongs to `natty`, not `bosko`, so an earlier by-hand fix touched the wrong user's files), then — asked to make theme selection declarative for every user — read Home Manager's own `gtk`/`pointerCursor` module source directly rather than guessing from docs, and found that `gtk.enable = true` with `theme`/`iconTheme`/`colorScheme` set generates **both** `settings.ini` and the equivalent `dconf` keys from the same options in one activation step, closing the propagation gap that had gone stale three separate times in one session. `home.pointerCursor.gtk.enable = true` now feeds the cursor into the same pipeline instead of duplicating it. The first switch attempt failed activation because Home Manager correctly refused to silently clobber real pre-existing files at the paths the new `gtk` module claimed — fixed generally with `home-manager.backupFileExtension = "backup"`, since every user on every niri host was about to hit the same collision on their first switch. Verified live on all 3 hosts after the user switched each one: correct symlinks/content/dconf everywhere, and natalie-laptop's `natty` session picked up the fix for free, closing that gap without the sudo workaround originally drafted for it.

**2026-08-09** — Ran `/skill-suggestion` → `/skill-upgrade` → `/skill-audit` end-to-end across the whole Claude skill library, plus a routine flake update and weekly-PR check. `skill-suggestion` found no new skill candidates worth building (66 transcripts scanned, nothing cleared the reuse bar). `skill-upgrade` added 3 gotchas from real misfires: `add-secret` (bare `openssl` isn't on `PATH`), `session-closer` (`uptime -p`/`-s` fail on this host's coreutils build — use `who -b`), `journal` (`vpn-server` has no `~/.ssh/config` alias, unlike the LAN hosts). `skill-audit` swept all 58 skills via 5 parallel sub-agents and found 5 real correctness bugs — a bare `scripts/` path that 404s in 6 skills, `rollback`'s false claim that `nh` has no rollback subcommand, `review-improve-system-pr`'s PR-search query silently returning zero results (a `gh` query-tokenizer issue with `()`/`:` characters), `/update`'s missing check for an active nixpkgs pin, and `new-host`'s un-substituted state-version placeholder — all fixed, verified, and pushed across 5 commits. Separately, `/flake-update-verify` hit a 3rd consecutive blocked nixpkgs update (the Sweet-Dark theme removal still isn't fixed upstream even 6 days further in); the user again chose to keep the pin temporary rather than permanent, so `dms`/`financeguru`/`home-manager` updated on their own while nixpkgs stayed pinned. The weekly `improve-system` PR check found nothing open, as expected before its 2026-08-10 scheduled run.

**2026-08-08** — Five independent fixes/features landed since the last close: (1) the weekly `improve-system` PR-review skill got its first full live run, merging a stale-carve-out removal and a confirm-gate hardening; (2) `financeguru` bumped; (3) a power outage drifted both the pi-hole and famdash Raspberry Pis' DHCP addresses, which also surfaced a real bug — the primary LAN DNS resolver in `desktop-networking.nix` had silently become famdash's old slot, bypassing pi-hole ad-blocking fleet-wide since the outage — fixed alongside the drift itself; (4) niri's media keys (play/pause/next/prev) now route to whichever MPRIS player was most recently active, matching KDE Plasma's behavior, via `playerctld`; (5) a non-obvious VPN MTU bug was root-caused and fixed on vpn-server — Oracle's route cache misreports MTU 9000 for the default route, so `wg-quick` auto-detected `wg0` at 8920 with no explicit clamp, causing oversized return packets to fragment and get silently dropped, which read as random gaming stutter with no clear pattern; clamped to `mtu = 1380` to match the client side, confirmed live. A follow-up vpn-server audit also added a gaming-only WireGuard health-check watchdog timer (not yet switched live) and a weekly cloud routine nudging on nixpkgs-pin staleness. Separately, a `/research` pass concluded that running NixOS itself (not OPNsense/VyOS/an appliance) is the best fit for a future home router given this repo's declarative/Claude-managed workflow — written up in a sibling `home-lab` docs repo, no hardware bought yet.

**2026-08-03** — Found and fixed why Pinchflat's YouTube downloads were failing with "Sign in to confirm you're not a bot": gaming's WireGuard VPN is a deliberate full tunnel, so Pinchflat's yt-dlp traffic was exiting via the Oracle Cloud VPN server's own IP — a datacenter address YouTube flags far more aggressively than a residential one, confirmed directly (`curl -4 ifconfig.me` from gaming returned the VPN server's address). Added a UID-scoped `ip rule` (via `wg-quick`'s `postUp`/`postDown`) so only Pinchflat's traffic bypasses the tunnel via the normal LAN gateway, resolved at runtime rather than eval time since NixOS system users get dynamically-assigned UIDs. That alone wasn't sufficient — it made every one of Pinchflat's connections hang forever in `SYN-SENT`, because NixOS's firewall enforces its own strict reverse-path check independent of the kernel's `rp_filter` setting, and dropped the return traffic as spoofed since the global route still pointed at the VPN. Fixed by loosening `checkReversePath` for gaming, matching a pattern this repo's `vpn-server` host already used for the same class of asymmetric-routing problem. Confirmed fully live: all previously-failing videos downloaded successfully end-to-end.

**2026-08-03** — Added Pinchflat, a self-hosted YouTube archiver, to gaming and wired it to the existing Jellyfin library. `/research` found nixpkgs already ships a native `services.pinchflat` module (no Docker container needed); configured it to download into a new `/mnt/media/YouTube` folder sharing Jellyfin's existing `media` group, with its web UI firewalled to LAN + WireGuard like Jellyfin, and its session secret through a dedicated sops entry rather than the module's weak `selfhosted` fallback. Confirmed live via direct verification (not assumed) after gaming's same-day reboot and switch — which, as a side effect, also activated a backlog of several previously-unclosed sessions' worth of pending changes (the 2026-07-30 skill-audit global-skill fixes, a `cups-browsed` boot-race fix, an `amd_pstate` kernel param, and a stale-`dbus-broker` USB-automount bug all confirmed resolved in the process). Added a first source (Yes Theory) and hit two real Pinchflat quirks along the way: a `download_cutoff_date` correctly filtering out all current videos until the channel's next upload (working as intended, not a bug), and a channel getting misdetected as a `playlist` internally — confirmed as a real, still-open upstream bug rather than a local misconfiguration, and accepted as a cosmetic-only limitation (missing series artwork) after re-adding the source didn't fix it.

**2026-07-30** — Full `skill-audit` sweep of all 58 Claude skills (5 parallel sub-agents): fixed 3 real correctness bugs (`remote-rebuild`'s tunnel-restart script tried a privileged sudo command over non-interactive SSH on hosts without passwordless sudo; `skill-audit`'s own Step 1 contradicted its documented Gotcha; `session-closer`'s size-estimate numbers were stale) plus 4 style fixes, and declined one recommendation after confirming it was an intentional convention rather than a deviation. Shipped a new `printer-diagnose` skill automating a diagnostic sequence that had been done by hand across 4+ prior sessions, and a `nixos-dry-run` improvement that recommends `switch` vs. `boot` based on the diff content. Also, on the desktop side: added then same-day reverted a Wine tray-icon bridge (`snixembed`) after confirming live it breaks DMS's own tray entirely; fixed `.docx`/`.odt` files opening in the archive manager instead of OnlyOffice (a stray mimetype mapping, confirmed fixed on natalie-laptop); fixed niri's screen-share/screenshot portal routing (was pointed at a backend that doesn't implement those interfaces, confirmed on gaming); confirmed gaming's static LAN IP is holding after reboot; and fixed the `/srv/shared` CIFS mount failing to resolve gaming's bare hostname (confirmed on natalie-laptop, laptop still pending its own rebuild).

**2026-07-29** — Added a shared network folder (`/srv/shared`, symlinked to `~/Shared` in Thunar) so `bosko` and `natty` can exchange files. First pass built it as an independent local folder on all 3 desktop hosts; once the user realized that meant no syncing between machines, redone as a real network share instead — gaming (already the always-on Jellyfin host) serves it via Samba with guest access, laptop and natalie-laptop CIFS-mount it at the same path, auto-mounting at boot without blocking if gaming happens to be off. See Features and Module Composition below for the resulting module layout.

**2026-07-28** — Added an explicit "verify visually after reboot" step to four DE/niri skills (`switch-de`, `add-niri-fullscreen-rule`, `add-niri-window-rule`, `add-niri-keybind`), which previously ended their next-steps reminder at commit with no confirmation the visual change actually landed — none of these take effect without a reboot, so a clean config eval never proved the result looked right. Then ran the `fewer-permission-prompts` skill: scanned the 50 most-recent session transcripts across every project for Bash/MCP calls, filtered out anything already auto-allowed, anything mutating, and the whole interpreter/shell-runner category (never allowlisted regardless of frequency), and added 4 genuinely read-only, still-prompting patterns to `.claude/settings.json` — a status-check script from `session-closer`, the `status` (not `commit`) mode of `git-commit`, the `status` (not `execute`) mode of `git-push`, and `flatpak remote-info`.

**2026-07-28** — Audited every skill that performs a live `nixos-rebuild switch`/`nh os switch`, after being asked whether any should be removed since Claude can't supply an interactive sudo password (no NOPASSWD rule exists on any host but vpn-server). `rollback` and `nixos-gc` already handed their privileged step off to the user correctly, and `remote-rebuild` works end-to-end because vpn-server alone has passwordless sudo — but `fleet-rollout`'s switch step had no hand-off and would just fail on the password prompt. Fixed it to hand the switch command to the user for every host except vpn-server, made explicit that the pause applies even under an unattended Training Mode OFF run, and fixed a second bug found in the process: the skill was pointing vpn-server's own deploy at `switch --target-host`, which the `remote-rebuild` skill's Gotchas already document as dropping the SSH session mid-activation on that host — corrected to `remote-rebuild`'s actual `boot` + reboot method. Also fixed `rollback`'s Step 3, which contradicted its own hand-off Gotcha by instructing the sudo command to be run directly. Considered and declined adding a NOPASSWD sudo rule to remove the friction entirely: `nixos-rebuild switch` activates an arbitrary system config — effectively unrestricted root code execution — so even a rule scoped to that one command would remove the last human checkpoint against a compromised or prompt-injected agent session. Separately, three commits from an earlier same-day unclosed session landed too: disabling `amd_pstate` on gaming to stop CPPC probe-fail log spam (BIOS reports zeroed frequency tables for the 3700X), a new `boot-error-triage` skill generalizing that root-causing workflow, and a Gotchas note added to `git-commit`/`git-push` about an absolute-path mistake that had recurred twice.

**2026-07-27** — Verified natalie-laptop's nvidia driver fix live rather than trusting eval alone. An earlier same-day session had fixed the MX350 (Pascal) GPU failing to bind under nvidia's default driver: switched to the closed kernel module (open module needs GSP firmware, Turing+ only) and pinned the driver package to the legacy 580.xx branch (595.84 silently dropped Pascal support). This session checked `journalctl -k -b 0` and `nvidia-smi` directly on a real boot: the module now loads, binds to the GPU, and reports the driver live, with no probe failures and no failed units — confirming both fixes actually work, not just that they evaluate. Along the way, an SSH connection attempt to natalie-laptop from another host hit a host-key mismatch that was deliberately not bypassed (flagged rather than assumed benign) — moot once it turned out the session was already running locally on the host. Separately, the routine `secret-scan` pass run before this close flagged what looked like a real leaked-password-hash-in-history finding; before acting on it, checked reachability and found the flagged commits existed only in local `git stash` entries, never pushed or public — fixed the scanner (`.claude/skills/secret-scan/scripts/secret-scan.sh`) to exclude `refs/stash` from its history scan so it doesn't false-alarm on stash WIP again.

**2026-07-27** — Follow-up on the previous session's natalie-laptop close-out: confirmed both prior fixes (VPN autostart disabled, printer `cups-browsed` config) are live, then closed the one open thread from that session — why OnlyOffice still couldn't see the network printer even though Kate could. Root cause: Kate's system CUPS client does live DNS-SD printer discovery and can show a network printer before any permanent CUPS queue exists, while OnlyOffice's bundled print dialog (running in its own `buildFHSEnv` sandbox) only ever sees printers already registered as permanent CUPS destinations. The permanent queue hadn't been created despite correct `CreateIPPPrinterQueues Everywhere` config because `cups-browsed.service` is bound to `avahi-daemon.service` and gets reset by every avahi restart — journal showed it bouncing every few minutes to hours on this host, never getting a clean run long enough to finish. Fixed live with `sudo systemctl restart cups-browsed`; no repo change needed. Diagnostic/live-fix session only, no commits.

**2026-07-27** — Root-caused all three of natalie-laptop's reported issues directly via an SSH session on that host: clock desync, no internet while connected to the VPN, and the printer still not appearing in OnlyOffice. The clock desync is a confirmed hardware fault — a dying CMOS/RTC battery (the RTC reports the same ~4.8-year-stale date on every boot, corrected within ~60s by chrony's already-optimal default config) — with no software fix possible. The VPN issue turned out not to be a config difference from gaming/laptop at all: `wg-quick-wg0` autostarts at boot on all three hosts, and the full-tunnel kill-switch route captures all traffic (including chrony's own NTP retries) the instant the service starts, even before the WireGuard handshake completes — if this host's WiFi is still associating at that exact moment, everything vanishes into the tunnel with no fallback until `vpn-off` is run manually. Fixed by setting `networking.wg-quick.interfaces.wg0.autostart = false;` on natalie-laptop only. The printer issue was never OnlyOffice-specific — CUPS had zero configured destinations for any app — because the previous session's printer fix (`6877f4b`) had landed in a generation built from a checkout 54 seconds too old to include it in substance; confirmed via `nix derivation show` that the deployed config was baked empty. A fresh dry-run against the current checkout now builds correctly. Both fixes are staged and cached, awaiting the user's own `nh os boot` + reboot.

**2026-07-27** — Fixed network printer detection under niri. A live-inspection diagnosis on gaming (`avahi-browse`, `lpstat`, reading the shipped `cups-browsed.conf.5` man page) found that `cups-browsed`'s default `CreateIPPPrinterQueues=LocalOnly` was discovering network printers via mDNS but never turning them into real CUPS destinations — print dialogs only show actual destinations, so the printer was invisible everywhere despite avahi working correctly. Fixed with `services.printing.browsedConf = "CreateIPPPrinterQueues Everywhere"` in `modules/printing.nix` (shared across all desktop hosts), verified via a 4-host deep-eval sweep, then confirmed live on gaming. A follow-up report that Zen Browser (Flatpak) still couldn't see the printer turned out to be a stale long-running-process issue, not a second bug — relaunching Zen picked up the fix. natalie-laptop was rebuilt and fully rebooted but the printer is still not detected there; not yet diagnosed, and the user separately noted natalie-laptop has other unrelated ongoing issues to investigate directly on that host.

**2026-07-27 (session 57)** — Found and fixed a second real bug in the "Weekly improve-system sweep" cloud routine, then hardened it toward parity with a live session. The routine had been re-reporting the same 6 "structural findings" for three straight weekly runs; investigation found all 6 were already fixed by a commit from session 52 (2026-07-20), and the routine's `persist_session: true` setup — reusing the same session across every weekly fire — had a stale, wrong belief about what had changed since its last audit and never noticed the fix. Rather than just patch around the stale belief, removed the reuse-shortcut entirely (the routine now always runs its full 55-skill audit fresh, every run), set `persist_session: false` (no cross-week memory at all — everything derives from live git/GitHub state), and added a new "self-verifiable structural fixes" auto-apply tier so a skill-`SKILL.md`-only finding with a scriptable check can land as a PR instead of sitting report-only forever (still excludes anything touching a `.nix` file, since the cloud sandbox has no `nix`/`nh` available to actually verify one). Manually triggered the hardened routine to confirm the changes worked: it found and applied 7 genuinely real documentation-drift bugs this time (a rules-count typo, a false permissions claim about the `natty` user, a stale security-workaround description, a wrong desktop-environment host roster, and three hardcoded host lists replaced with live `jq` enumeration against `.claude/hosts.json`), and correctly refused one sub-agent's false-positive claim after cross-checking it against the real files. Reviewed and merged the resulting PR by hand.

**2026-07-26 (catch-up close covering 2026-07-23 through -26)** — Four sessions' worth of work closed out in one pass (no individual `/session-closer` run in between). `flake-update-verify` gained a mandatory `AskUserQuestion` gate before its commit+push step — it now always stops and shows the per-input revision summary plus the eval verdict before pushing to `origin/main`, regardless of Training Mode (whose default flipped ON→OFF the same day, so the update/verify steps themselves run autonomously) or how the invocation was phrased. Routine flake bumps landed on 2026-07-24 (resolved the `electron-40.10.5` insecure-package waiver — nixpkgs now ships vesktop on `electron_43` — but the same bump surfaced a new `rtk` build failure on unstable hosts, patched with `doCheck = false`) and 2026-07-26 (`dms`/`financeguru`). niri's cursor theming was made comprehensive (2026-07-25): a missing `cursor{}` KDL block meant niri's own shape-protocol rendering (text/grab/resize cursors) fell back to niri's bundled default instead of `breeze_cursors`, even though GTK apps looked correctly themed via dconf — fixed with the KDL block plus `home.pointerCursor` (GTK path deliberately left off to avoid clobbering the hand-set Sweet-Dark theme). Claude tooling gained a 5th standing rule (route interview/decision confirmations through `AskUserQuestion`), a new `/create-secret-scan` generator (project-tuned secret scanning on demand, rather than one global scanner — also fixed a real bug where `git grep`'s leading-dash pattern handling meant private-key detection had never actually worked), a `git-commit` safety improvement (asks before dropping unrelated uncommitted files), and a new `commit-and-push` chaining skill. Smaller fixes: gaming's Jellyfin drive now shows in Thunar's sidebar, and niri's Mod+B/Mod+G also open a Zen private window.

**2026-07-20 (session 52 continued)** — Fixed all 6 structural findings the improve-system routine's first clean run (below) surfaced but didn't auto-apply. `de-smoke-check`/`public-repo-guard`'s free-prose pick-one/confirm gates now route through AskUserQuestion, matching the pattern already used by `bump-input`/`nixos-gc`/`rollback`/`pin-input`. `shared-module-check` gained the `## Arguments` section it was missing. `create-loop`'s mechanical structural checks were extracted into a new `scripts/lint-loop.sh`. `skill-suggestion`'s duplicated transcript-directory-resolution logic was pulled into a new shared `dotfiles/bosko/claude/skills/lib/find-transcript-dir.sh` — and, going a step further than the finding asked, `skill-upgrade` and `session-closer` were also rewired to call the same shared script instead of leaving their own copies in place. `skill-upgrade`'s own Gotchas-append confirm gate got the same AskUserQuestion treatment. Verified via `nixos-dry-run` (clean) and a full 4-host `shared-module-check` sweep (the new shared-lib wiring touches `bosko-claude.nix`, which is shared across every desktop host) — all PASS.

**2026-07-20 (session 52)** — Found and fixed the root cause behind the "Weekly improve-system sweep" cloud routine's silent failures (session 51). Re-checked the third redesigned run ~20 hours after it fired: it had run, but still produced zero trace on GitHub — meaning even its literal first instructed action (`gh issue create`) never landed, ruling out a mid-run hang and pointing at an infra/auth problem instead. Traced it through GitHub's settings UI: the "Claude" GitHub App was **authorized** on the account (visible under Authorized GitHub Apps, with Issues/PRs permissions listed) but had **zero installations** — GitHub Apps require both gates independently, and authorization alone grants no actual repo access. Installed the app on this repo, then re-triggered the routine to verify: a tracking issue was created within 15 seconds, all six checkpoints posted, and the issue closed with a full report about 90 seconds later — the routine's first-ever clean end-to-end run. Confirms session 51's prompt redesign was correct all along; the blocker was entirely on the GitHub-integration side.

**2026-07-19/20 (session 51)** — The "Weekly improve-system sweep" cloud routine (scheduled session 46) fired for the first time and produced no visible trace at all — no PR, no branch, no issue on GitHub — with no way to tell whether it ran clean or failed silently, since the routine's `persist_session` flag was off and its session transcript was simply discarded on completion. Turned that flag on and re-ran; after 78 minutes of active polling, still nothing appeared, exposing the real design flaw: the routine only ever wrote anything externally-visible as its very last action, so a mid-run hang and a clean pass looked identical from outside. Redesigned the routine's prompt so it opens a GitHub tracking issue as its literal first action (before reading any skill file), posts a short checkpoint comment after each of its six steps, and only closes the issue once a final consolidated report has been posted — turning silence into "check the issue's checkpoints" instead of an unrecoverable black box. A third run was kicked off under the new design; its outcome was resolved the next session (see 2026-07-20 above).

**2026-07-18 (session 50)** — Rolled a cost/quality paradigm out across the Claude Code skill library, then chased down two real cross-host bugs the verification work surfaced along the way. Classified all 52 skills as mechanical ("grunt") or judgment work via parallel sub-agents, and pinned `model: haiku` in frontmatter on the 32 grunt-work skills (fixed command sequences, fetch/format/relay, template-fill) while leaving judgment-heavy skills (synthesis, auditing, planning, drafting) on the session's default model — confirmed `SKILL.md`'s `model:` field is an official, documented Claude Code feature. Then ran a scoped skill-audit for deterministic, judgment-free work still being re-improvised in prose every run, and extracted it into `scripts/` for 11 skills across six commits, including a new shared `.claude/lib/resolve-host.sh` that de-dupes host-resolution logic previously copy-pasted between `ssh-host` and `journal`. Both paradigms — model tiering and script extraction — were then baked into `new-skill`/`create-loop` so every future skill gets them automatically, not just this one-time retrofit. The new `switch-de` script immediately caught a real, previously-unnoticed bug: `natalie-laptop`'s flake entry still imported both `niri.nix` and `plasma.nix` (a leftover SDDM dual-session setup from session 42 that was never actually used) — removed Plasma, niri-only now. Separately, the sweep's own cross-host verification run caught a second real bug: `vpn-server`'s flake evaluation had been broken since a prior session's `rtk` install, because `rtk` only exists in nixpkgs-unstable and `vpn-server` pins `nixpkgs-25.11` stable for its `aarch64-linux` build — fixed by gating the package and its Claude Code hook on `pkgs ? rtk` existence rather than hardcoding it, so it self-heals once a future stable release backports the package (the fix is deployed to the flake but deliberately not yet rolled out to the live vpn-server, at the user's call, since there's no urgency until nixpkgs catches up). Built a new `shared-module-check` skill specifically to close the gap that let that bug through uncaught — it forces a full 4-host evaluation sweep after any shared-module edit, instead of trusting a local dry-run that silently verifies nothing about the other hosts. Also caught and fixed a bug in a script written earlier in the very same session: `session-closer`'s new git-scan script used `git log --all`, which pulls in stash entries and unrelated branch history into what should be a clean session-to-session commit range.

**2026-07-17 (session 48)** — Added declarative default-application associations (`xdg.mimeApps` in `home.nix`) for Kate/Ark/Okular/Gwenview/VLC, verifying every mimetype against each app's real `.desktop` file. Then discovered Dolphin's "Open With" picker is fundamentally unreliable under niri: KDE's `ksycoca` application database needs `kded6` (normally started by a full Plasma session) to stay valid, and niri never runs it — confirmed live via `xdg-open` resolving correctly while Dolphin's own picker stayed empty even after clearing caches and testing a genuinely fresh process. A standalone `kded6` systemd service was drafted as a fix and flake-checked clean, but ultimately dropped in favor of switching the whole file-opening stack to apps that don't depend on it: Thunar replaced Dolphin (and Double Commander, added earlier the same session), xarchiver replaced Ark, zathura replaced Okular, imv replaced Gwenview — net **-123 MiB** on gaming. Built a new `add-default-app` skill for the resulting repeatable workflow (shipped initially without a smoke-test, which led to `new-skill` gaining a permanent "this hasn't been tested, see `ship-skill`" reminder). Fixed three niri skills (`add-niri-window-rule`, `add-niri-keybind`, `add-niri-fullscreen-rule`) that still pointed at the pre-overlay-split `niri-config.kdl` location. Separately root-caused two live bugs: USB drives not auto-mounting under Thunar (needed `services.gvfs.enable` + `thunar-volman`, since Dolphin's KIO backend never needed them — though a full fix still needs a reboot or session D-Bus restart to pick up gvfs's new service registration) and `nh os boot` failing outright with "Failed to reload apparmor.service" (a genuine nixpkgs packaging bug in `apparmor-parser-5.0.0` — worked around via `reloadIfChanged = false`, AppArmor enforcement itself unaffected). Gaming is fully rebuilt and live through all of this; laptop and natalie-laptop still need their own rebuild.

**2026-07-17 (session 47)** — Split the shared niri configuration into a common base plus a per-host overlay, so each niri host (gaming, laptop, natalie-laptop) can carry its own window rules. Discovered mid-implementation that niri's KDL config format has a native `include "path"` directive (already used for DMS's `dms/outputs.kdl`), which turned out to be simpler than the originally-planned Nix-level text concatenation. `dotfiles/common/configs/niri-config.kdl` stays shared (input, layout, binds, and the two "standard" window rules) and now includes `niri-overlay.kdl`; gaming's monitor-pinning workspaces and window rules moved verbatim into `hosts/gaming/niri-overlay.kdl`, while laptop and natalie-laptop got empty placeholder overlays. Also found and fixed a second, previously-unknown qBittorrent bug distinct from session 45's autostart fix: its window rule matched app-id `qbittorrent`, but the live window's real app-id is `org.qbittorrent.qBittorrent` — the rule had silently never matched. Verified via `niri validate` on all three hosts' composed configs, then rebuilt and switched gaming live (generation 332→333), confirming both the overlay split and the qBittorrent fix work correctly — this also activated the entire backlog of pending niri changes from sessions 34-46 on gaming. Laptop and natalie-laptop still need their own rebuild.

**2026-07-16 (session 46)** — Scheduled a recurring cloud agent ("Weekly improve-system sweep") to run this repo's Claude-ecosystem maintenance sweep every Sunday, via `claude.ai`'s routine scheduler rather than anything local — it runs entirely in Anthropic's cloud, independent of any machine being on. Getting there required working through real constraints of unattended execution: the cloud sandbox has none of this repo's Home-Manager-managed `~/.claude/skills/` symlinks, so the routine's prompt reads each relevant skill's `SKILL.md` directly from the repo checkout instead of relying on slash commands; and `/improve-system`'s own design assumes a human answers its `AskUserQuestion` approval gates, so those were replaced with a fixed policy (auto-apply low-risk additive changes, report-only for anything structural) since nobody's present on a schedule. The routine opens a PR against `main` for review rather than pushing directly, since it's a brand-new, never-yet-run workflow. Also folded in one small fix made in a concurrent local session: removed `open-maximized true` from kitty's window-rule (added in session 45) after deciding kitty specifically shouldn't launch maximized, keeping its monitor pin intact.

**2026-07-16 (session 45)** — Root-caused a workspace-placement bug rather than re-checking the already-correct window rule. qBittorrent kept opening on the wrong workspace via the `Mod+G` launcher despite session 44's `open-on-workspace` rule matching its app-id exactly; the real cause was qBittorrent's own "run on startup" preference autostarting it ~12 seconds before `Mod+G` ran — since qBittorrent is single-instance, the launcher's `qbittorrent &` was just raising that already-misplaced window rather than creating a fresh one the rule could act on. Fixed live (outside the flake, not git-tracked): stopped the autostart systemd unit and removed the autostart `.desktop` file, so qBittorrent now only ever opens via `Mod+G` — already in effect on gaming, no reboot needed. Also added `open-maximized true` (the `Mod+F` equivalent) to five window rules — Kitty, Vesktop, Zen Browser, Deezer, qBittorrent — so they launch already-maximized; the same rule was briefly added to Steam too, then reverted at the user's request. Shipped a new project-local **`add-niri-fullscreen-rule`** skill (sibling of `add-niri-window-rule`, scoped to just `open-maximized`) after doing that exact edit six times by hand in one session, and added a Gotchas section to `nixos-dry-run` documenting that its output is a redrawing TUI tree whose useful summary sits at the very end. The five new `open-maximized` rules ride along with the already-pending gaming/laptop reboot; the qBittorrent autostart fix needs no reboot.

**2026-07-16 (session 44)** — Continuation of the same day's work: bumped the `financeguru` flake input (`50335413` → `652a706b`, verified clean), pinned Deezer to a new `dell-3` workspace slot, then shipped a new **`add-niri-window-rule`** skill after noticing the standalone "pin X to monitor Y" workflow (no keybind involved) had now been done twice by hand with nothing covering it — `add-niri-keybind` only handles window rules tied to a *new* keybind. Smoke-tested the new skill for real: asked it to pin qBittorrent to the Asus monitor, it correctly found all three Asus slots already occupied and asked rather than double-booking, then created a new `dell-4` slot on the Dell monitor instead (kept as a real change). Finally expanded `Mod+G` from a 3-app "gaming apps" launcher into a full 7-app initial-setup launcher (kitty, steam, lutris, vesktop, zen browser, deezer, qbittorrent) — every app already had a monitor pin from this session's work, so one keybind now spreads the whole setup across both of gaming's outputs. All four commits pushed; rides along with session 43's already-pending gaming/laptop reboot.

**2026-07-16 (session 43)** — Niri keybind/window-rule additions, a real theming bug fix, a routine flake bump, and a Claude-tooling maintenance pass. Bound `Mod+G` to launch Steam, Lutris, and Vesktop together and `Mod+E` to open Dolphin; added per-monitor window rules on gaming pinning kitty/Steam/Lutris to the Asus output and Vesktop/Zen Browser to the Dell output. Found and fixed a real bug in the Dolphin dark-theme fix from session 38: the `kdeglobals` `ColorScheme` write was gated on `XDG_CURRENT_DESKTOP=niri` already being set at Home Manager activation time, a condition that only holds for an interactive live-session switch — this repo's actual workflow (`nh os boot` then reboot) activates before any session exists, so the guard silently never fired; removed it (safe, since a real Plasma session re-applies its own scheme on login regardless). Bumped `dms`, `financeguru`, `home-manager`, and `nixpkgs`, verified via flake-check plus a deep-eval of all four hosts; the bump both fixed vesktop's pinned `pnpm` build tool upstream (letting the session-29 insecure-package waiver be removed) and flagged its Electron runtime as newly insecure (EOL), so a fresh, equivalent waiver was added for that instead, with the user's explicit sign-off. Ran `/improve-system`: added a project-local `add-niri-keybind` skill, a `flake-update-verify` gotcha recorded from the waiver swap, a clean 54-skill audit with a few low-risk script/doc extractions applied, and a smoke-test step added to `improve-system`'s own skill-creation handoff. All eight commits pushed; the keybind/window-rule/theming/flake changes are staged for gaming and laptop's next reboot.

**2026-07-12 (session 42)** — Fixed Dank Material Shell not starting for the `natty` user under Niri. User reported it worked for `bosko` on natalie-laptop but not `natty`; root cause was that `niri.nix`'s entire DMS/`config.kdl`/`qt6ct.conf`/kdeglobals Home Manager block only ever targeted `home-manager.users.bosko` — `natty` received the shared `home.nix` base like bosko but none of the DMS-specific wiring, so DMS never started in natty's niri session on *any* niri host (gaming, laptop, natalie-laptop), not just the one it was reported on. Refactored the block into a shared `niriHomeConfig` binding assigned to both `home-manager.users.bosko` and `home-manager.users.natty` — confirmed with the user first that the fix should cover every niri host, not just natalie-laptop, since the module is shared. Verified via a deep-eval of all four hosts (all clean). Also folded in two commits from a prior, unclosed session: natalie-laptop gaining Niri alongside Plasma as a second SDDM session choice, and a follow-up that gated DMS/swayidle/kdeglobals to only fire in a niri session (not Plasma) once natalie-laptop could boot either. All three commits pushed; staged for next boot on natalie-laptop, not yet applied.

**2026-07-11 (session 41)** — Asked whether the previous session's `omarchy.nix` was based on the community `omarchy-nix` flake (`github:henrysipp/omarchy-nix`); it wasn't, so did a full deep-dive comparison (all ~30 files of that repo) to check. Found it architecturally deeper — a real `nix-colors` base16 theme-switching pipeline, fuller Wayland/Qt env glue, `hyprpaper`, `mako`, a much richer keybind set, window/layer rules — but unmaintained (the author has moved off it) and hard-conflicting with this repo's shared SDDM and networking modules (greetd, its own NetworkManager/Bluetooth/PipeWire stack, a source-built Hyprland, global `allowUnfree`). Asked directly for the downside of just importing it as a flake input; answered with six concrete conflicts, then chose to hand-port the good ideas instead. Reworked `omarchy.nix` onto `nix-colors` for real theme-switching (one `colorScheme` line now feeds Hyprland, Waybar, Hyprlock, and a newly-added `mako`, instead of hand-copied hex), and ported the "quick wins": Wayland/Qt/GTK env vars, a `swaybg` solid-color background (no bundled wallpaper asset in this repo), a fuller keybind set (`hyprshot`/`hyprpicker`/`clipse`, Omarchy's real Escape-key session menu), and window/layer rules — checking Walker's actual Rust source first to confirm it does its own blur via a Wayland protocol rather than needing a compositor layerrule. Along the way, investigated a hypothesized `XDG_DATA_DIRS` gap by checking the *live* session's real environment — found no gap (this repo's setup already covers it via a different path) and removed a line that referenced a path that doesn't exist here. Also built a new **`de-smoke-check`** skill (project-local) after realizing this repo's documented DE-module verification step silently skips the 9 DE modules no host imports — dogfooded immediately against `omarchy`, passed — plus a CLAUDE.md note pointing future work at it. Verified via `lib.deSmoke.omarchy` deep-eval (before and after the `XDG_DATA_DIRS` fix) and a full `flake-check` across all four real hosts. `omarchy.nix` is still not wired into any host.

**2026-07-10 (session 40)** — Two Hyprland-related DE module changes. First, wired up Dank Material Shell on `hyprland.nix` to match the integration `niri.nix` already had: DMS shell + systemd autostart, the qt6ct/kdeglobals Qt theming glue, DMS's System Check dependencies, and HM-managed swaylock/swayidle in place of plain packages. Mid-task, reading the actual DMS flake input source revealed it has no declarative NixOS/Home-Manager module for Hyprland (unlike niri) — since Hyprland 0.55 DMS manages Hyprland's own config imperatively via `dms setup` (a Lua-based config it generates/migrates at runtime) — so the "managed hyprland.conf with niri-style keybinds" part of the original ask was dropped rather than shipping something DMS would silently back up on first run. Second, researched and built a brand-new **Omarchy**-style DE module (`omarchy.nix`) via a 10-source parallel `/research` pass: confirmed Omarchy (DHH's opinionated Arch+Hyprland desktop) has no official NixOS support, and the existing community port `omarchy-nix`, while architecturally clean, hard-assumes a greetd+PipeWire+its-own-networking stack that would conflict with this repo's shared SDDM and desktop-networking modules (and is ~8 months stale) — so built a from-scratch module instead, replicating Omarchy's actual shell (Waybar + Hyprlock + Walker, confirmed via research — a different stack from DMS), Tokyo Night theming, and its keyboard-driven keybind philosophy, while keeping this repo's existing app choices (kitty, dolphin). Caught and fixed a real bug during verification: home-manager's Hyprland module is migrating its default config format from the classic `hyprlang` schema to Lua, which would have silently broken the new module's `settings` attrset — pinned explicitly. Neither module is wired into any live host; both verified via deep-eval of their `lib.deSmoke` targets (Hyprland/Omarchy aren't active on gaming/laptop/natalie-laptop) plus all four live hosts unaffected.

**2026-07-10 (session 39)** — Asked the `skill-suggestion` skill cold for another skill idea, with no prior conversation to mine, so it forked a sub-agent to sweep past-session transcripts instead. It found that `nix flake check`'s known shallow-evaluation gap (documented since the session-29 pnpm/vesktop incident: a broken package deep in `environment.systemPackages` can pass the check but fail a real rebuild) had a manual workaround — deep-evaluating each host's `toplevel.drvPath` — that had been run by hand across a dozen-plus past sessions but never turned into a skill. Built a new project-local **`deep-eval-check`** skill that loops the deep-eval trick over all four hosts and reports per-host pass/fail; live-tested it immediately and all four hosts came back clean. While shipping it (propose → build → test → commit → push), noticed that exact sequence was itself a recurring, un-skilled workflow, so built a second, global **`ship-skill`** orchestration skill that chains `skill-suggestion`/`new-skill` into a smoke-test, a commit, a deliberate confirmation pause, and finally a push — the pause exists because by that point the skill has already made two consequential decisions (built + verified working) with no user checkpoint, so it declines to make a third (pushing) without asking. Ran a self-paced `/loop /ship-skill` afterward to look for a third gap; the mining sub-agent came back with a clean "nothing found" verdict (checked all 76 transcripts, the full skill roster, and memory), so the loop stopped itself after one iteration exactly as designed. Also fixed a stale assumption carried from the previous session: managed-global skill changes turned out to go live in the *same* running session immediately after a rebuild, with no new session required. Both new skills are already committed and pushed; `deep-eval-check` is project-local and was live immediately, `ship-skill` needed (and got) a rebuild before its symlink appeared.

**2026-07-10 (session 38)** — Fixed Dolphin (the KDE file manager added last session) rendering light-themed despite the system's dark qt6ct/matugen theming. Root cause was two independent gaps stacking: unwrapped Qt binaries only search plugin directories baked into their own build-time closure, so `QT_QPA_PLATFORMTHEME=qt6ct` was never actually resolvable without a `QT_PLUGIN_PATH` pointing at the merged system profile — confirmed by a plain Qt app (qBittorrent) picking up the dark theme correctly once that was added, while Dolphin still didn't. That's because full KDE Frameworks apps resolve their color scheme through a separate mechanism (`KColorScheme` reading `kdeglobals`), entirely independent of the Qt platform theme; a 4-agent research pass across NixOS/Arch community threads and an upstream nixpkgs issue found the fix (`[UiSettings] ColorScheme=*`), applied via a `kwriteconfig6`-based Home Manager activation script rather than a static config file, since KDE apps actively write real state back into `kdeglobals` (recent files, window geometry) and a read-only managed copy would break that. Verified live at every step via screenshots of the running desktop. Separately, ran the `/improve-system` skill-maintenance orchestrator end-to-end for a second time: a 5-agent parallel audit swept all 50 skills in the library and found 14 real issues — six verified correctness bugs (a broken nix command already flagged-but-unfixed in its own troubleshooting notes, a wrong systemd unit name, a wrong NixOS option path, a VPN client template that would silently break IPv6 for new peers, a broken offline package-search fallback, and a bug in the audit tooling's own scanning script), one hardcoded path fixed to match its four sibling skills, four deterministic-step extractions, and three prompt-to-structured-question conversions — all approved and applied. A new project-local **`wayland-screenshot`** skill was also added, generalizing the live-screenshot verification technique used throughout this session. Nine new read-only command patterns were added to the permission allowlist based on actual usage. Claude-tooling changes reach `~/.claude` on the next rebuild + reboot + a new session; the NixOS config fix (`modules/desktop-environments/niri.nix`) and all project-local skill fixes are already live.

**2026-07-09 (session 37)** — Repo layout restructured after an organization review, verified as a functional no-op. `dotfiles/common/modules/` moved to top-level `modules/` (git mv, history preserved), so `dotfiles/` now holds only Home Manager material. The three desktop hosts' ~95%-identical `networking.nix` files and copy-pasted app lists were deduplicated into shared `modules/desktop-networking.nix` and `modules/desktop-apps.nix`; host files keep only their WireGuard address, extra SSH users, and host-unique packages. `mkSystem` was reworked to take a host name (deriving `networking.hostName` and injecting `specialArgs` once), `stateVersion` moved per-host, and `vpn.nix` joined `desktopModules`. New DE-rot protection: a `lib.deSmoke` flake output evaluates the laptop config with each of the 11 DE modules swapped in, exercised by a weekly CI `de-smoke` job — which caught real rot on its first run (pantheon's terminal package renamed upstream, three deprecated `mate.*` aliases), all fixed. Verified by drvPath comparison: vpn-server bit-identical to the pre-refactor baseline, desktop hosts field-identical (package sets, flatpaks, sshd, DNS, wg); dry-run on gaming showed zero package changes; CI green on both jobs.

**2026-07-09 (session 36)** — Ran `/improve-system` end-to-end again, this time cold with no prior scoping ("let's test this out"). Claude-tooling only — no NixOS system config touched. `skill-upgrade` mined transcripts across several past sessions (not just the live one) and hardened `session-closer` with two recurring gotchas: a helper script that kept being invoked with the wrong relative path, and this project's own `project-state.md` having grown too large for a single `Read`. `skill-suggestion` noticed the transcript-mining logic itself had been hand-rewritten from scratch in at least three prior runs and built a reusable script for it. A parallel, 4-way sub-agent audit swept all 49 skills in the library and, with explicit approval to apply the full report, fixed four verified bugs (a host-key helper that broke specifically on the VPN server, a stale flake-input cross-reference, a duplicated IP that could silently drift from its source of truth, and a wrong reference table already made redundant by a live read elsewhere), consolidated a path independently hardcoded across four related skills into one shared file, extracted several inlined templates and command sequences into proper `scripts/`/`assets/` files, and converted a dozen free-text confirmation prompts to the structured question tool. `fewer-permission-prompts` added three more read-only permission entries, and — asked directly — the decision was to keep them alongside earlier ones already made redundant by a broader existing allow rule, since removing them would cost nothing to reprove later. All changes verified with a clean dry-run at each step; committed and pushed. Global-skill changes reach `~/.claude` on the next rebuild plus a new session; project-local changes are already live.

**2026-07-09 (session 35)** — Fixed Qt application theming (e.g. qBittorrent) on niri hosts under DMS, for real this time: an earlier same-day fix added the `qt6ct` theme engine and pointed `QT_QPA_PLATFORMTHEME` at it, but Qt apps stayed on Qt's default light style. Root cause, found by reading DMS's own logs and settings rather than guessing: DMS's theming-apply toggle was off in its own settings, and even once enabled, DMS's backend only refreshes an *existing* `qt6ct.conf` — it never creates one from scratch, and this host never had one (that file is normally seeded once via a one-time manual step that had never happened here). Added `qt6ct.conf` as a Home Manager-managed file (gaming and laptop), pointing at DMS's dynamically-generated color scheme. Deliberately left DMS's own settings file and generated color file unmanaged, since both are rewritten live by DMS itself and a read-only Nix copy would break that. Verified live on gaming (wrote the file by hand, restarted qBittorrent, confirmed via screenshot it rendered dark) before committing the Nix-managed version; dry-run on gaming and a direct evaluation of laptop's configuration both passed clean. Staged for next boot only; not yet applied.

**2026-07-09 (session 34)** — Brought Niri's `config.kdl` under Home Manager management on gaming and laptop, replacing a plain mutable file with no version history. DMS's own generated theme/settings state (`DankMaterialShell/settings.json`, `niri/dms/*.kdl`) was deliberately left unmanaged since it's written live by DMS's own settings UI. Also made a few requested tweaks: removed niri's stock `spawn-at-startup "waybar"` default (DMS's own shell is the actual bar here; waybar isn't installed), added `Mod+Return` (kitty) and `Mod+B` (browser, via `xdg-open` with a Zen flatpak fallback) keybinds, and reduced window gaps from 16px to 8px. Verified via dry-run on gaming and a direct eval of laptop's configuration. Staged for next boot only; not yet applied.

**2026-07-08 (session 33)** — Switched gaming's desktop environment from Plasma 6 to Niri ahead of a gaming test on that hardware. Plasma's Wayland session was already proven working on this nvidia GPU, so the swap itself is low-risk; added Niri-specific nvidia session variables (`GBM_BACKEND`, `__GLX_VENDOR_LIBRARY_NAME`, `LIBVA_DRIVER_NAME`) to `nvidia.nix` since Niri is Smithay-based (not wlroots) and doesn't get KWin's automatic vendor-lib selection. Everything else needed for gaming under Niri — Steam, gamescope, gamemode, controller udev rules, `xwayland-satellite` for X11-only overlays, SDDM+Niri session — was already in place and proven on `laptop`. Dry-run passed clean (niri + its XDG portal added, KDE removed, ~1.48 GiB smaller closure). Staged for next boot only; not yet applied.

**2026-07-07 (session 32)** — Routine flake update via the `/flake-update-verify` loop: bumped `dms`, `financeguru`, `home-manager`, and `nixpkgs` (`nixpkgs-stable` unchanged). Verified with both `nix flake check` and a deep-eval of every host's full system derivation (the stricter check adopted after session 29's shallow-check incident) before committing and pushing. Lock-only — activates on each host's next rebuild, stacking on top of the still-unapplied session-29/31 bumps.

**2026-07-04 (session 31)** — Routine flake update: bumped `nixpkgs`, `home-manager`, `sops-nix`, `dms`, and `financeguru`. Dry-run verified a clean evaluation (KDE Plasma 6.7.2 fleet-wide, `tmux` 3.7, `starship` 1.26.0, plus smaller package moves) before committing and pushing. Lock-only — activates on each host's next rebuild, stacking on top of the still-unapplied session-29 kernel bump.

**2026-07-03 (session 30)** — Two small maintenance items. (1) natalie-laptop's LAN address changed after a router DHCP reassignment (the host deliberately has no static IP — it's a roaming laptop on plain DHCP with no reservation), so the host-data source of truth (`.claude/hosts.json`) and the Home Manager-managed SSH alias were updated to the new address; the new address was verified live over SSH before editing, and the config still evaluates cleanly. The SSH alias fix reaches each machine's `~/.ssh/config` on its next rebuild. (2) Retired the project-local `commit`/`push` Claude skills in favor of the global `git-commit`/`git-push` pair — after an earlier session closed the behavioral gaps between them, the two pairs were near-identical duplicates with overlapping trigger phrases; the one unique asset (the repo's conventional-commit format reference) moved into `CLAUDE.md` as a "Commit Style" section, and the six skills that referenced the deleted pair were retargeted.

**2026-07-02 (session 29)** — Lifted the zen-kernel nixpkgs pin (upstream ships `bzImage` again in linux-zen 7.1.2 — verified by building the kernel from the binary cache before committing) and bumped all five flake inputs. Added **GitHub Actions CI**: every push and PR now evaluates all four hosts. The first CI version was immediately humbled by a real incident: the fresh nixpkgs marks vesktop's pinned build tool (`pnpm-10.29.2`) insecure, which broke evaluation on all three desktop hosts — yet both the local `nix flake check` and CI passed, because flake check verifies NixOS configurations only shallowly. A dry-run caught it; the workflow now **deep-evaluates every host's full system derivation**, a temporary (loudly-commented) `permittedInsecurePackages` waiver unblocks rebuilds until the already-merged upstream vesktop fix reaches nixos-unstable, and the lesson was written into the relevant skills as gotchas. Also: a backup-system proposal was scoped and deliberately scrapped (all irreplaceable data is already covered — GitHub for projects, an existing cloud for photos/videos; Jellyfin state loss accepted), and a new project-local `ci-status` skill reports/triages the latest CI run.

**2026-07-02 (session 28)** — Fixed OnlyOffice's font detection for real: the earlier fix (copying real font files instead of symlinking) turned out insufficient because the sandbox's own build process re-creates symlinks over those files during a later merge step; a second override now dereferences them after that merge, and the fix is confirmed working live on gaming. Separately, ran the `/improve-system` skill-maintenance command end-to-end and fixed all 13 issues its audit surfaced across the Claude skill library — most notably a silent bug in the audit tooling's own enumeration script (it was failing to scan roughly half the skill library without any visible error), a stale attribution line duplicated across four skills, and a couple of skills that assumed passwordless sudo where none exists on this fleet. Each fix was verified and committed individually.

**2026-07-02 (session 27)** — A pre-push `secret-scan` found plaintext password hashes still publicly reachable on five old version-tag branches (`3.5`, `4.0`, `4.1`, `4.1.1`, `4.1.2`) that the 2026-06-15 history rewrite never touched (it only rewrote `main`). Deleted all five from the public remote and verified clean with a repo-wide history sweep; password rotation was offered and declined as low-risk. Added a global **`research`** skill: given a topic, it web-searches, fans out `n` (default 10, overridable) fresh sub-agents in parallel to each review one top result in depth, and reports back a synthesized consensus (majority view, notable dissent, per-source takeaways) in chat. Claude-tooling only — no NixOS system config touched. Reaches `~/.claude/skills/` on the next rebuild + a new session.

**2026-07-01 (session 26)** — Fixed X11-only apps (OnlyOffice) failing to open on niri: niri ≥ 25.08 spawns its own `xwayland-satellite` and sets `DISPLAY` automatically, so a manually-added service + global `DISPLAY` export was removed after it broke the SDDM greeter (the global export leaked into SDDM's own Wayland session, crashing it to a black screen). Separately, attempted to fix OnlyOffice not finding system fonts on NixOS (`fonts.fontDir.enable` + `corefonts` in the shared `fonts.nix`, plus a `/usr/share/fonts` symlink bridge to the fontDir tree) — the symlink bridge was later reverted without a recorded reason, so whether OnlyOffice actually sees fonts now is unverified.

**2026-06-30 (session 25)** — Added VirtualBox to the natalie-laptop host so it can run a Windows virtual machine. A new `hosts/natalie-laptop/virtualisation.nix` module enables the VirtualBox host with its extension pack (USB 2.0/3.0 passthrough, RDP, disk encryption) and grants both user accounts permission to manage VMs; the Guest Additions ISO ships with the host package, so no separate install is needed. Verified by evaluating the natalie-laptop configuration directly (the local machine is a different host, so the standard dry-run would have built the wrong one) — the full VirtualBox stack, including kernel modules matched to the zen kernel, evaluated cleanly. Also corrected a stale note in the project guide that still described natalie-laptop as running Cosmic; it has run Plasma 6 since early June. Activates on the host's next rebuild + reboot (the extension pack triggers a from-source build).

**2026-06-29 (session 23)** — Small maintenance pass. Made the `session-closer` skill read the session's on-disk transcript as its source of truth — the live context window summarizes and drops early turns as a conversation grows, so a close now reads the unsummarized transcript log to capture decisions and rationale accurately (with a guard to extract only the relevant turns rather than load multi-megabyte files). Removed the `brobot` host module from the gaming machine (`hosts/gaming/brobot.nix` and its flake import) since that project is discontinued — a dry-run confirmed it cleanly drops `yt-dlp` and its dependency closure (~187 MiB) while leaving `ffmpeg`/`nodejs`, which other modules still use. Also ran a consistency check confirming the system-managed permission policy (the destructive-command guardrails and fork-bomb hook, deployed via NixOS) is a deliberately separate, higher-precedence layer from the per-project permission allow-list — they are intentionally not merged.

**2026-06-28 (session 22)** — Ran the new `/improve-system` orchestrator end-to-end for the first time and acted on its findings across the Claude skill library (Claude-tooling only — no NixOS system config touched). A read-only audit (fanned over the whole library via sub-agents) surfaced real drift bugs, each verified against the live config before fixing: a VPN-status helper that referenced the wrong subnet, a package helper routing to a non-existent host, a VPN-peer helper pointing at the wrong key location (the repo wires WireGuard keys via sops-nix), and a few stale host lists. Then two consolidation passes: the desktop-host roster and per-host config paths — previously re-typed across four skills, which is how the stale host crept in — were lifted into a single source of truth (`.claude/hosts.json`); and large inlined templates and fixed command sequences were extracted into per-skill `assets/`/`scripts/`, with the package-search skill now preferring the live `nixos` MCP over a slow shell search and the session-closer delegating its public-repo secret check to the existing scan skill. The repo-managed global skills that gained companion files were switched to recursive-directory symlinks so future additions need no extra wiring. Verified with a dry-run; the global-skill changes activate on the next rebuild + a fresh session.

**2026-06-28 (session 21)** — Gave the two session-reactive maintenance skills awareness of past conversation history. `skill-upgrade` and `skill-suggestion` previously analyzed only the live session; both now also `grep` the project's stored transcripts (under `~/.claude/projects/`) so that a skill misfire recurring across sessions is treated as the highest-value fix, and a workflow repeated across many sessions is recognized as the strongest candidate for a new skill. Both carry a grep-not-read guard since transcripts can be multi-megabyte. The other skills `/improve-system` chains were left as-is (`claude-rules` and `skill-audit` don't need history; `fewer-permission-prompts` already scans it). Claude-tooling only — no NixOS system config touched.

**2026-06-28 (session 20)** — Two changes, both to the Claude tooling layer (no NixOS system config touched). (1) Added a global **`/improve-system`** orchestrator skill that chains the five Claude-ecosystem maintenance skills — `skill-upgrade`, `skill-suggestion`, `claude-rules`, `skill-audit`, and `fewer-permission-prompts` — into a single pass: it auto-applies low-risk additive changes (skill gotchas, CLAUDE.md rules, permission-allow entries) and gates structural ones (new skills, audit refactors) behind confirmation, invoking each sub-skill rather than reimplementing it. (2) Scrubbed operationally-sensitive network detail (the VPN server's public endpoint address and the VPN/LAN subnets + per-host addresses) out of this public README and added a "no sensitive information" guardrail to the `session-closer` skill's README step so future updates keep it clean — a README-only change; the values still live in the host config where the tunnel needs them.

**2026-06-26** — Routine flake update with one caveat: the 2026-06-26 `nixos-unstable` rev shipped a broken `linux-zen-7.0.12` (installs `vmlinuz` but declares its target as `bzImage`, failing the bootloader sanity check on all zen-kernel hosts — vpn-server, on standard `linuxPackages`, is unaffected). The dry-run caught it, so **`nixpkgs` was pinned back lock-only** to the prior good rev `567a49d1` while the other five inputs (`dms`, `financeguru`, `home-manager`, `nixpkgs-stable`, `sops-nix`) were kept at latest. The next `nix flake update` will lift the pin automatically once upstream ships `bzImage` again. Lock bump only — not yet applied.

**2026-06-23** — Full audit and overhaul of the Claude skill library (7 commits, no NixOS system config changed). Added a **4-bucket single-responsibility gate** to `new-skill` and a new global **`/skill-audit`** meta-skill that sweeps every skill against that gate plus five structural lenses (deterministic→`scripts/`, templates→`assets/`, re-entered-config→shared JSON, pick-ones→AskUserQuestion, inputs→`## Arguments`). New skills: **`bump-input`** (bump one flake input to latest), **`save-memory`**, and **`team-member-synthesize`** (split out of `team-member-ingest`). Fixed real drift bugs the audit surfaced — `/vpn-status` was broken (used the denied `ubuntu@` user; corrected fleet-wide to `bosko@`, verified live), the commit skills stamped the wrong model trailer, `new-host`'s DE list named five nonexistent modules, and `pin-input`'s input roster was stale (DE list and roster now enumerate live). Centralized host SSH targets / IPs / the WireGuard peer map into **`.claude/hosts.json`** (single source of truth) and rewired the host-touching skills + `fleet-status`/`vpn-status` scripts to read it; removed the phantom `nixos-server` host. Extracted deterministic work into per-skill `scripts/` (and a shared `.claude/lib/flake-lock-diff.sh`), moved `new-host`'s nine inlined Nix templates into `assets/` (493→180 lines), and added AskUserQuestion/`## Arguments` polish across ~11 skills. **Pending:** the `dotfiles/`-based skill changes (`new-skill`, `git-commit`, `team-member-*`, `/skill-audit`) go live in `~/.claude` after the next rebuild + reboot; project-local skill changes are already live.

**2026-06-22** — Bumped the **FinanceGuru** flake input for an upstream update (lock-only). Added a `.gitignore` rule for **loop run-logs** (`.claude/loops/**/output-*.md` and `memory-*.md`) so each loop run's dual-file output stays local instead of cluttering `git status`. The loop *skills* themselves live in `.claude/skills/` and remain tracked (portable to any clone); loop *config* files like `.claude/loops/public-repo-guard/baseline-allowlist.md` stay tracked too — only the dated per-run logs are ignored.

**2026-06-21** — Generated the first three loops with `/create-loop` (confirming the meta-skill works end-to-end). Each is a project-local, self-orchestrating loop under `.claude/skills/`, runnable immediately: **`/fleet-rollout`** deploys the committed config across `gaming → laptop → natalie-laptop → vpn-server` one host at a time — dry-run gate → `switch` (activate live) → full health sweep (new generation, zero failed units, no reboot-pending, fresh WG handshake on vpn-server), advancing only on green; local host via `nh os switch`, the rest via `remote-rebuild`. **`/flake-update-verify`** runs `nix flake update` → `flake-check`, then **commits the `flake.lock` bump without applying it** (apply is `/fleet-rollout`'s job), and **restores the previous lock** if the update breaks eval. **`/public-repo-guard`** runs `secret-scan` + `audit-config`, triaging every finding against a seeded baseline allowlist (`.claude/loops/public-repo-guard/baseline-allowlist.md`) and passing only at zero genuine findings — a pre-push / periodic guard for this public repo. All three default to Loop Training Mode ON with a retry cap of 3 and dual-file run output under `.claude/loops/<name>/`.

**2026-06-20** — Added a global **`/create-loop`** meta-skill (`dotfiles/bosko/claude/skills/create-loop/SKILL.md`, wired into `bosko-claude.nix`). It interviews for a task's goal, ordered steps, and done-rule, then generates a **project-local, self-orchestrating loop skill** at `.claude/skills/<loop>/SKILL.md` that runs as a single command `/<loop-name>` — no rebuild needed (only `/create-loop` itself, being repo-owned, requires one to activate). Every generated loop bakes in: **Loop Training Mode** (a top-of-file toggle, ON by default — ON pauses at each step for approval, skips steps already passing their done-rule, re-runs only failures, respects a retry cap; OFF runs autonomously but keeps the done-rule checks and the cap), a **retry cap** (default 3/step), **dual-file output every run** (`.claude/loops/<loop>/output-<date>.md` for the artifact + `memory-<date>.md` for what worked/failed/remember-next, read back at the next run's start), and a **verification plan** where the done-rule is the bar. Generated loops are project-local and standalone by design (confirmed via `/interview`): no rebuild friction, and each loop is a complete, inspectable artifact rather than a thin `/loop` wrapper.

**2026-06-18** — Rewrote the `Parallelize with Sub-Agents` standing workflow rule (in both `CLAUDE.md` and the global `claude-rules` skill) so it actually triggers. The old wording was an aspirational value with no trigger condition and was silently losing to the harness's "don't spawn agents unless asked" default. The new version grants explicit standing authorization to spawn sub-agents, frames the parallelization check as a mandatory pre-task gate, and lists observable trigger conditions (2+ independent research areas / 2+ unreferenced files / planning "A→B→C where B doesn't need A"), while naming the coupled-edits exception (a module + its `flake.nix` import are correctly serial) so the rule no longer reads as self-contradictory. Docs-only; the skill text reaches `~/.claude` on the next rebuild.

**2026-06-17** — Bumped the `nixpkgs-stable` input from EOL **`nixos-25.05`** to current stable **`nixos-25.11`** (Xantusia) and redeployed vpn-server onto it. The deploy surfaced three latent issues, all fixed/captured: (1) clamped `wg0` MTU to **1380** in the shared `vpn.nix` — wg-quick's default 1420 exceeded the Oracle tunnel's ~1400-byte path MTU, silently truncating large `cache.nixos.org` downloads; (2) the `remote-rebuild` skill now uses `--build-host` (the x86_64 deployer can't build the aarch64 closure) and `--elevate=sudo` (replaces deprecated `--use-remote-sudo`); (3) vpn-server must be deployed via **`nixos-rebuild boot` + reboot**, not `switch` — `switch` over SSH ties activation to the SSH pipe, and the network restart drops the connection and corrupts the half-applied firewall/NAT (this, not a 25.11 regression, took the VPN down mid-session; recovered via a detached rollback). Both deploy gotchas are now documented in the `remote-rebuild` skill. The `mtu = 1380` clamp applies to the desktop clients on their next rebuild.

**2026-06-17 (session 12)** — Promoted the **mcp-nixos** server from project scope to **user scope** so Claude Code can query live NixOS package/option/version data in every project, not just this repo. The redundant project `.mcp.json` was removed (`claude mcp add … --scope user` registers it in `~/.claude.json` instead). To stay reproducible, a new `home.activation.claudeMcpServers` block in `dotfiles/bosko/bosko-claude.nix` idempotently reconciles the `.mcpServers.nixos` entry with `jq` on every rebuild — `~/.claude.json` is mutable state Claude rewrites constantly, so it's reconciled rather than symlinked, matching the existing `claudeAllowList`/`claudeNixdPlugin` activation pattern.

**2026-06-17 (later)** — Fixed `wg-quick-wg0.service` failing on rebuild. Disabling IPv6 the day before (below) left `"::/0"` in the WireGuard peer's `allowedIPs`; `wg-quick` installs a route per allowedIP, so `ip -6 route add ::/0 dev wg0` failed on the now-IPv6-disabled device and `wg-quick` tore the tunnel down — meaning the VPN had silently failed to come up on every boot since. Dropped `"::/0"`, leaving `allowedIPs = ["0.0.0.0/0"]` (still a full tunnel with no IPv6 to leak), and added a `NOTE:` so it isn't re-added while IPv6 is off. Applies to all three clients via the shared `vpn.nix`. Also added an **mcp-nixos** MCP server (`pkgs.mcp-nixos`) so Claude Code can query live NixOS package/option data (promoted to user scope the same day — see latest entry).

**2026-06-17** — Fixed Jellyfin artwork/metadata failing to load on gaming. The cause was a VPN routing issue, not Jellyfin: TMDb resolves to IPv6-only addresses, but the full-tunnel WireGuard client routes `::/0` into a v4-only `wg0`, so all IPv6 traffic black-holed and TMDb requests hung the 100s HTTP timeout. Added `networking.enableIPv6 = false` to the shared `modules/vpn.nix` to force IPv4 fallback through the tunnel — fixes the black-hole while keeping the full-tunnel guarantee (no IPv6 leak). Applies to all three VPN clients (gaming, laptop, natalie-laptop); gaming needs a rebuild + reboot to activate.

**2026-06-16 (later)** — Enabled sudo **password masking** fleet-wide: `security.sudo.extraConfig = "Defaults pwfeedback";` in `modules/security.nix` (part of `commonModules`) so the sudo prompt now prints a `*` per typed character. The historical pwfeedback CVE (CVE-2019-18634) is fixed in the shipped sudo, noted inline. Confirmed working on gaming.

**2026-06-16** — Stood up a **Jellyfin media server** on gaming (`hosts/gaming/jellyfin-server.nix`): native `services.jellyfin` backed by the spare 1TB Samsung SSD (ext4, mounted at `/mnt/media`, pinned by UUID), NVENC hardware transcoding via the RTX 3070, and a firewall scoped to LAN (`enp4s0`) + WireGuard (`wg0`) only — no internet-facing ports. A shared `media` group with setgid library dirs keeps manual drops readable by the scanner. `jellyfin-media-player` added to `desktopModules` so every desktop host ships the client. **Confirmed working on all devices.** Replaced the headless `qbittorrent-nox` service with the `qbittorrent` desktop GUI app (drops the listening Web UI; saves to `/mnt/media/downloads` so finished torrents stay scannable). Added a project-local `verify-service` skill — a read-only post-rebuild service health sweep — distilled from this session's by-hand verification.

**2026-06-15 (later)** — Added a fourth standing workflow rule, **Use Existing Skills First**, to the global `claude-rules` skill and to `CLAUDE.md`: prefer an existing skill over doing a task by hand (complements the existing "Skill Awareness" create-a-skill guidance). Also corrected the `famdash` SSH host login in `dotfiles/common/configs/ssh.nix` (`natalie` → `natty`).

**2026-06-15** — Adopted [sops-nix](https://github.com/Mic92/sops-nix) so the repo can be made public without leaking secrets (supersedes the 2026-05-20 decision to defer agenix). Login password hashes (`bosko`, `natty`) and all four per-host WireGuard private keys are now encrypted in `secrets/` and consumed via `hashedPasswordFile` / `privateKeyFile`; `users.nix` no longer contains plaintext hashes. Each host decrypts with an age key derived from its SSH ed25519 host key; the admin key stays at `~/.config/sops/age/keys.txt` (out of repo). WireGuard keys verified identity-preserving (derived pubkeys match the server's registered peers) and all four hosts rebuilt with live handshakes confirmed. Git history was rewritten with `git filter-repo` to purge the old plaintext password hashes from all commits. See the [Secrets](#secrets) section. _(The VPN endpoint address and tunnel subnet, originally noted here as public, are now kept out of this README — see Secrets.)_

**2026-06-09** — Three sessions of Claude Code policy work. (1) `modules/claude-code.nix` created to deploy `/etc/claude-code/managed-settings.json` system-wide; also: `gemini-cli` added to natty's user packages; `nodejs`, `pnpm`, `p7zip`, and `jq` consolidated from per-host `environment.nix` files into `shell.nix`. (2) Rebuilt and rebooted gaming to activate the managed policy; trimmed personal `~/.claude/settings.json` manually using the one-shot trim script, then deleted the script. (3) Replaced the deleted script with a declarative `home.activation.trimClaudeSettings` in `dotfiles/bosko/bosko-claude.nix`: on every rebuild, once the managed file exists, the activation script uses `jq` to strip redundant deny/ask/hooks keys while leaving personal prefs intact. Idempotent. laptop and natalie-laptop still need rebuild+reboot to activate the managed policy.

**2026-06-05/04** — natalie-laptop switched from Cosmic to Plasma 6. `krohnkite` (KWin tiling script) added to shared `plasma.nix` so both gaming and natalie-laptop get it automatically. FinanceGuru personal finance app added to gaming and natalie-laptop as a flake input (`github:CommanderBosko/FinanceGuru`). lutris re-enabled on gaming — the upstream `openldap-2.6.13-i686-linux` binary cache regression that blocked it since May 2026 is resolved in nixpkgs-unstable.

**2026-06-03** — Security audit and hardening pass. Multi-agent review across all six security domains identified three real findings. Two remediated: (1) `PermitRootLogin` changed from `"prohibit-password"` to `"no"` on vpn-server; `security.sudo.wheelNeedsPassword = false` added for non-interactive `nixos-rebuild --use-remote-sudo` deploys as bosko; (2) SDDM `autoLogin.enable = false` on gaming (now active and confirmed). Third finding — Avahi mDNS `openFirewall = true` in `printing.nix` — deferred: closing it would break printer discovery; interface-scoped firewall rules are the correct fix. Updated `remote-rebuild` skill to target `bosko@<vpn-endpoint>` (address resolved from `.claude/hosts.json`).

**2026-05-26** — Deployed a local LLM stack on gaming. New `hosts/gaming/hermes-agent.nix` enables Ollama with CUDA acceleration (`pkgs.ollama-cuda`) and Hermes Agent service, both pointing to `http://localhost:11434/v1`. Model settled on `mistral-nemo:12b` after iterating through four options: it is the only model satisfying Hermes Agent's 64K context minimum, fitting in 8GB VRAM (~7GB Q4), and providing good tool-calling support. `bosko` added to the `hermes` group so the interactive `hermes` CLI can traverse `/var/lib/hermes`. `hermes-agent` added as a flake input. Also corrected the SSH config module from the deprecated `programs.ssh.matchBlocks` API to `programs.ssh.settings`, capitalised option keys, added `enableDefaultConfig = false`, and added a global keepalive stanza.

**2026-05-25** — Migrated `~/.ssh/config` into Home Manager declaratively. New `dotfiles/common/configs/ssh.nix` defines all five SSH hosts (`natalie-laptop`, `laptop`, `gaming`, `pi-hole`, `famdash`) with explicit `Hostname` and `User` fields. Imported from `home.nix` so both users receive the managed config automatically on each rebuild. The hand-maintained `~/.ssh/config` on each host should be removed after the next rebuild.

**2026-05-22** — Added `vpn-on` / `vpn-off` shell aliases to `shell.nix`. Uses the systemd service (`wg-quick-wg0`) rather than `wg-quick` directly (the binary is not in PATH for non-root zsh sessions). Added `boot.kernelParams = lib.mkAfter [ "audit=0" ]` to `hosts/vpn-server/configuration.nix` to suppress the Oracle ARM kernel's broken audit subsystem. Replaced deprecated `nixfmt-classic` with `nixfmt`. Added `zsh.shellInit` sourcing of `hm-session-vars.sh` so `sessionPath` and `sessionVariables` are available in non-login shells.

**2026-05-21 (evening)** — dbus-broker transition completed on all five hosts. Root cause of resistance identified: `nix-flatpak` bundles its own older nixpkgs (`da5ad661`) that still defaults `dbus.implementation` to `"dbus"` via `mkDefault`, silently winning over nixpkgs-unstable's newer `mkDefault "broker"`. Fix: added an explicit (non-default) `services.dbus.implementation = "broker"` in `security.nix`; this plain assignment beats all `mkDefault` values regardless of source. Removed `server` host placeholder from `flake.nix` and `hosts/server/` — no physical hardware exists; will be re-added via `/new-host` when hardware arrives. Added `home.sessionPath = [ "$HOME/.local/bin" ]` to `dotfiles/bosko/bosko-claude.nix` so the native claude-code binary at `~/.local/bin/claude` is discoverable in PATH after rebuild.

**2026-05-21 (continued)** — nixpkgs channel split complete. Added `nixpkgs-stable` input (`nixos-25.05`) to `flake.nix`; server and vpn-server now pin to the stable channel while desktop hosts remain on `nixos-unstable`. vpn-server deployed via remote rebuild and confirmed running `25.05.20260102.ac62194 (Warbler)` with all three WireGuard peers intact. Stale VPN private-key pending items removed from docs — all clients have been fully operational since 2026-05-20. M-9 milestone complete.

**2026-05-21** — CLAUDE.md documentation audit: corrected natty's user permissions (she is wheel/sudo and a Nix trusted user, same as bosko); removed `virtualisation` from the `desktopModules` description (it is gaming-only); added `disko` to vpn-server module composition; fixed the directory layout tree. Added five new skills: `diff-generations` (nix store diff-closures between generations), `flake-check` (validate flake across all five hosts), `journal` (tail journald locally or via SSH), `nix-repl` (print repl invocation with host-specific starters), and `fmt` (alejandra/nixpkgs-fmt fallback for changed .nix files). Skill library now at 22 skills.

**2026-05-20** — Claude Code skill library completed. 18 project-local skills now live under `.claude/skills/`. The second build session added: `ssh-host` (short-name SSH resolver), `remote-rebuild` (headless deployment via `nixos-rebuild switch --target-host` to vpn-server or server), `rollback` (generation listing + confirm + `switch --rollback`), `search-pkg` (`nix search nixpkgs` wrapper with add-package nudge), `new-host` (interactive scaffolder for desktop/server/remote-arm host types with correct template per type), and `pin-input` (flake input pinning to a rev/tag — lock-only or permanent, with home-manager/disko follow-input warnings). The first build session earlier the same day added `nixos-dry-run`, `nixos-rebuild`, `nixos-gc`, `vpn-status`, `new-module`, `commit`, `push`, `update`, `add-package`, `add-flatpak`, `switch-de`, and `new-peer`. agenix for secret management was evaluated and deferred — VPN keys live at `/etc/wireguard/private.key` on each host and are never in the repo. _(Superseded 2026-06-15: secrets are now managed with sops-nix — see the [Secrets](#secrets) section and the 2026-06-15 entry.)_

**2026-05-18** — WireGuard VPN fully deployed. Oracle Cloud ARM vpn-server is live on its public endpoint; `wg0` active on the tunnel subnet with three configured peers (gaming, laptop, natalie-laptop). Shared `modules/vpn.nix` client module created with full-tunnel routing, DNS override (`1.1.1.1 8.8.8.8`), and `persistentKeepalive = 25`. Per-host VPN addresses configured in `hosts/*/networking.nix`. ARM build target fixed for `server-rebuild` alias (now builds natively on the server). binfmt aarch64 emulation added to gaming as offline fallback. natalie-laptop added as fourth WireGuard peer with real keys. `gh` added to common system packages.

**2026-05-17** — Inlined the Starship prompt configuration from an external `starship.toml` into a native `programs.starship.settings` attrset in `shell.nix`; removed the `xdg.configFile` symlink and deleted the TOML file. Fixed Nerd Font v3 symbol spacing for 9 language/tool glyphs in the inline config. Disabled auto-format globally across all Helix language configurations. Routine flake inputs bump (`lutris` openldap regression still unresolved upstream). Deleted stale `dotfiles/vpn/` directory (NordVPN remnants). Added `tmux` to gaming system packages.

**2026-05-15** — Repository layout restructured: host-specific directories moved from `dotfiles/<hostname>/` to a top-level `hosts/<hostname>/` directory; bosko-specific HM configs moved from `dotfiles/common/configs/` to `dotfiles/bosko/`. All `flake.nix` paths updated; dry-run passed cleanly.

**2026-05-12 to 2026-05-14** — Housekeeping and preparation for the gaming AMD card swap. `nvidia.nix` removed from `desktopModules` and added explicitly per-host (gaming, laptop, natalie-laptop) — dropping NVIDIA from gaming now requires only a one-line change in `flake.nix`. `claude-code` and `gemini-cli` consolidated into `users.users.bosko.packages` in `users.nix` (removed from per-host environment files). `mumble` moved from `users.nix` to `gaming/environment.nix` (gaming-only). `rebuild` alias changed from `nh os switch` to `nh os boot`. `cleanup` retention reduced from 5 to 3 builds. `home-manager.nix` refactored into a single nested block; natty's HM config now correctly imports `home.nix`. `discord` removed from gaming and laptop (vesktop is the replacement). `obs-studio` removed from laptop and natalie-laptop. Various natalie-laptop package list cleanups. `qdirstat` added to shell packages. Flake inputs bumped. `natalie-laptop` installed and running with Cosmic DE verified.

**2026-05-12** — `natalie-laptop` host config completed with real `hardware-configuration.nix` (Intel CPU, NVMe root, vfat /boot). Commented out `lutris` in `gaming.nix` to unblock `nh os boot`; `lutris` transitively requires `openldap-2.6.13/i686-linux` with no binary cache entry in nixpkgs-unstable. `faugus-launcher` covers the immediate need. Added `pnpm` to gaming. `rebuild` alias changed from `nh os switch` to `nh os boot`.

**2026-05-11** — Added `natalie-laptop` host (Cosmic DE, `natty` user restored without elevated privileges). Deleted `vpn.nix` (unused; to be rewritten when VPN is imminent). Fixed `audit-rules-nixos.service` activation failure by disabling the service via `lib.mkForce false` while keeping `auditd` running. Scoped `amd.nix` to gaming only. Fixed xwayland duplication in `niri.nix`. Added `dry-run` alias.

**2026-05-10 (evening)** — Pinned `services.dbus.implementation = lib.mkDefault "dbus"` in `security.nix` to prevent boot failure from nixpkgs-unstable's silent default change to dbus-broker (rev `4bd9165`, 2026-04-14). Laptop `nh os switch` completed successfully — first live switch since the 2026-05-06 security audit.

**2026-05-10** — Removed `bottles` from emulation.nix (unused). Moved `gaming.nix` out of `desktopModules` into the gaming host's own module list. Fixed nixpkgs-unstable breaking change: `programs.swaylock` and `services.swayidle` migrated from system scope to `home-manager.users.bosko` in `niri.nix`.

**2026-05-06** — Full security audit remediation. Replaced plaintext password with SHA-512 hash; recreated `security.nix` in `commonModules`; removed user `natty`; disabled SSH password auth on gaming and laptop; set `homeMode = "0700"`; bound qBittorrent to `127.0.0.1`; closed Steam remote-access firewall ports; replaced ntpd with chrony on all hosts; replaced NetworkManager with DHCP on server; restricted Avahi to virbr0; added swaylock/swayidle to laptop; removed php, nmap, and netcat; added `.gitignore`; fixed gaming boot `fmask`.

## Roadmap

- Apply rebuild + reboot on gaming, laptop, and natalie-laptop: activate managed Claude Code policy, FinanceGuru, package consolidation changes, the OnlyOffice font fix, and natalie-laptop's Plasma removal (niri-only now); remove hand-maintained `~/.ssh/config` on each host afterward
- Interface-scope Avahi mDNS: replace `openFirewall = true` in `printing.nix` with per-interface rules restricting UDP 5353 to the LAN interface
- AMD card swap on gaming: remove `nvidia.nix` from gaming's module list when card is physically replaced
- Harden vpn-server further (AppArmor profiles, fail2ban, rate-limiting on UDP 51820)
- Re-add `server` host via `/new-host` skill when physical hardware is available; pin to `nixpkgs-stable` following the vpn-server pattern

## License

Personal configuration — no formal license.
