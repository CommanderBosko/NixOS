# Memory Improvement Overview — <ISO8601 timestamp>

Status: PENDING_REVIEW

Source analysis: <path to analysis-<ISO8601>.md>
Generated: <human-readable date>

<!--
STATUS MARKER CONTRACT (the `dream` skill parses this -- keep the line above
in exactly this shape when editing this file by hand):

  - Must stay `Status: <VALUE>` on its own line, the first line in the file
    that starts with the literal text "Status:".
  - PENDING_REVIEW -- default. dream will not touch the Flagged section.
  - APPROVED       -- set this by hand once you've reviewed "Flagged for
                       Review" below and want it applied. dream applies it on
                       its next run (--mode full or --mode apply-fixes),
                       reports the result via send-results, then rewrites
                       this line to APPLIED itself.
  - REJECTED       -- set this by hand to decline the flagged changes without
                       deleting the file. dream treats this the same as
                       "don't apply" but won't nag about it again.
  - APPLIED        -- written by dream, not by hand. Don't edit this back to
                       APPROVED to re-run it -- run improve-memory again for
                       a fresh overview instead.
-->

## Auto-Applied Changes

Changes below were already made when this file was generated — no action needed from you.

(none this pass)

## Flagged for Review

Each item needs your explicit approval (set `Status: APPROVED` above) before `dream`
applies it. Every item states the **exact** change — applying it later should be
mechanical, never a fresh judgment call.

(none this pass)

## Project Coverage

<!-- One line per project this pass considered, so a gap is visible rather than silent. -->

(none recorded)
