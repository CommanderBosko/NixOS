# Planned Modes

Load this before adding a new mode to this skill (a `time-analysis)` case arm or similar).

## Mode: `time-analysis` (not yet built)

Reserved. When this is built, add a `time-analysis)` case arm to `scripts/scan-projects.sh`
alongside `dream)`, and a matching `## Mode: time-analysis` section in SKILL.md — don't change
`dream` mode's behavior to accommodate it.
